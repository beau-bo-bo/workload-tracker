# START-HERE.md — อ่านไฟล์นี้ก่อนเสมอ

> ไฟล์นี้คือจุดเริ่มต้นสำหรับ **แชทใหม่** หรือ **AI ตัวอื่น** ที่เพิ่งเข้ามารับงานต่อ
> อ่านไฟล์นี้จบแล้วจะทำงานต่อได้ทันที ไม่ต้องไปขุด `PROGRESS-ARCHIVE.md` ที่เก็บประวัติเก่าไว้
> อัปเดตล่าสุด: 2026-09-15/16 (หลังเซสชัน Code Review & Remediation)

---

## 1. โปรเจกต์นี้คืออะไร

**Workload Tracker** — ระบบติดตามภาระงานและ Workflow การส่งตรวจเอกสารประกอบการประชุม (Board / Excom) ขององค์กร (บริบท กฟผ.)
ผู้ใช้จริงคือเจ้าหน้าที่ไทย · UI ภาษาไทยเป็นหลัก (ยกเว้นฟีเจอร์ "The Wall/Memo" ที่ตั้งใจใช้ภาษาอังกฤษ)

**แนวคิดหลักของงาน:** เจ้าของงาน (Owner) สร้างวาระ → ส่งตรวจให้ผู้ตรวจ (Inspector) → ผู้ตรวจกด "ตรวจแล้ว" → วนได้หลายรอบ → เจ้าของกดปิดงาน
ทุกขั้นตอนถูกบันทึกลง Timeline ที่ตรวจสอบย้อนหลังได้

---

## 2. เอกสารในโปรเจกต์ — อ่านอะไรตอนไหน

| ไฟล์ | ใช้ตอนไหน | ขนาด |
|---|---|---|
| **`START-HERE.md`** (ไฟล์นี้) | เริ่มต้นทุกครั้ง | เล็ก |
| **`AGENTS.md`** | **กฎการทำงานถาวร — ต้องอ่าน** โดยเฉพาะข้อ 3 (Approval Gate) และข้อ 6.1 (กฎเทคนิค) | เล็ก |
| **`CODE-REVIEW.md`** | ผลตรวจโค้ดทั้งระบบ: บั๊ก 16 · performance 7 · clean code 18 · UX/UI 20 ข้อ พร้อมโค้ดตัวอย่าง | กลาง |
| **`FIX-INSTRUCTIONS.md`** | แผนงานที่เหลือ (Phase A–H) + **บันทึกการตัดสินใจของผู้ใช้ที่ถือเป็นข้อสรุปแล้ว** | กลาง |
| `PRODUCT.md` | ขอบเขต/สเปกการทำงาน (state machine อยู่ข้อ 5, กรณีบันทึกย้อนหลังอยู่ข้อ 5.1) | กลาง |
| `DESIGN.md` | สเปก UI/สี/layout (dialog ใหม่อยู่ข้อ 6.1) | ใหญ่ |
| `PLAN.md` | ลำดับ Phase การพัฒนา | กลาง |
| `PROGRESS-TRACKER.md` | สถานะปัจจุบัน + Handoff Note ฉบับล่าสุด (ฉบับเดียว) | เล็ก |
| `PROGRESS-ARCHIVE.md` | ประวัติเก่าที่ถูกแทนที่แล้ว — **ล้าสมัยและบางส่วนขัดกับระบบปัจจุบัน ห้ามใช้เป็นข้อมูลปัจจุบัน** | ใหญ่มาก (ไม่ต้องอ่าน) |
| `README.md` | วิธีติดตั้ง/รัน + สถาปัตยกรรมความปลอดภัย | เล็ก |

---

## 3. Tech Stack

- **Next.js 16** (App Router, TypeScript, Tailwind v4) · React 19 · Turbopack
- **Supabase (Postgres)** — project `workload-tracker` (ref `kalooxclwbnjunuxbjst`, ap-southeast-1)
- **Auth: เขียนเอง ไม่ใช้ Supabase Auth** — ตาราง `users` + รหัสผ่าน bcrypt ผ่าน pgcrypto ฝั่ง Postgres
  Session = JWT เซ็นเองด้วย `jose` เก็บใน httpOnly cookie (`src/lib/session.ts`, `src/lib/auth.ts`) · ป้องกัน route ด้วย `src/middleware.ts`
- **Test:** vitest `^2` (`npm test`) — ครอบ `src/app/task-workflow.ts` และ `src/app/board-derived.ts`
- **ขึ้นใช้งานจริงแล้ว 2026-09-16:** https://workload-tracker-kappa.vercel.app (Vercel, deploy อัตโนมัติทุกครั้งที่ push ขึ้น `main`)
  โค้ดอยู่ที่ https://github.com/beau-bo-bo/workload-tracker (private) · ระหว่างพัฒนายังเข้าผ่าน LAN `http://192.168.1.186:3000` ได้เหมือนเดิม
  ⚠️ **ตัวแปรสภาพแวดล้อมต่างกัน 1 ตัว:** บน Vercel ต้องเป็น `APP_SECURE_COOKIES=true` ส่วนบน LAN (http) ต้องเป็น `false`

---

## 4. ⛔️ กฎความปลอดภัยที่ห้ามละเมิด (เคยเกิดช่องโหว่ระดับยึดระบบมาแล้ว)

> **ประวัติ:** เดิมใช้ `NEXT_PUBLIC_SUPABASE_ANON_KEY` และเปิดให้ role `anon` เรียก RPC ได้ทั้ง 42 ตัว
> ผลคือ **ใครก็ได้บนอินเทอร์เน็ตยึดบัญชี admin ได้ใน 3 requests โดยไม่ต้อง login** (ยิงทดสอบจริงแล้วได้รายชื่อ 13 คนกลับมา)
> ปิดช่องโหว่แล้วเมื่อ 2026-09-15 — รายละเอียดเต็มที่ `CODE-REVIEW.md` ข้อ S1

1. **ห้ามใช้ `NEXT_PUBLIC_` กับคีย์ Supabase ทุกกรณี** (ตัวแปรนั้นถูกฝังลง JS ที่ส่งให้เบราว์เซอร์ทุกคน)
2. **เข้าถึงฐานข้อมูลจากฝั่งเซิร์ฟเวอร์เท่านั้น** ผ่าน `SUPABASE_SERVICE_ROLE_KEY` · `src/lib/supabase.ts` มี `import "server-only"` **ห้ามถอด**
3. **service_role ข้ามด่านตรวจทุกอย่างของฐานข้อมูล** → **ทุก Server Action ต้องตรวจสิทธิ์ผู้ใช้เองก่อนเสมอ**
   ห้ามเชื่อ parameter ที่ client ส่งมาว่าเป็นตัวตนจริง
4. **ห้ามสร้าง client component ที่เรียก Supabase ตรง ๆ**
5. **ข้อมูลที่ผู้ใช้ไม่มีสิทธิ์เห็น ต้องไม่ถูกส่งออกจากเซิร์ฟเวอร์ตั้งแต่ต้น** ห้ามส่งมาแล้วซ่อนด้วย UI
6. RLS เปิดทุกตารางโดย**ไม่มี policy โดยตั้งใจ** = ปิดการเข้าถึงตารางตรง ๆ ทั้งหมด
   (Supabase advisor ขึ้น INFO `rls_enabled_no_policy` — **ถูกต้องแล้ว ไม่ต้องแก้**)
7. **ทุกการเปลี่ยนแปลงฐานข้อมูลต้องเป็นไฟล์ใน `supabase/migrations/`** ห้ามแก้ผ่าน Dashboard/MCP แล้วไม่มีไฟล์

---

## 5. โครงสร้างโค้ดที่ต้องรู้

```
src/
  app/
    app-data.ts           ← ⭐ ตัวโหลดข้อมูลกลาง (RPC ~10 ตัว) ที่ทุก route เรียกใช้
    page.tsx              ← หน้าหลัก "/" (Server Component บาง ๆ เรียก loadAppData แล้วส่งให้ AppShell)
    board/ my-tasks/ dashboard/ trash/   ← อีก 4 route ของ 5 หน้าจอ (แยกจริงตั้งแต่ 2026-09-16)
    AppShell.tsx          ← Client Component ตัวใหญ่ (~870 บรรทัด) คุม state + 5 หน้าจอ
    task-workflow.ts      ← ⭐ ตรรกะสถานะงานทั้งหมด (pure function) + task-workflow.test.ts
    board-derived.ts      ← ฟังก์ชันคำนวณ (pure) + PeopleIndex/personName สำหรับเปิดชื่อจาก id
    board-actions.ts      ← Server Actions ของวาระ/ครั้งที่ประชุม
    board-post-actions.ts ← Server Actions ของ The Wall/Memo
    TaskRow.tsx           ← แถววาระ + dialog ทั้งหมด (~730 บรรทัด)
  lib/                    ← supabase.ts (server-only) · auth.ts · session.ts
supabase/migrations/      ← SQL ทั้งหมด 0001–0008
```

**กฎโครงสร้าง:**
- **ตรรกะสถานะงานใหม่ ต้องไปอยู่ใน `task-workflow.ts` เท่านั้น** + เพิ่ม test เสมอ ห้ามเขียนกลับเข้า component
- **อ้างถึงคนด้วย id เสมอ ห้ามใช้ชื่อเป็นตัวเชื่อม** (ชื่อเปลี่ยนได้ เคยทำให้งานขาดจากเจ้าตัวมาแล้ว)
- **ห้ามใส่ `useCallback`/`memo()` ด้วยมือ** — lint ของโปรเจกต์ใช้กฎ React Compiler การ memo เองทำให้ compiler ข้ามทั้ง component

---

## 6. สถานะปัจจุบัน

**Phase 1–6 อนุมัติแล้วครบ** (Phase 6 อนุมัติ 2026-09-16) · **Phase 7 ยังไม่เริ่ม**
🚀 **ระบบขึ้นใช้งานจริงแล้ว 2026-09-16 ที่ https://workload-tracker-kappa.vercel.app**

แทรกด้วย **Phase R (Code Review & Remediation)** ซึ่งแก้ไปแล้ว 9 ชุด:

| ชุด | เรื่อง |
|---|---|
| R1 | ปิดช่องโหว่ RPC · memo ส่วนตัวในถังขยะรั่ว · cookie ทำให้ login วนลูปตอน build จริง |
| R2 | ดึง SQL ทั้ง 42 ฟังก์ชันลง repo |
| R3 | บั๊กย้อนสถานะ + dialog ถามก่อนบันทึกย้อนหลัง + ติดตั้ง vitest |
| R4 | ผูกคนด้วย `owner_id`/`inspector_id` แทนชื่อ |
| R5 | ประสิทธิภาพ: ลบ `revalidatePath` 20 จุด · รับ props ใหม่จริง · แก้ปัญหาแก้ชนกันติดค้าง |
| R6 | **The Wall + Dashboard ตามคำสั่งผู้ใช้ 2026-09-16:** Red Badge = ของใหม่ที่ยังไม่ได้อ่าน (+ป้าย New) · ตัวเลขถาวรท้ายแท็บ Tagged/Mine · เลิกใช้ Private Memo · Dashboard เหลือแค่ Heat Map |
| R7 | ลบ RPC ที่ตายแล้ว 4 ตัว (A4) |
| R8 | **Accessibility ครบ E1–E8:** กรอบโฟกัส · แถววาระ/การ์ด memo กดด้วยคีย์บอร์ดได้ · Modal มี Esc/focus trap/คืนโฟกัส · ตัวอักษรขั้นต่ำ 11px · ปุ่มบอก "กำลังบันทึก..." · วันที่ยึดเวลาไทยเสมอ · Heat Map มี legend |
| R9 | **แยก 5 หน้าจอเป็น route จริง (F1)** — Back/refresh/ส่งลิงก์ทำงานถูกต้องแล้ว |

**ผลตรวจล่าสุด:** `npm test` 22/22 ✅ · `tsc` 0 error ✅ · `build` ✅ · `lint` 10 errors + 1 warning (**ของเดิมทั้งหมด**)

**ข้อมูลในระบบ:** เป็นข้อมูลสมมติล้วน (ผู้ใช้ยืนยัน) แต่ยังต้องถามก่อนแก้เสมอ เพราะผู้ใช้อาจทดสอบคู่ขนาน
**บัญชีทดสอบ:** `admin` / `123456` — ⚠️ **ข้อมูลนี้อาจล้าสมัยแล้ว** เพราะระบบขึ้นเว็บสาธารณะแล้ว และผู้ใช้ควรเปลี่ยนรหัสผ่านทุกบัญชีก่อนเปิดให้คนอื่นใช้
**⛔️ assistant กรอกรหัสผ่านให้ไม่ได้** ไม่ว่าจะเป็นบัญชีทดสอบก็ตาม — งาน reset password ต้องให้ผู้ใช้ทำเองที่ `/admin/users` เสมอ

---

## 7. งานถัดไป

| ลำดับ | งาน | สถานะ |
|---|---|---|
| 1 | ~~Badge สะสมไม่มีวันเป็นศูนย์~~ | ✅ **เสร็จแล้ว 2026-09-16** (ดู `DESIGN.md` ข้อ 4.12) |
| 2 | ~~Accessibility~~ | ✅ **เสร็จแล้ว 2026-09-16** (ดู `DESIGN.md` ข้อ 4.13) |
| 3 | ~~ลบ dead function ใน DB 4 ตัว~~ | ✅ **เสร็จแล้ว 2026-09-16** (migration `0008` — เหลือ 38 ฟังก์ชัน) |
| 4 | ~~แยก 5 หน้าจอเป็น route จริง~~ | ✅ **เสร็จแล้ว 2026-09-16** — `/` `/board` `/my-tasks` `/dashboard` `/trash` |
| 5 | **เปลี่ยนรหัสผ่านที่ยังเป็น `123456`** (โดยเฉพาะ `admin`) | 🔴 **ด่วนที่สุด — URL เปิดสาธารณะแล้ว** · ผู้ใช้ต้องทำเอง |
| 6 | ~~Phase 6 Approval~~ ✅ → **Phase 7 (Edge Cases + Final Review)** | ⬜ ยังไม่เริ่ม — รอ feedback จากคนใช้จริงก่อนจะคุ้มที่สุด |
| 7 | **Phase F2–F8** — จัดโครงสร้างโค้ด (mapper/rpc helper/TaskActions/component พื้นฐาน/validate input) | ⬜ งานหลังบ้าน ผู้ใช้ไม่เห็นความต่าง |
| 8 | **LINE Login** — ผูก LINE ID ผ่าน LINE OA แล้วให้ตั้งรหัสผ่านสำหรับ Desktop ทีหลัง | ⬜ แผนของผู้ใช้ (พักไว้ก่อน 2026-09-16) |
| 9 | ตรวจที่ยังค้าง 2 ข้อ: รัน **Lighthouse Accessibility** (เกณฑ์ ≥ 90) และทดสอบ **`/trash` ด้วยบัญชีที่ไม่ใช่ admin** | ⬜ ทำบนเว็บจริงได้เลย |

> **งาน session revoke ให้รอทำพร้อม LINE Login** อย่าลงแรงรื้อระบบ session ตอนนี้ (ผู้ใช้ระบุชัด)

---

## 8. วิธีทำงานที่ผู้ใช้คาดหวัง

1. **หยุดรอ Approval ทุก Phase** — ดู `AGENTS.md` ข้อ 3 (กฎนี้เหนือกว่าความสะดวกทุกอย่าง)
2. **ถามก่อนเมื่อสเปกตีความได้หลายแบบ** โดยเฉพาะเมื่อเจอพฤติกรรมที่ดู "แปลก" — อาจเป็นฟีเจอร์ที่ตั้งใจ
   *ตัวอย่างจริง:* การที่ระบบเขียน "X ตรวจแล้ว" ให้อัตโนมัติ ดูเหมือนบั๊ก แต่จริง ๆ รองรับกรณี "ผู้บังคับบัญชาตรวจนอกระบบแล้วไม่ได้กดปุ่ม" ถ้ารื้อทิ้งจะพังงานจริง
3. **อธิบายให้คนที่ไม่อ่านโค้ดเข้าใจได้** — ผู้ใช้ไม่ได้อ่านโค้ด ขอคำอธิบายแบบไม่มีศัพท์เทคนิคบ่อย ๆ
4. **รายงานตามจริง** รวมถึงตอนที่ตัวเองสรุปผิด — ให้แก้เอกสารพร้อมเขียนกำกับว่าผิดตรงไหน ไม่ใช่ลบทิ้งเงียบ ๆ
5. **ห้ามแตะข้อมูลในฐานข้อมูลโดยไม่ได้รับคำสั่ง** — ถ้าข้อมูลไม่ตรงกับที่คิด ให้ถามก่อน
6. **ทดสอบจริงก่อนบอกว่าเสร็จ** — เปิดเว็บ กดใช้จริง และถ้าสร้างข้อมูลทดสอบต้องลบทิ้งให้หมด

---

## 9. คำสั่งที่ใช้บ่อย

```bash
npm run dev          # เปิด dev server (หรือใช้ .claude/launch.json)
npm test             # vitest — ต้องผ่าน 12/12
npx tsc --noEmit     # ต้อง 0 error
npm run build        # ต้องผ่านก่อนส่งงาน
npm run lint         # เทียบจำนวน error กับก่อนแก้ ห้ามเพิ่มใหม่โดยไม่รายงาน
```

> ถ้า console ขึ้น error ที่ขัดกับไฟล์บนดิสก์ → `rm -rf .next` แล้ว restart dev server และเปิด **tab ใหม่**
> (console ของ tab เดิมเก็บประวัติเก่าไว้ ทำให้เข้าใจผิดว่ายังพังอยู่)
