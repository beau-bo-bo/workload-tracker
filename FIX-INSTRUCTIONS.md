# FIX-INSTRUCTIONS.md — คำสั่งแก้ไขสำหรับ Coding Assistant

> เอกสารนี้สั่งงานต่อจากผลตรวจใน `CODE-REVIEW.md` (2026-09-15)
> **อ่าน `CODE-REVIEW.md` ให้จบก่อนลงมือ** รหัสอ้างอิง (S1, B1, P1, C4, U8 …) ในเอกสารนี้ตรงกับหัวข้อในนั้น

---

## บันทึกการตัดสินใจของผู้ใช้ (2026-09-15) — ถือเป็นข้อสรุปแล้ว ห้ามตีความใหม่

| เรื่อง | คำตอบของผู้ใช้ | ผลต่อการทำงาน |
|---|---|---|
| **ข้อมูลในระบบตอนนี้** | เป็นข้อมูลสมมติล้วน ยังไม่มีวาระประชุมจริง | ยังแก้ security ได้โดยไม่ต้องกลัวข้อมูลจริงรั่ว แต่**ต้องปิดให้เสร็จก่อนเอาข้อมูลจริงเข้า** |
| **ระบบ Login** | ตอนนี้เป็นของชั่วคราวสำหรับให้คนลองใช้ · **แผนจริงคือผูกกับ LINE ID ผ่าน LINE OA** แล้วให้ผู้ใช้ตั้งรหัสผ่านสำหรับ Desktop ทีหลัง | **ห้ามลงทุนแก้ระบบ Login เดิมเกินจำเป็น** แก้เฉพาะที่ทำให้ใช้งานไม่ได้ (S6) และที่เป็นช่องโหว่ (S1) · งาน Phase B2 (session revoke) ให้รอไปทำพร้อม LINE |
| **ประวัติอัตโนมัติ ตอนส่งตรวจซ้ำ (B1/U20)** | ให้ถามในหน้าต่างว่า *"งานยังค้างที่คุณ A — บันทึกว่า A ตรวจเสร็จแล้ว หรือข้ามไปเลย?"* แล้วเขียนตามที่ผู้ใช้เลือก | ทำใน Phase C · **สถานะหลักต้องเป็น "ส่ง B" เหมือนกันทั้งสองทางเลือก** ต่างกันแค่บรรทัดใน Timeline และการติ๊กขั้นตอนความคืบหน้าเท่านั้น |
| **ผู้รับผิดชอบ/ผู้ตรวจ (B5)** | **ต้องเลือกจากคนในระบบเท่านั้น** ไม่ต้องรองรับการพิมพ์ชื่อคนนอก | ตัดข้อกังวลเรื่อง free text ออกได้ → Phase G ใช้ **ทางเลือกที่ 1 หรือ 2** ได้เลย ไม่ต้องใช้ทางเลือกที่ 3 |

### เพิ่มเติม 2026-09-16 (ถือเป็นข้อสรุปแล้วเช่นกัน)

| เรื่อง | คำสั่งของผู้ใช้ | ผลต่อการทำงาน |
|---|---|---|
| **Red Badge ของ The Wall** | "เอาเป็นข้อความใหม่ที่ Tag แล้ว User ยังไม่ได้กดดู" | ทำแล้ว — badge = unread เท่านั้น · เข้าหน้า The Wall = อ่านทั้งหมด · ของใหม่ติดป้าย **New** ค้างไว้ตลอดรอบที่เปิดหน้านั้น |
| **ตัวเลขท้ายแท็บ Tagged/Mine** | "เป็นแบบถาวร แค่เพื่อแสดงจำนวน จะหายไปก็ต่อเมื่อลบ Memo" | ทำแล้ว — เป็นยอดรวมของแท็บ ไม่ผูกกับการอ่าน และใช้สีกลาง ไม่ใช่สีแดงแบบ badge แจ้งเตือน |
| **Private Memo** | "ไม่เอาแล้ว มันมองไม่เห็น คุมจำนวนไม่ได้ เดี๋ยวรก db" + "หลักการของ The Wall คือแปะสิ่งที่ต้องทำ และแจ้งข่าว ควรถูกลบหลังทำเสร็จ / ผ่าน event ไปแล้ว" | ลบทั้งฟีเจอร์และคอลัมน์ `is_private` แล้ว (migration 0007) — **ห้ามเสนอกลับมาอีก** |
| **ตารางสรุปบน Dashboard** | "ไม่เอาละ เหลือแค่ Heat Map ก็น่าจะพอบอกข้อมูลได้ คิดว่ามันซ้ำๆ กันอยู่" | ลบตารางสรุป + `workloadSummary()` แล้ว เหลือ Heat Map อย่างเดียว |

---

## 0. กฎเหล็กสำหรับงานชุดนี้

กฎใน `AGENTS.md` ทุกข้อยังบังคับใช้ตามเดิม โดยเฉพาะ **Approval Gate — ห้ามข้าม Phase เด็ดขาด** และเพิ่มกฎเฉพาะงานแก้บั๊กชุดนี้:

1. **ห้ามแก้หลาย Phase พร้อมกัน** แม้จะ "แก้ไปด้วยกันเลยง่ายกว่า" — งานชุดนี้แตะ security และ data model ถ้าพังต้องรู้ให้ได้ว่าพังเพราะ commit ไหน
2. **ห้ามเพิ่มฟีเจอร์ใหม่** ระหว่างแก้บั๊ก ถ้าเห็นอะไรน่าปรับปรุงให้จดไว้ท้าย `PROGRESS-TRACKER.md` แล้วถามผู้ใช้ ห้ามทำเอง
3. **ทุกการเปลี่ยนแปลง SQL ต้องเขียนเป็นไฟล์ใน `supabase/migrations/` ก่อนเสมอ** ห้ามแก้ผ่าน Supabase Dashboard หรือ MCP โดยตรงแล้วไม่มีไฟล์ (นี่คือสาเหตุที่ช่องโหว่ S1 รอดมาได้ทุก Phase)
4. **ห้ามแตะข้อมูลจริงในตาราง** โดยที่ผู้ใช้ไม่ได้สั่ง — ผู้ใช้อาจกำลังทดสอบระบบอยู่คู่ขนาน ถ้าเห็นข้อมูลไม่ตรงกับที่คิด **ให้ถามก่อน อย่าแก้ให้**
5. **หลังแก้ทุก Phase ต้องรัน `npx tsc --noEmit` และ `npm run lint` ให้ผ่าน** ก่อนส่งให้ผู้ใช้ตรวจ
6. รายงานตามจริง — ถ้าแก้ไม่ครบให้บอกว่าข้อไหนยังไม่ได้แก้และเพราะอะไร ห้ามสรุปว่า "เสร็จแล้ว" ถ้ายังไม่ครบ

---

## Phase A — ปิดช่องโหว่ยึดระบบ · ✅ เสร็จแล้ว (2026-09-15) — เหลือ A4

> **สถานะ:**
> - ✅ **A0** — `0001_baseline_schema.sql` + `0002_baseline_functions.sql` (สำเนาครบทั้ง 42 ฟังก์ชันอยู่ใน repo แล้ว)
> - ✅ **A3** — `0003_fix_missing_auth_checks.sql` (ปิด private leak ในถังขยะ + เติม guard ให้ `set_own_avatar`)
> - ✅ **A1 + A2** — `0004_lockdown_rpc_grants.sql` · โค้ดใช้ `SUPABASE_SERVICE_ROLE_KEY` แล้ว · ผู้ใช้กรอกคีย์เรียบร้อย
> - ⬜ **A4 ยังไม่ทำ** — ลบ overload `update_task_details` ที่ซ้ำ และ dead function 4 ตัว (`admin_rename_user`, `admin_reorder_user`, `admin_update_roles`, `update_meeting`)
>
> **ผลการทดสอบยืนยัน (ทดสอบกับระบบจริง):**
>
> | รายการ | ก่อนแก้ | หลังแก้ |
> |---|---|---|
> | คนนอกเรียก `list_all_users` | HTTP 200 · ได้รายชื่อ 13 คน | **HTTP 401** |
> | คนนอกเรียก `list_meetings` / `list_active_tasks` | HTTP 200 | **HTTP 401** |
> | คนนอกเรียก `admin_reset_password` | เรียกได้ | **HTTP 401** |
> | เซิร์ฟเวอร์เรียกผ่าน service_role | 200 | **200 (ปกติ)** |
> | Supabase security advisor | เตือน 42 รายการ | **0 รายการ** (เหลือแต่ INFO `rls_enabled_no_policy` ซึ่งถูกต้องตามการออกแบบ) |
> | เว็บเชื่อมฐานข้อมูล | ปกติ | **ปกติ** (ทดสอบผ่านหน้า login ด้วยบัญชีปลอม ได้ข้อความ "Username หรือ Password ไม่ถูกต้อง" ตามที่ควรเป็น) |
>
> ไฟล์ที่แก้: `src/lib/supabase.ts`, `src/lib/auth.ts`, `src/app/page.tsx`, `.env.local.example`, `.env.local`
> สำรองไว้ที่: `.env.local.backup-before-fix`
>
> **หมายเหตุสำหรับคนทำต่อ:** ตั้งแต่นี้ไปฐานข้อมูลเข้าถึงได้จากฝั่งเซิร์ฟเวอร์เท่านั้น
> `service_role` **ข้ามด่านตรวจทุกอย่างของฐานข้อมูล** ดังนั้น **ทุก Server Action ต้องตรวจสิทธิ์ผู้ใช้เองก่อนเสมอ**
> ห้ามสร้าง client component ที่เรียก Supabase ตรง ๆ เด็ดขาด (`import "server-only"` จะทำให้ build พังถ้าเผลอทำ)

**สถานะความเสี่ยงตอนนี้:** ใครก็ได้ที่เข้าถึงเว็บได้ สามารถยึดบัญชี admin ได้ใน 3 requests โดยไม่ต้อง login (รายละเอียดใน `CODE-REVIEW.md` ข้อ S1)

### A0. ก่อนแก้อะไร — ดึง schema ปัจจุบันลง repo (แก้ C17)

1. สร้างโฟลเดอร์ `supabase/migrations/`
2. Dump ทุกอย่างที่มีอยู่จริงบน project `kalooxclwbnjunuxbjst` ลงเป็นไฟล์:
   - `0001_initial_schema.sql` — ตาราง `users`, `meetings`, `tasks`, `board_posts` พร้อม enum ทั้งหมด
   - `0002_initial_functions.sql` — **ทั้ง 42 ฟังก์ชัน** ตามที่มีอยู่ตอนนี้ (ยังไม่ต้องแก้ ก๊อปมาตรง ๆ)
3. เพิ่มหัวข้อ "Database" ใน `README.md` บอกวิธี apply migration
4. **หยุด แจ้งผู้ใช้ว่าตอนนี้ SQL อยู่ใน repo แล้ว รอ approval**

> เหตุผลที่ต้องทำข้อนี้ก่อน: ถ้าไม่มี baseline ใน git การแก้ security ต่อจากนี้จะ review ไม่ได้ และ rollback ไม่ได้

### A1. ถอนสิทธิ์ `anon` (แก้ S1)

ไฟล์ `supabase/migrations/0003_lockdown_rpc_grants.sql` — ใช้ SQL ตาม `CODE-REVIEW.md` ข้อ 4.1 ขั้นที่ 1

### A2. เปลี่ยนมาใช้ service_role key (แก้ S1 ต่อ)

- แก้ `src/lib/supabase.ts` ตามข้อ 4.1 ขั้นที่ 3 (เพิ่ม `import "server-only"`)
- ติดตั้ง package `server-only`
- เปลี่ยนชื่อ env: `NEXT_PUBLIC_SUPABASE_URL` → `SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_ANON_KEY` → `SUPABASE_SERVICE_ROLE_KEY`
- แก้ `.env.local.example` ให้ตรง
- **แจ้งผู้ใช้ว่าต้องไปหยิบ service_role key จาก Supabase Dashboard มาใส่ `.env.local` เอง — ห้าม assistant ไปอ่านหรือเขียน key ให้**
- ตรวจสอบด้วย `grep -r "NEXT_PUBLIC_SUPABASE" src/` ต้องไม่เหลือสักที่

### A3. ปิด private leak + เติม guard ที่ขาด (แก้ S2, S4)

ไฟล์ `0004_fix_missing_auth_checks.sql` — ใช้ SQL ตามข้อ 4.1 ขั้นที่ 2
จากนั้นแก้ `page.tsx` ให้ส่ง `p_user_id` เข้า `list_deleted_board_posts` และ **ดึงข้อมูลถังขยะเฉพาะเมื่อผู้ใช้เป็น admin เท่านั้น**

### A4. ลบ overload ซ้ำและ dead function (แก้ B11, C15) — ✅ เสร็จแล้ว 2026-09-16

ทำจริงในไฟล์ `0008_drop_dead_functions.sql` (ไม่ใช่ `0005` ตามที่ร่างไว้ เพราะเลข 0005/0006 ถูกใช้ไปกับงาน data model ก่อนหน้า)
`update_task_details` ตัวซ้ำถูกลบไปแล้วตั้งแต่ `0005` — รอบนี้จึงเหลือ 4 ตัว: `admin_rename_user`, `admin_reorder_user`, `admin_update_roles`, `update_meeting`

**ตรวจก่อน drop 3 ชั้น ผ่านหมด:**
1. `grep -r` ทั้ง `src/` ทีละชื่อ → 0 ครั้งทั้ง 4 ตัว (ฟังก์ชันที่ยังใช้อยู่ 38 ตัวถูกนับทีละตัวด้วย ไม่มีตัวไหนถูกลบพลาด)
2. ไล่ `pg_get_functiondef` ของทุกฟังก์ชันใน `public` → ไม่มีฟังก์ชันไหนเรียกถึง 4 ตัวนี้เอง
3. ไม่มี trigger ใน schema `public` เลย

**ผลหลังลบ:** ฟังก์ชันใน `public` เหลือ 38 ตัว · ตัวที่ตายแล้วเหลือ 0 · `anon`/`authenticated` เรียกได้ 0 ตัวเหมือนเดิม

### เกณฑ์ผ่าน Phase A

- [ ] มีโฟลเดอร์ `supabase/migrations/` พร้อมไฟล์ 0001–0005 และ SQL ทั้งหมดอยู่ใน git
- [ ] `select has_function_privilege('anon', oid, 'EXECUTE')` คืน `false` ครบทุกฟังก์ชันใน `public`
- [ ] Supabase advisor (security) ไม่มี lint `anon_security_definer_function_executable` เหลืออยู่
- [ ] `grep -r "NEXT_PUBLIC_SUPABASE" src/` ไม่พบอะไร
- [ ] login / สร้างงาน / ส่งตรวจ / ปิดงาน / จัดการผู้ใช้ ยังทำงานได้ครบเหมือนเดิม
- [ ] ผู้ใช้ที่ไม่ใช่ admin ไม่มี memo ในถังขยะหลุดมาถึง browser (ตรวจด้วย React DevTools)

**หยุดรอ approval**

---

## Phase B — Session และ Cookie

> **ปรับขอบเขตตามที่ผู้ใช้แจ้ง:** ระบบ Login ปัจจุบันเป็นของชั่วคราว แผนจริงคือผูกกับ **LINE ID ผ่าน LINE OA**
> ดังนั้น **B2 และ B3 ให้เลื่อนไปทำพร้อมงาน LINE** อย่าเพิ่งลงแรงรื้อ session ตอนนี้
> ทำเฉพาะ **B1** ซึ่งเป็นเรื่องที่ทำให้ระบบใช้งานไม่ได้จริง ๆ

### B1. Cookie ใช้งานได้บน HTTP LAN (แก้ S6) — ✅ แก้แล้ว 2026-09-15 · **ทดสอบบนของจริงผ่านแล้ว 2026-09-16**

`src/lib/auth.ts` เปลี่ยนมาใช้ env `APP_SECURE_COOKIES` แทน `NODE_ENV` แล้ว
**ผลทดสอบจริง:** deploy ขึ้น Vercel (https) พร้อมตั้ง `APP_SECURE_COOKIES=true` → ผู้ใช้ login ผ่าน เข้าหน้าหลักได้ ไม่วนลูป
นี่คือการพิสูจน์ว่าการแยก flag ออกจาก `NODE_ENV` ถูกต้อง — production build + https ใช้ `true`, LAN http ใช้ `false`, สลับได้โดยไม่ต้องแก้โค้ด

### B2. Session ที่ revoke ได้ (แก้ S3) — ⏸️ รอทำพร้อม LINE Login

- ลด `SESSION_DURATION_SECONDS` เหลือ 8 ชั่วโมง
- ถอด `roles` ออกจาก JWT payload
- สร้าง `src/lib/authz.ts` (`getActor` / `requireActor` / `requireAdmin`) ตามข้อ 4.2
- สร้าง RPC `get_actor(p_user_id)` คืน `id, display_name, roles, can_manage_tasks` ในครั้งเดียว (แทน `get_own_permissions`)
- แทนที่ `getSession()` ด้วย `getActor()` ในทุก server action และทุก page
- `middleware.ts` ยังเช็คแค่ "มี token ที่ signature ถูก" ได้ (เพื่อความเร็วบน edge) แต่ **การตัดสินใจเรื่องสิทธิ์จริงต้องอยู่ที่ page/action ที่เรียก `requireAdmin()`**

### B3. หน้าเว็บป้องกันตัวเอง (แก้ B9, B10)

ใช้โค้ดตามข้อ 4.9 — `page.tsx` redirect แทน return null, `admin/users/page.tsx` เช็ค role เอง และ log error ที่เดิมถูกทิ้ง

### เกณฑ์ผ่าน Phase B

- [ ] build production แล้วเข้าผ่าน `http://<LAN IP>` login ได้ ไม่วนลูป
- [ ] ลบ user ที่กำลัง login ค้างอยู่ → เขาถูกเด้งออกในการกดครั้งถัดไป (ไม่ต้องรอ 7 วัน)
- [ ] ถอดสิทธิ์ admin ของใครคนหนึ่ง → เขาเข้า `/admin/users` ไม่ได้ทันทีโดยไม่ต้อง login ใหม่
- [ ] user ที่ไม่ใช่ admin เปิด `/admin/users` แล้วถูก redirect กลับหน้าแรก (ไม่ใช่เห็นหน้าเปล่า)

**หยุดรอ approval**

---

## Phase C — Workflow / History ที่ถูกต้อง · ✅ C1–C4 เสร็จแล้ว (2026-09-15)

> **สถานะ:**
> - ✅ **C1** — ติดตั้ง vitest (ใช้ `^2` เพราะ v5 ต้องการ `@types/node` ใหม่กว่าที่โปรเจกต์ใช้) · `npm test` · 11 tests ผ่าน
> - ✅ **C2 + C4** — ย้ายตรรกะ workflow ออกจาก `AppShell.tsx` มาไว้ที่ `src/app/task-workflow.ts` (pure function ทดสอบได้)
>   และเปลี่ยนจาก "เขียน record ให้เองเงียบ ๆ" เป็น "ถามผู้ใช้ก่อน แล้วกำกับว่าใครบันทึกแทน"
> - ⏸️ **C3 (ย้าย state machine ไป server)** — ยังไม่ทำ เป็นงานฝั่งความปลอดภัย รอพร้อม LINE Login
>
> **หลักฐานว่า test จับบั๊กได้จริง:** ย้อนตรรกะกลับเป็นแบบเดิมชั่วคราวแล้วรัน test → **fail 4 ข้อ**
> รวมถึง `expected 'done' to be 'pending'` ซึ่งคือบั๊ก B1 · พอคืนตรรกะที่แก้แล้ว → ผ่านครบ 11
>
> **ไฟล์ที่เพิ่ม/แก้:** `src/app/task-workflow.ts` (ใหม่) · `src/app/task-workflow.test.ts` (ใหม่) ·
> `AppShell.tsx` · `TaskRow.tsx` · `TaskTable.tsx` · `MeetingCard.tsx` · `MyTasksView.tsx` · `board-types.ts` (เพิ่ม `recordedBy`)
>
> **สิ่งที่ตั้งใจไม่ทำ:** ไม่แตะ `buildReopenTransition` และไม่เพิ่ม `prevStatus`/`prevInspector` — บั๊ก B1 หายไปเองแล้ว
>
> **ทดสอบบนหน้าเว็บจริงแล้ว (2026-09-15)** โดยสร้างงานทดสอบใหม่ ไม่แตะงานจริง และลบทิ้งหลังเสร็จ:
>
> | ทดสอบ | ผล |
> |---|---|
> | กดปิดงานตอนสถานะยังไม่ค้างที่ใคร | ไม่มี dialog ขึ้นมากวน ✅ |
> | กดปิดงานตอนงานค้างที่ "พี่ปู" | dialog ขึ้น ระบุชื่อถูก ค่าเริ่มต้นเลือก "ตรวจแล้ว" ไว้ ✅ |
> | ตอบ "ตรวจแล้ว" + ใส่หมายเหตุ | Timeline ขึ้น `พี่ปู > ตรวจแล้ว (บันทึกย้อนหลังโดย โบ)` พร้อมหมายเหตุ ✅ |
> | ย้อนสถานะต่อจากนั้น | ได้ "พี่ปู แล้ว" ตรงกับที่บันทึกไว้ ✅ |
> | ส่งตรวจต่อให้ "พี่จา" แล้วปิดงาน ตอบ "ยังไม่ได้ตรวจ" | ไม่มี entry ปลอมถูกเขียน ✅ |
> | ย้อนสถานะต่อจากนั้น | ได้ **"ส่ง พี่จา"** ตรงกับก่อนปิดงาน ✅ ← จุดที่เคยเป็นบั๊ก |
> | งานจริงทั้ง 4 รายการ | สถานะไม่เปลี่ยนแปลง ✅ · ถังขยะว่าง ไม่มีขยะตกค้าง ✅ |

---

### บันทึกไว้สำหรับคนทำต่อ

`src/app/task-workflow.ts` เป็น pure function ล้วน ไม่มี state ไม่มี I/O — **ตรรกะ workflow ใหม่ทุกอย่างควรไปอยู่ที่นี่**
และเพิ่ม test ใน `task-workflow.test.ts` เสมอ อย่าเขียนตรรกะสถานะกลับเข้าไปใน component อีก

> Phase นี้แตะ business logic หลัก **ต้องเขียน test ก่อนแก้โค้ด**

### C1. เขียน test ให้ `board-derived.ts` ก่อน (แก้ C16)

- ติดตั้ง `vitest`
- เขียน test ครอบคลุม: `taskSteps`, `taskProgress`, `workloadSummary`, `taskNeedsMyAction`, `reminderUrgency`, `formatShortDate`
- **ต้องมี test เคสของ B1 ด้วย** โดยใช้ลำดับที่ถูกต้องดังนี้ (ปุ่ม "ย้อนสถานะ" กดได้เฉพาะตอนสถานะ `closed` เท่านั้น — ดู `TaskRow.tsx:457`):

  | เคส | ลำดับ | ผลที่ถูกต้อง | สถานะตอนนี้ |
  |---|---|---|---|
  | ปกติ | ส่งตรวจ A → A ตรวจแล้ว → ปิดงาน → ย้อนสถานะ | `done` | ✅ ผ่านอยู่แล้ว |
  | ปกติ | ปิดงานตั้งแต่ยังไม่ส่งตรวจ → ย้อนสถานะ | `blank` | ✅ ผ่านอยู่แล้ว |
  | **บั๊ก B1** | ส่งตรวจ A → ปิดงานเลย โดยผู้ใช้ตอบ **"ยังไม่ได้ตรวจ"** → ย้อนสถานะ | `pending` (รอ A ตรวจ) ไม่มี entry "ตรวจแล้ว" | ❌ ตอนนี้ได้ `done` |
  | **บั๊ก B1** | ส่งตรวจ A → ปิดงานเลย โดยผู้ใช้ตอบ **"ตรวจแล้ว"** → ย้อนสถานะ | `done` + entry ระบุ `recordedBy` | ⚠️ ได้ `done` ถูก แต่ยังไม่มี `recordedBy` |

  สองเคสล่าง **ต้อง fail ก่อน** — ให้ผู้ใช้เห็นว่า fail แล้วค่อยแก้ที่ C4
  (เคสที่ 3 จะผ่านเองทันทีที่ทำ C4 เสร็จ เพราะผู้ใช้เลือก "ยังไม่ได้ตรวจ" แล้วระบบจะไม่เพิ่ม entry)

### C2. แก้ reopen ให้ถูกต้อง (แก้ B1, B2)

ใช้แนวทาง `prevStatus` / `prevInspector` / `autoSkipped` ตามข้อ 4.3
รวมถึงเพิ่ม `default` ใน `historyLabel` (B13)

**หมายเหตุเรื่องข้อมูลเดิม:** history ที่มีอยู่แล้วในฐานข้อมูลไม่มี `prevStatus`
→ **ห้ามเขียน migration ไปเดา `prevStatus` ย้อนหลังให้ข้อมูลเก่าเอง** ให้ทำ fallback ในโค้ด (ถ้าไม่มี `prevStatus` ค่อยใช้ logic เดิม) แล้ว**ถามผู้ใช้**ว่าต้องการ backfill หรือไม่

### C3. ย้าย state machine ไปฝั่ง server (แก้ S7)

สร้าง RPC `apply_task_transition` ตามข้อ 4.3 แล้วเปลี่ยน `updateTaskWorkflowAction` มาเรียกตัวใหม่
เก็บ `update_task_workflow` ตัวเก่าไว้ก่อน (ยังไม่ drop) จนกว่าผู้ใช้จะยืนยันว่าตัวใหม่ทำงานถูก

### C4. เรื่อง "ส่งตรวจซ้ำ" — ผู้ใช้ตัดสินใจแล้ว ✅

ผู้ใช้เลือก: **ให้ถามใน modal ทุกครั้ง** แทนที่จะเขียน log ให้เองเงียบ ๆ

**บริบทสำคัญจากผู้ใช้ (ห้ามตีความใหม่):**
การที่ระบบเขียน record "ตรวจแล้ว" ให้เอง **เป็นฟีเจอร์ที่ตั้งใจ ไม่ใช่บั๊ก** — ในงานจริงผู้บังคับบัญชามักตรวจเอกสารเรียบร้อยแล้ว
แต่ไม่ได้กดปุ่ม "ตรวจแล้ว" ในระบบ (ตรวจนอกระบบ) เจ้าของงานจึงกดปิดงานเลย และต้องการให้ระบบบันทึกว่าผ่านการตรวจแล้วจริง
**ห้ามเอาฟีเจอร์นี้ออก** ให้ทำให้มันชัดเจนขึ้นเท่านั้น

**ต้องทำทั้ง 2 จุด** (ทั้งคู่เรียก `withSkippedReviewEntry` ด้วย `skippable = true`):
- กด **ปิดงาน** ขณะสถานะเป็น `pending` ← จุดนี้ใช้บ่อยที่สุด และเป็นต้นเหตุของบั๊ก B1
- กด **ส่งตรวจ** ขณะสถานะเป็น `pending`

**หน้าตา dialog ที่ต้องการ:**

```
งานนี้ยังค้างอยู่ที่ คุณ A (ยังไม่ได้กด "ตรวจแล้ว" ในระบบ)

  ( • ) คุณ A ตรวจแล้ว — บันทึกย้อนหลังให้ด้วย     ← ตั้งเป็นค่าเริ่มต้น
  (   ) คุณ A ยังไม่ได้ตรวจ — ไม่ต้องบันทึก

  หมายเหตุ (ถ้ามี): [____________________]        ← เก็บลง HistoryEntry.message

                                   [ ยกเลิก ]  [ ปิดงาน ]
```

**ข้อกำหนด:**

1. **ตั้งค่าเริ่มต้นเป็น "ตรวจแล้ว"** เพราะเป็นกรณีที่เกิดบ่อยกว่า → ผู้ใช้กดผ่านได้เร็ว ไม่กลายเป็นอุปสรรค
2. **record ต้องระบุที่มา** — เพิ่มฟิลด์ `recordedBy` (ชื่อคนที่กดปุ่ม) ลง `HistoryEntry` แล้วแสดงใน Timeline ว่า
   `A > ตรวจแล้ว (บันทึกย้อนหลังโดย สมชาย)` **ห้ามเขียนให้เหมือน A กดเองในระบบ** — เอกสารประชุมต้องอ้างอิงย้อนหลังได้
3. กรณี **ส่งตรวจ**: ทั้งสองคำตอบต้องได้สถานะสุดท้ายเหมือนกันคือ `pending` ที่ผู้ตรวจคนใหม่ ต่างกันแค่บรรทัดใน Timeline และการติ๊กขั้นตอนความคืบหน้า
4. กรณี **ปิดงาน**: สถานะสุดท้ายเป็น `closed` ทั้งสองคำตอบ

**⚠️ ห้ามแตะ `reopenTask` และห้ามเพิ่ม `prevStatus` / `prevInspector`**
ข้อเสนอเดิมในรีวิวข้อ 4.3 **ยกเลิกแล้ว** เพราะเมื่อจำนวน entry ที่เพิ่มขึ้นอยู่กับคำตอบของผู้ใช้
มันจะตรงกับที่ `reopenTask` ถอดออกพอดีทั้งสองทาง (ทดสอบด้วยการจำลองตรรกะจริงแล้ว):

| ผู้ใช้ตอบ | ย้อนสถานะแล้วได้ | ถูกต้องไหม |
|---|---|---|
| "ตรวจแล้ว" | "A แล้ว" (`done`) | ✅ |
| "ยังไม่ได้ตรวจ" | "ส่ง A" (`pending`) | ✅ |

→ **บั๊ก B1 หายไปเองจากการแก้จุดนี้จุดเดียว** อย่าไปแก้ซ้ำอีกที่

### เกณฑ์ผ่าน Phase C

- [ ] `npx vitest run` ผ่านทั้งหมด รวมถึงเคส B1 ที่เคย fail
- [ ] ทดสอบด้วยมือ: ส่งตรวจ A → ส่งตรวจ B → ย้อนสถานะ → ต้องกลับเป็น "รอ A ตรวจ" ไม่ใช่ "A ตรวจแล้ว"
- [ ] ยิง `apply_task_transition` ด้วย transition ที่ผิดกฎ (เช่น reviewed ตอน status = blank) ต้องได้ `invalid_transition`
- [ ] history เดิมของงานเก่ายังแสดงผลถูกต้อง ไม่มีบรรทัดไหนหาย

**หยุดรอ approval**

---

## Phase D — Performance · ✅ เสร็จแล้ว (2026-09-15)

> **สถานะ:**
> - ✅ **D1** — ลบ `revalidatePath("/")` ออก 20 จุด (board-actions 12 · board-post-actions 7 · avatar-actions 1)
>   และทำให้ `AppShell` รับข้อมูลชุดใหม่จาก server จริง ตาม pattern ของ `UserList.tsx`
> - ✅ **D2** — ห่อ `useMemo` ให้ค่าที่คำนวณทุก render + เพิ่ม `taskIndex` (Map) ให้ค้นหางานเป็น O(1)
>   + ห่อ `useCallback` 13 ตัว และ `memo()` ครอบ `TaskRow`
> - ✅ **D3** — toast เก็บกวาด timer ตัวเองแล้ว (B7)
> - ⏸️ **D4** — ยังไม่ทำ ทับซ้อนกับ Phase F (แยก route) ให้ทำรวมกันทีเดียว
>
> **แถม:** แก้ B3 (แก้ชนกันแล้วติดค้าง) ไปด้วย — ปุ่มเปลี่ยนจาก "Refresh" (reload ทั้งหน้า เสียสิ่งที่พิมพ์ค้าง)
> เป็น **"ดูข้อมูลล่าสุด"** ที่เรียก `router.refresh()` แล้วอัปเดตในที่เดิม
>
> **วิธีวัดผล (ใช้ `pg_stat_statements` นับจำนวนครั้งที่ฐานข้อมูลถูกเรียกจริง):**
>
> | การกระทำ | จำนวนครั้งที่เรียกฟังก์ชันอ่านรายการ |
> |---|---|
> | โหลดหน้าใหม่ (control) | **+4** (ยืนยันว่าตัวนับทำงาน) |
> | กดปิดงาน 1 ครั้ง | **+0** ← เดิมคือ +10 RPC ที่ถูกทิ้งเปล่า |
>
> ⚠️ **อย่าวัดด้วยจำนวน HTTP request** — Next.js ส่งข้อมูลที่ revalidate กลับมาใน response ของ server action เดียวกัน
> จำนวน request จึงเท่าเดิมทั้งที่งานฝั่งฐานข้อมูลต่างกัน 10 เท่า
>
> ### ⚠️ บทเรียน: อย่าใส่ `useCallback` / `memo()` ด้วยมือในโปรเจกต์นี้
>
> ตอนแรกลองใส่ `useCallback` 13 ตัว + `memo(TaskRow)` ตามข้อเสนอ P3 ในรีวิว → **lint พุ่งจาก 10 เป็น 16 errors**
> ทุกตัวคือ `Compilation Skipped: Existing memoization could not be preserved` จาก `eslint-plugin-react-hooks` v7
> (กฎชุดนี้วิเคราะห์ด้วย React Compiler) — การ memo ด้วยมือทำให้ compiler ข้ามการ optimize ทั้ง component
>
> **ข้อเท็จจริงที่ตรวจแล้ว:** React Compiler **ยังไม่ได้เปิดใช้ตอน build** (ไม่มี `babel-plugin-react-compiler`
> และ `experimental.reactCompiler` เป็น undefined) กฎ lint จึงเป็นแค่คำเตือนล่วงหน้า
>
> **สรุป:** ถอย `useCallback`/`memo` ออกทั้งหมด เหลือแต่ `useMemo` (ซึ่ง lint ไม่ติดและได้ผลจริง)
> → **ข้อเสนอ P3 ในรีวิวถือว่ายกเลิก** ถ้าจะเอา P3 จริง ๆ ทางที่ถูกคือ **เปิด React Compiler** ให้มัน memo ให้ทั้งแอป
> ซึ่งเป็นการเปลี่ยน build config ทั้งโปรเจกต์ — **ต้องถามผู้ใช้ก่อน ห้ามเปิดเอง**

### D1. เลิกทิ้งข้อมูลที่ดึงมา (แก้ P1, B15, B3)

เลือกแนวทาง **(ข)** ในข้อ 4.5 — ให้ `AppShell` รับ props ใหม่จริงตาม pattern ของ `UserList.tsx:23-29`
เพราะได้ทั้ง performance, multi-user sync และแก้ปัญหา version conflict ทางตัน (B3) ในคราวเดียว

### D2. useMemo + index (แก้ P2, P4, P6)

ตามข้อ 4.5 — `notifications`, `taskIndex`, `activeMeetings` และแก้ `taskProgress` ให้รับ `steps` ที่คำนวณแล้ว

### D3. Toast ที่เก็บกวาด timer (แก้ B7)

ตามข้อ 4.5 ส่วน `toastTimer`

### D4. โหลดข้อมูลเท่าที่ใช้ (แก้ P5, P7)

- ย้าย Trash และ Dashboard ไปเป็น route แยก (`/trash`, `/dashboard`) ที่ดึงข้อมูลของตัวเอง
- `next/dynamic` กับหน้าจอรองที่เหลือ
- **ข้อนี้ทับซ้อนกับ Phase E (แยก route)** ถ้าจะทำ E อยู่แล้ว ให้ทำรวมกันใน E และข้ามข้อนี้ — แจ้งผู้ใช้ก่อนตัดสินใจ

### เกณฑ์ผ่าน Phase D

- [ ] ติ๊ก checklist 1 ช่อง แล้วดู Network tab: ไม่มี RSC request ที่ดึงข้อมูลทั้งหน้าโดยไม่ได้ใช้
- [ ] เปิดสองหน้าต่างด้วย user คนละคน แก้งานเดียวกัน → อีกฝั่งเห็นข้อมูลใหม่โดยไม่ต้อง refresh เอง
- [ ] React DevTools Profiler: กดเปิด avatar picker แล้ว `TaskRow` ไม่ re-render ทั้งหมด

**หยุดรอ approval**

---

## Phase E — Accessibility และ UX · ✅ เสร็จแล้ว 2026-09-16

> **สถานะ:** ทำครบ E1–E8 · `npm test` 22/22 (เพิ่มชุดทดสอบวันที่ 10 ข้อ) · `tsc` 0 error · `build` ผ่าน · `lint` 10 errors + 1 warning เท่าเดิม
> รายละเอียดการออกแบบทั้งหมดถูกบันทึกไว้ที่ `DESIGN.md` ข้อ 4.13
> **สิ่งที่ยังทำไม่ได้:** วัด Lighthouse Accessibility (เบราว์เซอร์ที่ assistant ใช้รันไม่ได้) และการกด Enter/Space จริงบนปุ่ม (เครื่องมืออัตโนมัติส่งคีย์แล้วปุ่มไม่ทำงาน — พิสูจน์แล้วว่าเป็นข้อจำกัดของเครื่องมือ ไม่ใช่ของแอป ด้วยการทดสอบกับปุ่ม `<button>` เปล่า ๆ ที่สร้างขึ้นมาเทียบ)

เรียงตามลำดับ ทำทีละข้อ:

1. **E1** — `Modal` ให้ครบ (Escape, focus trap, คืน focus, `role="dialog"`, scroll lock) ตามข้อ 4.7 (U9)
2. **E2** — แถวงานและการ์ด memo เป็น `<button>` + `aria-expanded` ตามข้อ 4.8 (U8)
3. **E3** — เพิ่ม `:focus-visible` global ใน `globals.css` (U10)
4. **E4** — ตัวอักษรขั้นต่ำ 12px สำหรับข้อความ, `EMPTY_MARK_CLASS` แก้เป็น `text-muted/70` (U11)
5. **E5** — ปุ่มงานทุกปุ่ม disable + แสดงสถานะระหว่างรอ ตามข้อ 4.8 (U5)
6. **E6** — `task.owner` ที่ว่างให้แสดง `EMPTY_MARK` (B4)
7. **E7** — วันที่/เวลา timezone-safe ตามข้อ 4.6 (B12) แล้ว**ลบ `suppressHydrationWarning` ออกทุกจุด** ถ้ายังมี warning เหลือ ให้รายงานสาเหตุจริง ห้ามใส่กลับ (C12)
8. **E8** — Heat map ใส่ legend (U12)

### เกณฑ์ผ่าน Phase E

- [ ] ใช้ Tab + Enter อย่างเดียว ทำครบ flow ได้: เปิดงาน → ส่งตรวจ → ปิด modal ด้วย Esc
- [ ] เห็นกรอบ focus ชัดเจนทุกปุ่ม
- [ ] ไม่มี hydration warning ใน console และไม่มี `suppressHydrationWarning` เหลือในโค้ด
- [ ] เปิดด้วย Lighthouse Accessibility ได้ ≥ 90

**หยุดรอ approval**

---

## Phase F — โครงสร้างโค้ด (ทำหลังมี test แล้วเท่านั้น)

1. ~~**F1** — แยก 5 หน้าจอเป็น route จริง (C1, U1)~~ → ✅ **เสร็จแล้ว 2026-09-16** (ดู `DESIGN.md` ข้อ 4.14)
   ได้ครบตามเป้า: Back/refresh/ส่งลิงก์ทำงานถูกต้อง · หน้าป้องกันตัวเอง (B9/B10) ไปด้วยในตัว
   **แต่ยังไม่ได้ลบ prop drilling** ตามที่คาดไว้ตอนเขียนแผน — เพราะ Header ต้องใช้ข้อมูลชุดเต็มทุกหน้าอยู่ดี ถ้าจะลดจริงต้องทำ F4 (`type TaskActions`) แยกต่างหาก
2. **F2** — `src/lib/mappers.ts` เก็บ row type + mapper ที่ก๊อปกัน 3 ไฟล์ (C3)
3. **F3** — `src/lib/rpc.ts` helper `callRpc` ตามข้อ 4.4 (C4, C6, C8)
4. **F4** — `type TaskActions` ตัวเดียวแทน callback 8 ตัวที่ประกาศซ้ำ 4 รอบ (C2)
5. **F5** — component พื้นฐาน `<IconButton> <Badge> <Pill>` (C10)
6. **F6** — รวม taxonomy Board/Excom × Resume/Draft/Conduct ไว้แหล่งเดียว (C9)
7. **F7** — validate input ที่ชั้น action (C7)
8. **F8** — เขียน README จริง (C18)

**หยุดรอ approval หลังแต่ละข้อย่อย** ถ้าผู้ใช้อนุมัติให้ทำรวดเดียวค่อยรวม

---

## Phase G — Data model · ✅ เสร็จแล้ว (2026-09-15)

> **ผู้ใช้เลือก:** ทำครั้งเดียวจบ (ทางเลือกที่ 2) เพราะข้อมูลยังเป็นข้อมูลสมมติและมีแค่ 10 งาน — เป็นจังหวะที่เสี่ยงน้อยที่สุด
>
> **ทำอะไรไปบ้าง:**
> - `supabase/migrations/0005_task_owner_inspector_ids.sql` — เพิ่ม `owner_id` / `inspector_id` (FK → users) + ย้ายข้อมูลเดิม
> - ย้าย `history` ทุกบรรทัดให้มี `ownerId`/`inspectorId` (ชื่อเดิมยังเก็บไว้เป็นสำรอง)
> - ย้าย `excluded_auto_steps` จาก key แบบ `sent:ชื่อ` เป็น `sent:id` — ถ้าไม่ทำ ขั้นตอนที่ผู้ใช้ลบไว้จะโผล่กลับมา
> - เปลี่ยน RPC `create_task` / `update_task_details` / `update_task_workflow` ให้รับ id + ตรวจว่า id มีจริง
> - ฝั่งเว็บ: เทียบคนด้วย id ทั้งหมด · ชื่อที่แสดงเปิดจาก `PeopleIndex` (`indexPeople` / `personName` ใน board-derived.ts)
>
> **ผลตรวจสอบการย้ายข้อมูล:** 10 งาน · owner_id ครบ 10 · inspector_id ครบ 5 · ประวัติไม่ตกหล่นสักบรรทัด · ไม่มีชื่อกำพร้า
>
> **ทดสอบจริงบนเว็บ:** เปลี่ยนชื่อ "พี่โอ๋" → "พี่โอ๋นามใหม่" แล้ว **งานยังอยู่กับเขาครบ** ชื่อใหม่ขึ้นทั้งสถานะงาน/ป้ายรอตรวจ/Timeline แล้วเปลี่ยนกลับเป็นเดิมเรียบร้อย
>
> **✅ ลบคอลัมน์เดิมแล้ว** — `0006_drop_legacy_owner_inspector_columns.sql` (ผู้ใช้ยืนยันว่ายังไม่ใช่ข้อมูลงานจริง)
> ตรวจก่อนลบครบ 4 ข้อ: ไม่มีแถวที่ไม่มี id · id ชี้ถูกคนทุกแถว · ไม่มีฟังก์ชันอ้างถึง · ไม่มีโค้ดอ่าน
> จากนั้นทดสอบเส้นทางการเขียนทั้งหมดใหม่ (สร้าง/ส่งตรวจ/ปิดงาน/ลบ) เพราะการลบคอลัมน์ทำให้ชนิดผลลัพธ์ของฟังก์ชัน `returns tasks` เปลี่ยน
>
> **บทเรียนสำหรับคนทำต่อ:** ตอน rename `currentUserName` → `currentUserId` TypeScript **จับไม่ได้** เพราะเป็น string เหมือนกัน
> ทำให้เกือบส่งชื่อเข้าไปในฟังก์ชันที่ต้องการ id (กรอบแดง "งานของฉัน" จะไม่ขึ้นเลยโดยไม่มี error)
> → เวลาเปลี่ยนจากชื่อเป็น id **ต้องไล่ดูทุก call site ด้วยตาด้วย อย่าเชื่อ tsc อย่างเดียว**

---

### บันทึกทางเลือกเดิม (เก็บไว้อ้างอิง)

**เรื่อง B5: `tasks.owner` / `tasks.inspector` เก็บเป็นชื่อ ไม่ใช่ user id**

นี่คือข้อบกพร่องที่ใหญ่ที่สุดในเชิงข้อมูล (แก้ชื่อคน = งานเก่าขาดจากเขาทั้งหมด) แต่การแก้ต้อง migrate ข้อมูลจริงและกระทบเกือบทุกไฟล์

**ห้ามเริ่ม Phase นี้เองเด็ดขาด** ให้เสนอทางเลือกและผลกระทบให้ผู้ใช้ตัดสินใจก่อน:

- **ทางเลือก 1:** เพิ่มคอลัมน์ `owner_id` / `inspector_id` คู่ขนานไปก่อน เขียนทั้งสองที่ แล้วค่อย ๆ ย้าย read ไปใช้ id (ปลอดภัยที่สุด ใช้เวลานาน)
- **ทางเลือก 2:** migrate ครั้งเดียวจบ + แก้ทุกจุดพร้อมกัน (เร็วกว่า เสี่ยงกว่า ต้อง backup ก่อน)
- **ทางเลือก 3:** ไม่แก้ แต่ **ล็อกไม่ให้เปลี่ยน `display_name` ได้** ถ้าคนนั้นมีงานอยู่ในระบบ (workaround ราคาถูก แต่ปัญหาชื่อซ้ำยังอยู่)

พร้อมกันนี้ต้องถามผู้ใช้เรื่อง `admin_delete_user` ด้วยว่า เมื่อลบผู้ใช้แล้วงานที่เขาถืออยู่ควรเป็นอย่างไร (โอนให้ใคร / ค้างไว้ / ห้ามลบถ้ายังมีงานค้าง)

---

## Phase H — ฟีเจอร์ที่ผู้ใช้น่าจะอยากได้ (เสนอ ห้ามทำเอง)

จดไว้ให้ผู้ใช้เลือก **ห้ามเริ่มโดยไม่ได้รับคำสั่ง**:

- ค้นหางานจากชื่อเรื่อง / ECM / ผู้รับผิดชอบ (U6)
- ~~Badge แบบ unread จริง ต้องมีตาราง `board_post_reads` (U2, B6)~~ → ✅ **ทำแล้ว 2026-09-16** ตามคำสั่งผู้ใช้
  ไม่ได้สร้างตารางใหม่ตามที่เสนอไว้ — ใช้คอลัมน์ `board_posts.seen_by uuid[]` แทน (memo มีอายุสั้นและถูกลบทิ้งเป็นปกติ ข้อมูลการอ่านจึงควรหายไปพร้อมกัน) ดู migration `0007` และ `DESIGN.md` ข้อ 4.12
- Undo toast แทน `confirm()` (U4)
- ถังขยะแสดงวันที่ลบ + auto-purge (U17)
- เรียงตารางตามวันครบกำหนด/สถานะ (U18)
- ปรับ header บนมือถือ (U13)

---

## แบบฟอร์มรายงานท้ายแต่ละ Phase

```
Phase: <ชื่อ>
สถานะ: Ready for user check

แก้อะไรไปบ้าง:
- <รหัสปัญหา> <ไฟล์:บรรทัด> — <สรุปสั้น ๆ>

ตรวจอะไรบ้าง (ทำตามทีละขั้น):
1. ...

ยังไม่ได้แก้ / ติดปัญหา:
- <ถ้ามี ต้องเขียน ห้ามเว้นว่างเพื่อให้ดูเรียบร้อย>

คำถามที่ต้องให้ผู้ใช้ตัดสินใจ:
- <ถ้ามี>

ผลรัน: tsc <ผ่าน/ไม่ผ่าน> · lint <ผ่าน/ไม่ผ่าน> · test <ผ่าน x/y>
```
