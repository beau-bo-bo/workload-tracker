# AGENTS.md — Coding Assistant Permanent Working Rules

## 1. Working Principles
1. ทำระบบให้เล็กที่สุดก่อน (Keep it minimal)
2. ทำเฉพาะสิ่งที่อยู่ใน Scope เท่านั้น
3. ใช้ภาษาที่ผู้ใช้ทั่วไปเข้าใจ
4. ใช้ Manual Input และ Logic ที่ชัดเจน
5. อย่าเพิ่ม Feature หรือฟังก์ชันใด ๆ โดยไม่ได้รับอนุมัติจากผู้ใช้
6. รักษาข้อมูล Workflow และ History ให้ถูกต้อง
7. ให้ผู้ใช้ตรวจสอบได้ในทุก Phase
8. หยุดทันทีหลังจบแต่ละ Phase เพื่อรอ Approval

## 2. Phase Workflow
สำหรับทุก Phase ที่ดำเนินการ:
1. อ่าน Context Files ทั้ง 5 ให้ครบถ้วน
2. ตรวจสอบ Scope ของ Phase ปัจจุบัน
3. ลงมือทำเฉพาะ Phase ปัจจุบันเท่านั้น
4. ทดสอบสิ่งที่สร้างเบื้องต้น
5. อัปเดตไฟล์ `PROGRESS-TRACKER.md`
6. เปลี่ยนสถานะเป็น "Ready for user check"
7. แจ้งผู้ใช้ว่า Phase นี้ทำอะไรเสร็จบ้าง
8. บอกผู้ใช้ให้ชัดเจนว่าต้องตรวจสอบหน้าจอหรือฟังก์ชันใดบ้าง
9. **หยุดทำงานทันที**
10. รอ Explicit Approval จากผู้ใช้

## 3. Mandatory User Approval Gate (Critical Rule)
# DO NOT EVER proceed to the next phase unless the user has explicitly approved the current phase.

กฎนี้มีผลเหนือกว่า:
- ความเร็ว / ความสะดวก
- การทำงานต่อเนื่อง / การ Batch งาน
- การประเมินหรือความมั่นใจของ Coding Assistant
- Test ที่ผ่าน หรือ Deployment ที่สำเร็จ

### สิ่งต่อไปนี้ "ไม่ถือเป็น Approval":
- ผู้ใช้เงียบ / ไม่มี Feedback
- Test ผ่าน / Build ผ่าน / Deploy สำเร็จ
- Coding Assistant ประเมินแล้วว่างานสมบูรณ์ดี

*ต้องได้รับคำยืนยันที่ชัดเจนจากผู้ใช้เท่านั้น (เช่น "อนุมัติ Phase X ค่ะเขต")*

### ข้อห้ามเด็ดขาด (ห้ามทำเด็ดขาดก่อนได้ Approval):
- เริ่ม Phase ถัดไป
- Scaffold โครงสร้างของ Phase ถัดไป
- Prepare หรือเตรียมความพร้อมสำหรับ Phase ถัดไป
- สร้างส่วนหนึ่งส่วนใดของ Phase ถัดไป
- เตรียม Database สำหรับ Phase ถัดไป

## 4. How to Present a Phase for Review
เมื่อทำ Phase เสร็จ ให้แจ้งผู้ใช้ตามรูปแบบนี้:
1. ระบุชื่อ Phase ที่เสร็จสิ้น
2. สรุปสิ่งที่สร้างขึ้นมา
3. บอกหน้าจอที่ผู้ใช้ต้องเปิดใช้งาน
4. บอกขั้นตอนการทดลองทีละสเต็ป
5. แสดง User Check Checklist
6. บอก Acceptance Criteria
7. อัปเดตสถานะใน `PROGRESS-TRACKER.md` เป็น "Ready for user check"
8. **หยุดและรอการอนุมัติ**

## 5. Change Handling (กรณีมีการแก้ไขระหว่างตรวจ)
ถ้าผู้ใช้ขอแก้ไขระหว่างตรวจสอบ Phase:
1. เปลี่ยน Status ใน `PROGRESS-TRACKER.md`เป็น "Needs changes"
2. ยังคงอยู่ใน Phase เดิม ห้ามขยับไปไหน
3. แก้ไขเฉพาะจุดที่ผู้ใช้ต้องการ
4. ทดสอบระบบใหม่อีกครั้ง
5. เปลี่ยนสถานะกลับเป็น "Ready for user check"
6. รอการอนุมัติใหม่อีกครั้ง (คำขอแก้ไขและการแก้เสร็จไม่นับเป็น Approval)

## 6. File Update Rules (Project Memory)
เมื่อมีการเปลี่ยนแปลง Scope หรือ Decision สำคัญ ต้องอัปเดตไฟล์เหล่านี้ทันที:
- `PRODUCT.md` → ขอบเขตผลิตภัณฑ์
- `DESIGN.md` → หน้าตาและรูปแบบ UI/Interaction
- `PLAN.md` → ลำดับขั้นตอนการพัฒนา
- `PROGRESS-TRACKER.md` → สถานะปัจจุบัน
- `AGENTS.md` → กฎการทำงานถาวร

*ห้ามเก็บ Project State หรือการตัดสินใจสำคัญไว้เฉพาะในแชทเด็ดขาด*

**กฎดูแล `PROGRESS-TRACKER.md` (เพิ่ม 2026-09-15/16):** ไฟล์นี้ต้องมี Handoff Note **ฉบับเดียวเสมอ**
เขียนฉบับใหม่แล้ว **ย้ายฉบับเก่าไป `PROGRESS-ARCHIVE.md` ทันที** — เคยปล่อยให้สะสม 10 ฉบับจนไฟล์โต 483KB เปิดอ่านในแชทใหม่ไม่ไหวมาแล้ว
ส่วน `START-HERE.md` คือไฟล์ที่ใช้เปิดแชทใหม่/ส่งให้ AI ตัวอื่น ต้องอัปเดตให้ตรงกับความจริงเสมอ

## 6.1 กฎถาวรด้านเทคนิค (เพิ่ม 2026-09-15 หลังการตรวจโค้ดทั้งระบบ — ละเมิดข้อใดข้อหนึ่งเคยทำให้เกิดช่องโหว่ระดับยึดระบบมาแล้ว)

### ฐานข้อมูล
1. **ทุกการเปลี่ยนแปลงฐานข้อมูลต้องเขียนเป็นไฟล์ `.sql` ใหม่ใน `supabase/migrations/` เสมอ**
   ห้ามแก้ผ่าน Supabase Dashboard หรือ MCP แล้วไม่มีไฟล์ — ที่ผ่านมา business logic ทั้ง 42 ฟังก์ชันมีอยู่แค่บน production ทำให้ไม่มีใคร review ได้ และช่องโหว่รอดมาทุก Phase
2. **ห้ามใช้ prefix `NEXT_PUBLIC_` กับคีย์ Supabase ทุกกรณี** — ตัวแปรที่ขึ้นต้นด้วย `NEXT_PUBLIC_` ถูกฝังลงไฟล์ JavaScript ที่ส่งให้เบราว์เซอร์ทุกคน
3. **เข้าถึงฐานข้อมูลจากฝั่งเซิร์ฟเวอร์เท่านั้น** ผ่าน `SUPABASE_SERVICE_ROLE_KEY` — `src/lib/supabase.ts` มี `import "server-only"` ห้ามถอดออก
4. **service_role ข้ามด่านตรวจทุกอย่างของฐานข้อมูล** → **ทุก Server Action ต้องตรวจสิทธิ์ผู้ใช้เองก่อนเสมอ** ห้ามเชื่อ parameter ที่ client ส่งมาว่าเป็นตัวตนจริง
5. **ข้อมูลที่ผู้ใช้ไม่มีสิทธิ์เห็น ต้องไม่ถูกส่งออกจากเซิร์ฟเวอร์ตั้งแต่ต้น** — ห้ามส่งมาแล้วซ่อนด้วย UI (เปิดเครื่องมือ developer ก็อ่านได้)
6. RPC ที่เปลี่ยนชนิด/คอลัมน์ที่คืน ต้อง `drop function` ก่อน (`create or replace` ใช้ไม่ได้) และหลังลบคอลัมน์ของตาราง **ต้องทดสอบเส้นทางการเขียนใหม่ทั้งหมด**

### โค้ดฝั่งเว็บ
7. **ตรรกะสถานะงาน (workflow) ต้องอยู่ใน `src/app/task-workflow.ts` เท่านั้น** (pure function) และเพิ่ม test ใน `task-workflow.test.ts` เสมอ — ห้ามเขียนกลับเข้าไปใน component
8. **ห้ามใส่ `useCallback` / `memo()` ด้วยมือ** — โปรเจกต์นี้ lint ด้วยกฎของ React Compiler การ memo เองทำให้ compiler ข้ามการ optimize ทั้ง component (ถ้าจะเอาจริงต้องเปิด React Compiler ซึ่ง **ต้องถามผู้ใช้ก่อน**)
9. **อ้างถึงคนด้วยรหัสประจำตัว (id) เสมอ ห้ามใช้ชื่อเป็นตัวเชื่อม** — ชื่อเปลี่ยนได้ และเคยทำให้งานเก่าขาดจากเจ้าตัวทั้งหมดมาแล้ว
   ⚠️ เวลาเปลี่ยนตัวแปรจากชื่อเป็น id **TypeScript จับให้ไม่ได้** (เป็น string เหมือนกัน) ต้องไล่ดูทุก call site ด้วยตา

### การทดสอบและการรายงาน
10. ก่อนส่งงานให้ผู้ใช้ตรวจ ต้องรันให้ผ่าน: `npx tsc --noEmit` · `npm test` · `npm run build` และ **เทียบจำนวน error ของ `npm run lint` กับก่อนแก้** (ห้ามเพิ่ม error ใหม่โดยไม่รายงาน)
11. **วัดประสิทธิภาพที่งานฝั่งฐานข้อมูล (`pg_stat_statements`) ห้ามวัดด้วยจำนวน HTTP request** — Next.js แนบข้อมูลที่ revalidate มาใน response เดียวกัน ตัวเลข request จึงหลอกตา
12. **รายงานตามจริงเสมอ** รวมถึงตอนที่ตัวเองเคยสรุปผิด — ให้แก้เอกสารพร้อมเขียนกำกับว่าผิดตรงไหน ไม่ใช่ลบทิ้งเงียบ ๆ

## 7. Definition of Done
### Phase ถือว่าเสร็จเมื่อ:
1. ฟังก์ชันตาม Scope ของ Phase ทำงานได้สมบูรณ์
2. ข้อมูลสำคัญถูกบันทึกถูกต้อง
3. ไม่มี Scope นอกเหนือจากที่อนุมัติแอบแฝงเข้ามา
4. จัดเตรียมรายการ User Check พร้อม
5. `PROGRESS-TRACKER.md` ถูกอัปเดตเรียบร้อย
6. สถานะเป็น "Ready for user check"
7. Coding Assistant หยุดทำงานและรอ User Approval

### Project ถือว่าเสร็จสมบูรณ์เมื่อ:
- ทุก Phase ได้รับ Explicit Approval จากผู้ใช้ครบถ้วนทุกขั้นตอน
