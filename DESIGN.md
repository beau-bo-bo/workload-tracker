# DESIGN.md — UI/UX Standards

> ฉบับนี้แทนที่ดีไซน์แบบ Pale Yellow/Beige เดิมทั้งหมด สรุปจากการลองออกแบบ 3 แนวทาง (Minimal SaaS / Dark Ops Dashboard / Corporate) แล้วผสมและปรับแก้ร่วมกันจนได้ข้อสรุปสุดท้ายนี้ ดูตัวอย่างที่ทำงานได้จริง (interactive prototype) ได้ที่ Artifact "Workload Tracker Hybrid"

## 1. แนวคิดหลัก (Design Concept)
- **โครงสร้าง/เลย์เอาต์:** อ้างอิงแนว Minimal/Clean SaaS — โปร่ง อากาศเยอะ ไม่ใช่ทุกส่วนต้องเป็นการ์ด
- **โทนสี:** พาสเทล อ่อนโยน อิงจากโลโก้ กฟผ. จริง (แผนที่ไทยสีทอง + สายฟ้าสีแดง + ตัวอักษรสีน้ำเงิน) แต่ลดความจัดจ้านลง
- **โหมดมืด:** อนุญาตให้เข้มและมีสีสันมากกว่าโหมดสว่างได้ (ไม่ต้องพาสเทลเท่าโหมดสว่าง) ใช้บุคลิกที่หนักแน่นขึ้น พร้อมฟอนต์หัวข้อเฉพาะตัว (Chakra Petch)
- รองรับทั้ง Desktop และ Mobile (Mobile-friendly, responsive)
- **หลักการฟอร์ม/ปุ่มแก้ไข (Progressive Disclosure):** ฟอร์มแก้ไข/สร้างข้อมูล (เช่น แก้ไขผู้ใช้งาน, Reset Password, สร้างผู้ใช้งานใหม่) ต้องไม่โชว์ค้างไว้ตลอดเวลา ให้เริ่มจากปุ่ม/ไอคอนสั้นๆ ก่อนเสมอ (เช่น ไอคอนดินสอ หรือปุ่ม "+ เพิ่ม...") กดแล้วค่อยขยายเป็นฟอร์มพร้อมปุ่ม "บันทึก"/"ยกเลิก" และปิดฟอร์มกลับอัตโนมัติเมื่อบันทึกสำเร็จ — ใช้หลักการนี้กับฟอร์มใหม่ๆ ใน Phase ถัดไปด้วย (เช่น ฟอร์มสร้างวาระใน Phase 3)
  - **กล่องฟอร์มสร้างใหม่ (เพิ่มเรื่อง / เพิ่มครั้งที่ประชุม) เด่นขึ้นจากพื้นหลัง (2026-09-15):** เดิมกล่องที่ขยายออกมาใช้ `border-border`/`bg-bg`(หรือ `bg-surface`) กลืนกับพื้นรอบๆ มากเกินไป ผู้ใช้ขอให้เด่นขึ้น — ลองทำ 3 แนวทาง (แถบหัวสีทึบ / โทนน้ำตาลเข้มทั้งกล่อง / ขอบหนา+พื้นหลังนวลอุ่น) ผ่าน mockup เทียบกัน ผู้ใช้เลือก**ขอบหนา + พื้นหลังนวลอุ่น**: `border-2 border-primary bg-primary-soft` (จากเดิม `border border-border bg-bg`/`bg-surface`) — เพิ่ม token ใหม่ `--primary-soft` ใน `globals.css` (ไม่ใช้ `bg-primary/10` แบบโปร่งแสง เพราะเคยมีบทเรียนแล้วว่าสีโปร่งแสงของ `--primary` อ่านไม่ออกในโหมดมืด — ดูข้อ 2) ใช้กับทั้ง `CreateTaskForm.tsx` และ `CreateMeetingForm.tsx`
    - **แก้ dark mode อ่านยาก (รอบต่อมา, แทนที่ค่า dark เดิม):** ค่า dark ที่ตั้งไว้แรก (`#362c17` น้ำตาลเข้มอิ่มสี) ผู้ใช้ทดสอบจริงแล้วบอกว่าอ่านยาก — ตัวอักษร `text-muted`/เส้นขอบปุ่มแคปซูลที่ไม่ได้เลือก (โทนเย็น ปรับมาเข้ากับ `--surface`/`--border` เดิมของแอปซึ่งเป็นโทนน้ำเงินเข้ม) ไปชนกับพื้นหลังน้ำตาลอุ่นที่ใหม่ ทำให้ดูมัวๆ ไม่ใช่แค่คอนทราสต์ตัวเลขไม่พอ แต่โทนสีตีกัน (คนละ hue temperature) → ทดสอบสดในเบราว์เซอร์ก่อนแก้ไฟล์จริง (set CSS var ตรงๆ ผ่าน `javascript_tool` เทียบหลายค่า) พบว่า **ตั้ง `--primary-soft` (dark) ให้เท่ากับ `--surface` เดิมเป๊ะ (`#1b2029`)** อ่านง่ายสุด เพราะทุก element ข้างในยังคงอยู่บนโทนสีที่ออกแบบมาให้เข้ากันอยู่แล้ว (`--muted`/`--border`/input `bg-bg`) — ปล่อยให้ **ขอบทองหนา (`border-primary`) เป็นตัวสร้างความเด่นแทนพื้นหลังในโหมดมืด** (โหมดสว่างยังคงพื้นหลังครีมอุ่นเหมือนเดิม เพราะไม่มีปัญหา) — บทเรียน: เฉดสีสำหรับ "โหมดมืดที่เข้มขึ้นได้กว่าโหมดสว่าง" (ตามหลักการข้อ 1 บนสุดของไฟล์นี้) ไม่ได้แปลว่าใส่สีอิ่มตัวแบบเดิมได้เสมอไป ต้องเช็ค hue ให้ harmonize กับ token เย็นอื่นๆ ที่ใช้ร่วมอยู่ในองค์ประกอบเดียวกันด้วย ไม่ใช่แค่คำนวณ contrast ratio ตัวเลขอย่างเดียว

## 2. สี (Color Tokens)
ใช้ CSS custom properties เป็นคู่ light/dark เสมอ ห้าม hardcode สีตรงๆ ใน component

### โหมดสว่าง (Light)
> ปรับให้เข้มขึ้นกว่าเวอร์ชันแรก (2026-09-13) เพราะผู้ใช้ทดลองใช้จริงแล้วบอกว่าตัวอักษรสี/ปุ่มหลักจางเกินไป อ่านยาก องค์ประกอบกลืนกัน — ยังคงกรอบพาสเทลเดิมไว้ แค่เพิ่ม contrast

| Token | Hex | ใช้กับ |
|---|---|---|
| `--bg` | `#f6f4ee` | พื้นหลังหลัก (ไอวอรี่อุ่น) |
| `--surface` | `#ffffff` | การ์ด, header, table |
| `--border` | `#ddd4bd` | เส้นขอบ |
| `--text` | `#2b2e33` | ตัวอักษรหลัก |
| `--muted` | `#635c4d` | ตัวอักษรรอง/label |
| `--accent` | `#33517d` | แท็บที่เลือก, badge, ลิงก์ (ฟ้าพาสเทลจากตัวอักษรในโลโก้) |
| `--accent-soft` | `#dfe8f7` | พื้นหลัง badge/แท็บที่เลือก |
| `--primary` | `#ddb876` | ปุ่มหลัก, โลโก้ (ทองพาสเทลจากแผนที่ไทยในโลโก้ — ลดความเข้มลงจากเดิมตามคำขอผู้ใช้) |
| `--primary-soft` | `#fbf3e4` | พื้นหลังกล่องฟอร์มสร้างใหม่ (เพิ่มเรื่อง/เพิ่มครั้งที่ประชุม) คู่กับขอบ `border-2 border-primary` (เพิ่ม 2026-09-15) |
| `--on-primary` | `#40300c` | ตัวอักษรบนปุ่มสีทอง |
| `--pending` | `#9c2415` | สถานะรอตรวจ/ด่วน — โทนแดงชัดเจน (ผู้ใช้ขอย้ำอีกรอบให้แดงขึ้น ลดสัดส่วนน้ำตาลลงอีก) — บรรทัด timeline ที่ใช้สีนี้เพิ่ม `font-medium` ตามคำขอให้"หนาขึ้นนิดนึง" ด้วย |
| `--pending-mid` | `#eab5a6` (มืด: `#5f3529`) | ระดับกลางของ Heat Map (3-4 งาน) เพิ่ม 2026-09-16 — **ต้องเป็น token จริง ห้ามใช้สีโปร่งแสงทับ token เดิม** |
| `--pending-soft` | `#f5ded7` | พื้นหลัง pill สถานะรอตรวจ |
| `--done` | `#2f6b4a` | สถานะตรวจแล้ว (เขียวเซจ) |
| `--done-soft` | `#e2f0e7` | พื้นหลัง pill ตรวจแล้ว |
| `--closed` | `#8f8674` | สีกลาง/เฉยๆ ใช้ทั่วไป — ไม่เกี่ยวกับสถานะปิดงานโดยตรง (คืนค่าเดิม หลัง `--closed-soft` เคยถูกปรับเป็นเทาแล้วกระทบพื้น track ของ Tabs หลัก/badge ผู้รับผิดชอบโดยไม่ตั้งใจ) |
| `--closed-soft` | `#ece5d7` | พื้นหลัง track ของแท็บหลัก, badge วงกลมผู้รับผิดชอบ, hover state ทั่วไป |
| `--status-closed` | `#5b6470` | **เฉพาะ**สถานะ "ปิดงาน" ของวาระ/เรื่อง — โทนเทาล้วน ตั้งใจให้ซีด/นิ่งกว่าสถานะอื่นเพราะเป็นสถานะจบงานแล้ว ไม่ต้องดึงความสนใจเหมือน pending — แยก token ออกจาก `--closed` เพื่อไม่ให้กระทบสีอื่นที่ใช้ token เดียวกัน |
| `--status-closed-soft` | `#e7e8ea` | พื้นหลัง pill ปิดงาน และปุ่ม "ปิดงาน" คู่กับ `--status-closed` |
| `--note-soft` | `#fbeec6` | พื้นหลังกล่อง ECM/หมายเหตุ ในส่วนขยายของแถววาระ (เหลืองอ่อนโทนสติกเกอร์โน้ต ให้เด่นขึ้นจากพื้นการ์ด — เพิ่ม 2026-09-15, ดูข้อ 4.5.1) |

### โหมดมืด (Dark) — เข้มและสดขึ้นได้กว่าโหมดสว่าง
| Token | Hex |
|---|---|
| `--bg` | `#14181f` |
| `--surface` | `#1b2029` |
| `--border` | `#2b323e` |
| `--text` | `#e7e8ec` |
| `--muted` | `#9aa1ac` |
| `--accent` | `#97b4ef` |
| `--accent-soft` | `#22314a` |
| `--primary` | `#e3b34a` |
| `--primary-soft` | `#1b2029` (เท่ากับ `--surface` — ดูเหตุผลที่ข้อ 1) |
| `--on-primary` | `#241a05` |
| `--pending` | `#dd9a82` / soft `#3a2420` |
| `--done` | `#7cc99a` / soft `#1e3327` |
| `--closed` | `#9ba3af` / soft `#262a30` |
| `--status-closed` | `#9ba3af` / soft `#262a30` — เฉพาะสถานะ "ปิดงาน" ของวาระ (เท่ากับ `--closed` ในโหมดมืด เพราะไม่เคยมีปัญหาโทนอุ่นชนกันแบบโหมดสว่าง) |
| `--note-soft` | `= --surface` (ตั้งใจให้เหมือนเดิม ไม่แยกสี — ผู้ใช้ขอปรับเฉพาะโหมดสว่างเท่านั้น "ใน Dark mode ดูจะปรับแล้วแย่") |

**กติกา theme:** ประกาศ token เต็มชุดใน `:root` (light) เสมอ, override เฉพาะ token ซ้ำใน `[data-theme="dark"]` เพื่อรองรับ manual toggle — ห้ามมี token ที่นิยามอยู่แค่ในบล็อกใดบล็อกหนึ่ง

**Default theme = light เสมอตอนเข้าครั้งแรก (แก้ 2026-09-14, บั๊กเดิม):** เดิม `globals.css` มีบล็อก `@media (prefers-color-scheme: dark)` คอย auto-apply โหมดมืดตามการตั้งค่าเครื่อง/เบราว์เซอร์ **ก่อน** ที่ผู้ใช้จะเคยกดปุ่มสลับธีมเองเลย ทำให้ user คนแรกที่เข้าเว็บบนเครื่อง/เบราว์เซอร์ที่ตั้ง dark mode ไว้ (พบบ่อยบนมือถือ) เจอ dark mode ทันทีทั้งที่ไม่เคยเลือกเอง — ผู้ใช้ยืนยันว่าต้องการ **light mode เป็นค่าเริ่มต้นเสมอ ไม่สนใจการตั้งค่าเครื่อง** → ลบบล็อก `@media (prefers-color-scheme: dark)` ออกจาก `globals.css` ทั้งหมด (เหลือแค่ `:root` เป็น light เสมอ + `[data-theme="dark"]` สำหรับตอนกดสลับเอง) และแก้ `ThemeToggle.tsx` ไม่ให้ fallback ไป `window.matchMedia("(prefers-color-scheme: dark)")` อีกต่อไป (default เป็น `"light"` ตรงๆ ถ้ายังไม่เคยมีค่าใน `localStorage`) — หลังจากกดสลับธีมครั้งแรกแล้ว ยังคงจำค่าไว้ใน `localStorage` ต่อเบราว์เซอร์เหมือนเดิมทุกประการ (ไม่ผูกกับบัญชี user, คนละเครื่อง/เบราว์เซอร์ต้องตั้งใหม่)

**หน้า `/admin/users` — ตารางผู้ใช้งาน (2026-09-14, เขียนใหม่ทั้งโครงสร้าง):** ไม่ใช่ `<table>` อีกต่อไป เปลี่ยนเป็น CSS Grid list แบบเดียวกับแนวทาง `TaskRow.tsx`/`TASK_ROW_GRID` (การ์ดแยกแต่ละ user, header row ใช้ template เดียวกับทุกแถว) เพื่อรองรับ responsive แบบไม่มี horizontal scrollbar อีกต่อไป (เดิม `overflow-x-auto` + `min-w-[760px]` ทำให้มือถือต้องเลื่อนแนวนอน) — ยุบคอลัมน์ "Username" เดิมเป็น subtitle บรรทัดเล็กใต้ชื่อที่แสดง (ไม่แยกคอลัมน์แล้ว) เพื่อลดจำนวนคอลัมน์ให้พอดีจอมือถือ
  - `USER_ROW_GRID` (export จาก `UserRow.tsx`) มือถือ: `[grip 1.5rem][ชื่อ+username minmax(0,1fr)][Role 4.5rem][จัดการ 6rem]` — ปุ่ม "Reset Password" เป็นไอคอนกุญแจอย่างเดียว (`KeyIcon`, ไม่มีข้อความ) กดแล้วขยายฟอร์มเต็มความกว้างด้านล่างแถว (ใช้ pattern เดียวกับส่วนขยายของ `TaskRow.tsx`) เช่นเดียวกับฟอร์มแก้ไขผู้ใช้งาน (เดิมใช้ `colSpan` ของ `<td>`)
  - Desktop (ปรับ 2026-09-14 รอบ 2 ตามคำขอผู้ใช้): `[1.75rem][ชื่อ minmax(0,16rem)][Role 8rem][จัดการ 13.5rem]` — คอลัมน์ชื่อที่แสดงมีเพดานตายตัว (ไม่ใช่ `1fr` แบบแรกที่เคยลองแล้วดูดพื้นที่ว่างไปกองจนคอลัมน์ Role ถูกดันไปไกล) ทำให้คอลัมน์ Role ขยับมาชิดคอลัมน์ชื่อมากขึ้นตามที่ขอ — ปุ่ม "Reset Password" กลับมาโชว์ข้อความเต็มอีกครั้ง **เฉพาะ desktop** (ไอคอน+ข้อความ, ใช้ `hidden sm:inline` ซ่อนข้อความเฉพาะมือถือ, ต้องมี `whitespace-nowrap` กันข้อความตกบรรทัดในปุ่มแคบ) สไตล์ปุ่มเปลี่ยนจาก icon-hover เป็นปุ่มมีกรอบ+ข้อความเหมือนของเดิมก่อนรื้อ (`sm:border sm:border-border sm:hover:border-accent sm:hover:text-accent`) — คอลัมน์ "จัดการ" ต้องกว้างพอ (`13.5rem`) ให้ปุ่มข้อความยาวไม่ตกบรรทัด
  - **จอกว้างมาก (`lg:` ขึ้นไป, เพิ่ม 2026-09-14):** ผู้ใช้ขอให้ปุ่มลบชิดขวาสุดของการ์ดจริงๆ ตอนขยายจอ แทนที่จะกระจุกอยู่กลางๆ เหมือนคอลัมน์อื่น — เปลี่ยนคอลัมน์ "จัดการ" จากความกว้างคงที่ `13.5rem` เป็น `1fr` เฉพาะ breakpoint `lg:` ขึ้นไป (คอลัมน์อื่นๆ คงที่เหมือนเดิม) พร้อมเปลี่ยน container ปุ่มภายในจาก `justify-end` เป็น `lg:justify-between` — ผลคือ Reset Password ชิดซ้ายของคอลัมน์ (ห่างจาก Role), ไอคอนแก้ไข/ลบกระจายไปทางขวาสุด ปุ่มลบชิดขอบขวาจริงของการ์ดพอดี
- **ฟอร์มแก้ไขผู้ใช้งานเพิ่ม checkbox "กลุ่มตรวจ" (2026-09-14):** ต่อท้าย checkbox Role เดิม — แถว "กลุ่มตรวจ (เฉพาะ Inspector, เว้นว่าง = ทุกกลุ่ม):" ตามด้วย checkbox Resume/ร่างรายงาน/Conduct (ใช้ `REVIEW_GROUPS` จาก `board-types.ts` เป็น single source of truth ร่วมกับ Sub-tab ในหน้าบอร์ด) โชว์ทุกแถวไม่ว่า role จะติ๊ก Inspector หรือไม่ (ฟอร์มเป็น uncontrolled ไม่มี state ให้ซ่อน/โชว์แบบ reactive จึงเลือกโชว์เสมอ ไม่กระทบอะไรถ้าติ๊กทั้งที่ไม่ใช่ Inspector) ส่ง submit พร้อมกับชื่อ+Role ในฟอร์มเดียวกันผ่าน RPC `admin_update_user` (เพิ่ม param `p_review_groups`)
- **การจัดลำดับเปลี่ยนจากปุ่มขึ้น/ลงเป็น Drag and Drop (2026-09-14):** ลบ `ReorderUserButtons.tsx`/ปุ่ม chevron ออกทั้งหมด ใช้ library `@dnd-kit/core` + `@dnd-kit/sortable` + `@dnd-kit/utilities` แทน (ติดตั้งใหม่ — เลือก dnd-kit เพราะรองรับทั้ง mouse/touch/keyboard ผ่าน Pointer Events โดยไม่ต้องเขียน drag math เอง ซึ่งจำเป็นเพราะต้องใช้งานได้บนมือถือด้วย ไม่ใช่แค่ desktop) — ตัวจับลาก (`GripIcon`, ไอคอนจุด 6 จุด, fill แทน stroke เป็นข้อยกเว้นเดียวในชุดไอคอนเส้น) อยู่ซ้ายสุดของแต่ละแถว ลากเฉพาะจากจุดนี้เท่านั้น (ไม่ใช่ลากทั้งแถว) — จัดลำดับแล้ว persist ไป Supabase ทันทีผ่าน RPC ใหม่ `admin_set_user_order(p_admin_id, p_user_ids[])` (set `sort_order` ทั้งชุดเป็น atomic ตาม index ของ array ที่ส่งมา ต่างจาก RPC เดิม `admin_reorder_user` ที่ทำทีละคู่สลับ — RPC เดิมยังอยู่ใน DB แต่โค้ดไม่เรียกใช้แล้ว) — **สำคัญ:** ห้ามเรียก `revalidatePath` ใน `setUserOrderAction` เพราะเคยลองแล้วชน error "Cannot update a component (Router) while rendering a different component (UserList)" (การเรียก Next.js router refresh ระหว่างที่ dnd-kit ยังอยู่ในช่วง commit ของ pointer event) — local state (`UserList.tsx`) เป็น source of truth พอสำหรับ session ปัจจุบันอยู่แล้ว ไม่ต้อง revalidate ทันที, ต้องให้ `setUsers(next)` เรียกแยกออกมาก่อน แล้วค่อย `startTransition(() => setUserOrderAction(...))` แยกต่างหาก (ห้ามเรียก `startTransition` ซ้อนอยู่ข้างในตัว updater function ของ `setUsers`) — `DndContext` ต้องใส่ `id` คงที่ (เช่น `id="user-list"`) กันปัญหา hydration mismatch ของ `aria-describedby` (dnd-kit auto-gen id ด้วย counter ที่ต่างกันระหว่าง server/client)

- **ฟอร์มแก้ไขผู้ใช้งานเพิ่ม checkbox "สร้าง/แก้ไข/ลบวาระได้" (2026-09-14, DB Persistence Migration):** ต่อท้ายกลุ่มตรวจ — สิทธิ์ใหม่ (`can_manage_tasks` boolean, default เปิดทุกคน) **แยกอิสระจาก Role เดิม** ไม่ผูกกับ Owner/Inspector/Admin ควบคุมเฉพาะปุ่มงานเชิงโครงสร้างบนหน้าบอร์ด: "+ ครั้งที่ประชุม", "+ เพิ่มเรื่อง", "แก้ไขชื่อ", "ย้ายไปที่เก็บ", "ลบ", "แก้ไขข้อมูล", กู้คืน/ลบถาวรในถังขยะ — **ไม่กั้นปุ่ม Action ของ workflow** (ส่งตรวจ/ตรวจแล้ว/ปิดงาน/ย้อนสถานะ ยังเปิดให้ทุกคนกดตามหลักการ "ยืดหยุ่น" เดิมของ Phase 4) ปิดสิทธิ์นี้แล้วปุ่มเชิงโครงสร้างจะหายไปจากหน้าจอเลย (ไม่ใช่แค่ disabled) และถูกเช็คซ้ำฝั่ง RPC ด้วยเสมอ

**ขอบเขตการใช้ token (2026-09-14):** ทุกหน้าที่อยู่หลัง login ต้องใช้ token ชุดนี้ทั้งหมด ห้าม hardcode สี Tailwind ตรงๆ (เช่น `zinc-900`, `border-black/10`, `text-red-600`) หรือใช้ `dark:` variant เปล่าๆ เด็ดขาด เพราะ `dark:` ของ Tailwind v4 (ไม่มี `@custom-variant` override) จะฟังแค่ `prefers-color-scheme` ของเครื่อง ไม่ฟัง `data-theme` ที่ `ThemeToggle.tsx` ตั้ง ทำให้หน้านั้นไม่ซิงค์กับปุ่มสลับธีมของแอป — หน้า `/admin/users` (และ subcomponent ทั้งหมดในโฟลเดอร์นั้น) ย้ายมาใช้ token ชุดนี้ครบแล้ว, หน้าถังขยะ (`TrashView.tsx`) ใช้ token มาตั้งแต่แรก — เหลือแค่หน้า `/login` ที่ยังไม่ได้ย้าย (นอกสโคปที่ขอจนถึงตอนนี้) — pattern ที่ใช้ซ้ำได้: ปุ่มลบ/destructive ใช้ `text-pending hover:bg-pending-soft` (ไม่ใช่ `red-600`), ปุ่ม icon เฉยๆ (แก้ไข/เลื่อนลำดับ) ใช้ `text-muted hover:bg-closed-soft hover:text-text`, ปุ่มหลัก/บันทึกใช้ `bg-primary text-on-primary`, ฟอร์ม input ใช้ `border-border bg-surface text-text focus:border-accent`

## 3. ตัวอักษร (Typography)
- **Body / UI ทั่วไป (ทั้งสองโหมด):** `IBM Plex Sans Thai` — รองรับไทย+อังกฤษในฟอนต์เดียว อ่านง่าย เป็นมิตร
- **หัวข้อในโหมดมืดเท่านั้น** (ชื่อแอป, ชื่อครั้งที่ประชุม, หัวข้อหน้าถังขยะ): `Chakra Petch` — ให้บุคลิกที่หนักแน่น/เทคนิคขึ้นตอนกลางคืน
- **หัวข้อในโหมดสว่าง:** ใช้ `IBM Plex Sans Thai` น้ำหนัก 600 (ไม่ต้องเปลี่ยนฟอนต์)
- โหลดจาก Google Fonts: `IBM+Plex+Sans+Thai:wght@400;500;600;700`, `Chakra+Petch:wght@500;600;700`

## 4. Layout โครงสร้างหน้าจอ

### 4.1 Header (บนสุด, sticky — แก้ 2026-09-15)
ซ้าย: โลโก้สี่เหลี่ยมมุมโค้ง (พื้น `--primary`) + ชื่อแอป + คำอธิบายสั้น
ขวา: ชื่อผู้ใช้ (ข้อความล้วน ไม่ต้องมี avatar ตัวย่อ ไม่ต้องโชว์ role) → ปุ่ม **"จัดการผู้ใช้งาน"** (โชว์เฉพาะบัญชีที่มี role `Admin` — รวมถึง Co-admin ที่ถูก assign role เดียวกัน) → ปุ่ม "เปลี่ยนรหัสผ่าน" → ปุ่ม "ออกจากระบบ" → **ปุ่มสลับธีมสว่าง/มืด** (ไอคอนดวงอาทิตย์/ดวงจันทร์ — **ค่าเริ่มต้นตอนเข้าครั้งแรกเป็น light เสมอ ไม่สนการตั้งค่าเครื่อง** ดูรายละเอียด/เหตุผลที่ข้อ 2 หัวข้อ "กติกา theme" — กดแล้วจำค่าไว้ใน `localStorage` ต่อเบราว์เซอร์) → ไอคอนกราฟ **"แดชบอร์ดภาระงาน"** (`ChartIcon`, เพิ่ม 2026-09-15 ดูข้อ 4.8) → ไอคอนถังขยะ (ไม่ต้องมีตัวเลขนับแจ้งเตือน)

- **ผู้ใช้ย้อนกำหนดเดิม — เพิ่ม Avatar กลับมา + ย้ายชื่อผู้ใช้ไปชิดชื่อแอป (2026-09-15 ต่อเนื่อง):** ก่อนหน้านี้ข้อ 7 ("สิ่งที่ตัดออก") บันทึกไว้ว่าตัด avatar ออกแล้วใช้ข้อความชื่อเฉยๆ — ผู้ใช้ขอกลับมาเพิ่มใหม่พร้อมย้ายตำแหน่ง หลังถามยืนยัน (เพราะขัดกับการตัดสินใจเดิม + ไม่มี field เก็บ avatar/mood ในระบบ) ผู้ใช้เลือก **"วงกลมตัวย่อชื่อ + สีประจำตัว"** (ไม่ใช่รูปจริง/ไม่ต้องเลือก emoji เอง — ไม่ต้องเพิ่ม DB column ใหม่) และ **ข้ามฟีเจอร์ "today mood"** ไปก่อน (นอก scope ที่มีอยู่ใน `PRODUCT.md` ไม่ได้ทำ)
  - ตำแหน่งใหม่: ย้าย **ชื่อผู้ใช้ (พร้อม avatar วงกลม)** จากฝั่งขวา (เดิมอยู่ปนกับกลุ่มไอคอน) มาอยู่ฝั่ง**ซ้าย ต่อจากชื่อแอป** โดยมีเส้นคั่นบางๆ กั้นระหว่างชื่อแอปกับ avatar+ชื่อผู้ใช้
  - Avatar: วงกลม (`h-8 w-8 rounded-full`) พื้นหลังวน Token pastel เดิม 5 สี (`accent`/`pending`/`done`/`primary`/`closed` soft-bg) เลือกจาก hash ของชื่อแบบ deterministic (`avatarColorClasses()` ใน `board-derived.ts`) — ลองมาแล้ว 2 รอบ: (1) ตัวอักษรย่อ 2 ตัว → (2) อีโมจิสัตว์ Unicode → **ล่าสุด (2026-09-15 ต่อเนื่องอีกรอบ) ผู้ใช้ขอ "ตลกๆ น่ารัก minimal" แบบมีสไตล์ชัดเจนกว่า emoji ปกติ (ส่งภาพตัวอย่างหน้าการ์ตูนแบบเส้นมาให้ดูสไตล์ — ระบุว่าแค่ตัวอย่าง ไม่ต้องเหมือนเป๊ะ)** → เปลี่ยนมาวาด**หน้าตัวการ์ตูนเส้น (line-art) เองในโค้ด** แทน emoji ไปเลย ให้ตรงกับสไตล์ไอคอนเส้นของแอปเองอยู่แล้ว (ตาม `icons.tsx`/ข้อ 5 ด้านล่าง) ไม่ใช้รูปจากที่อื่น/ไม่ใช้ service ภายนอก — Component ใหม่ `AvatarFace.tsx` (`src/components/`) มี 10 หน้า วาดด้วย `<svg>` primitives (circle/ellipse/path) ธรรมดา สีหน้า = `currentColor` รับสีจาก class `text-*` คู่กับพื้นหลัง `bg-*-soft` เดียวกัน (เช่น `text-accent` คู่ `bg-accent-soft`) ให้กลืนกันเป็นชุดเดียว — เลือก variant ไหนต่อคนแบบ deterministic เหมือนสี (`avatarFaceVariant()` แทน `avatarEmoji()` เดิมที่ลบออกแล้ว)
    - **รอบแรกผสม eye/mouth แบบระบบ (5 แบบตา × 5 แบบปาก) แล้วผู้ใช้ทักว่า "4 อันนี้แทบไม่ต่างเลย"** เพราะบางคู่ตา (dot/oval) และปาก (smile/grin) ต่างกันน้อยเกินไปตอนย่อเล็กจนแยกไม่ออก — **แก้ทั้งหมดเป็นวาดหน้าเฉพาะทีละหน้า (bespoke) ไม่ผสมแบบระบบอีกต่อไป** ให้ครอบคลุมอารมณ์ที่ผู้ใช้ขอมาตรงๆ ครบทุกอัน: ยิ้มธรรมดา, **ยิ้มอ้าปากกว้าง/หัวเราะ** (ตาปิดโค้ง ^ ^ + ปากรูปไข่ทึบ), ขยิบตา, **แลบลิ้น** (ตาปิดโค้ง + ปากอ้า + ลิ้นรูปสามเหลี่ยมมุมมนทึบด้านใต้), **หน้าโกรธ** (คิ้วเอียงเป็น V ลงมา + ตาเล็ก + ปากเส้นตรง), **ร้องไห้** (ตาเล็ก + หยดน้ำตารูป teardrop ใต้ตาซ้าย + ปากยิ้มหัวคว่ำ), **หน้าง่วง** (ตาหนังตาหย่อนโค้งลง + ปาก o เล็กเหมือนหาว), **หน้างง/มึน** (ตากากบาท ✕ ✕ ทั้งสองข้าง + ปากคลื่นเบาๆ), ตกใจ (ตากลมใหญ่ + ปาก o), เฉยๆ/กวนๆ (ตาเส้นตรงแบน + ปากเส้นตรง) — ทดสอบผ่าน Browser จริงแล้วแยกแบบกันได้ชัดเจนทุกหน้า
  - **Responsive (ผู้ใช้ขอเพิ่ม):** ก่อนหน้านี้ชื่อผู้ใช้อยู่ในกลุ่ม `flex-wrap` เดียวกับปุ่มไอคอนทั้งหมด ทำให้จอแคบแล้วชื่อผู้ใช้หลุดไปปนกับไอคอนตอนตกบรรทัด — แก้โดยแยกกลุ่มโลโก้+ชื่อแอป+avatar+ชื่อผู้ใช้ให้เป็นก้อนเดียว (`flex-none` ไม่หด/ไม่ยืดเทียบกับกลุ่มไอคอนฝั่งขวา) ส่วนกลุ่มไอคอนฝั่งขวายังเป็น `flex-wrap` ของตัวเองเหมือนเดิม ผลคือถ้าจอแคบจนต้องตกบรรทัด จะมีแค่ **กลุ่มไอคอนเมนูเท่านั้น** ที่ตกลงไปอยู่บรรทัดใหม่ ชื่อผู้ใช้ยังอยู่บรรทัดเดียวกับชื่อแอปเสมอ (ยกเว้นจอแคบมากจริงๆ ที่ก้อนซ้ายเองจะขึ้นบรรทัดที่ 2 แยกจากกลุ่มไอคอนอยู่ดี ไม่ปนกัน)
  - **เลือก Avatar เองได้แล้ว (เพิ่ม 2026-09-15 ต่อเนื่องอีกรอบ, DB Persistence):** ผู้ใช้ยืนยันให้ทำหลังถูกถามยืนยันเรื่องต้องเพิ่มคอลัมน์ DB จริง — กดที่วงกลม avatar ที่ Header เปิดป็อปอัพเล็กๆ (`w-52`, grid 5 คอลัมน์ 2 แถว) ให้เลือกจาก 10 หน้าเดิมของ `AvatarFace.tsx` (ไม่ใช่พิมพ์/อัปโหลดเอง) หน้าที่เลือกอยู่ตอนนี้มีวงแหวนสี accent ล้อมรอบ — เก็บค่าเป็น column ใหม่ `users.avatar_variant` (int, nullable — `null` = ยังไม่เคยเลือก ใช้ค่า deterministic จากชื่อเหมือนเดิมเป็น fallback) เขียนผ่าน RPC ใหม่ `set_own_avatar(p_user_id, p_avatar_variant)` (validate ช่วง 0-9 ฝั่ง DB ด้วย ไม่เชื่อ client อย่างเดียว) อ่านกลับมาผ่าน `get_own_permissions` ที่ขยาย return type ให้คืน `avatar_variant` มาด้วย (เดิมคืนแค่ `can_manage_tasks`) — ฝั่ง React ใช้ optimistic update (`setAvatarVariant` ก่อนเรียก action แล้ว rollback ถ้า error) ป็อปอัพปิดเองทันทีที่เลือก
    - **บั๊กที่เจอตอน build (แก้แล้ว):** ตอนแรกป็อปอัพใช้ `grid grid-cols-5` แบบไม่ตั้ง width ชัดเจน เพราะ container เป็น `position: absolute` ไม่มีความกว้างอ้างอิง ทำให้ track ของ grid ยุบเหลือ ~12px/คอลัมน์ (จาก `minmax(0,1fr)`) ปุ่ม avatar ขนาด 32px เลย overflow ทับกันเองจนอ่านไม่ออก และยังไปทับแถวไอคอนของ Header ที่ตกบรรทัดมาอยู่ตรงนั้นพอดี (ยืนยันด้วย `getBoundingClientRect()` ผ่าน `javascript_tool`) — แก้โดยตั้ง `w-52` ชัดเจนบน container ของป็อปอัพ (แก้ปัญหาตรงจุด ไม่ต้องพึ่ง z-index เพราะ z-30 ที่เพิ่มไปตอนแรกไม่ได้แก้ปัญหาที่ต้นเหตุ)

**Sticky (เพิ่ม 2026-09-15):** Header ติดอยู่บนสุดของจอเสมอตอนเลื่อนหน้าลง (`sticky top-0 z-20` บน `<header>` ใน `AppShell.tsx`) — เดิมระบุไว้ว่า "ไม่จำเป็นต้อง sticky" ผู้ใช้ขอเปลี่ยนภายหลัง เพราะเนื้อหาหลัก (รายการครั้งที่ประชุม/วาระ) ยาวเกินจอบ่อย การเข้าถึงปุ่ม Header (ถังขยะ/แดชบอร์ด/ธีม) ควรทำได้ตลอดโดยไม่ต้องเลื่อนกลับขึ้นไปบนสุด

### 4.2 Tabs หลัก (Level 1: Board / Excom / อื่น ๆ)
- **Segmented control เต็มความกว้างหน้าจอ** (ไม่ใช่ปุ่มขนาดเนื้อหาลอยชิดซ้าย) แบ่ง 3 ส่วนเท่ากัน (`flex:1`)
- พื้น track ใช้ `--closed-soft`, ปุ่มที่เลือกเป็น pill สีขาว/`--surface` ลอยมี shadow บางๆ
- มีเส้นแบ่งบางๆ (`--border`) คั่นระหว่างสองปุ่มที่ไม่ได้เลือกและอยู่ติดกัน เพื่อไม่ให้ปุ่มที่ไม่ได้เลือกกลืนเป็นก้อนเดียวกัน — เส้นนี้จะหายไปข้างปุ่มที่ถูกเลือกอยู่ (ใช้ `:has(+ .is-active)` หรือเทียบเท่า)
- **Red Badge notification (2026-09-14):** วงกลมแดงเล็ก **อยู่ในตัวปุ่ม ต่อจากข้อความ label** (`flex items-center gap-1.5`, ไม่ใช่ `absolute` มุมขวาบนอีกต่อไป — ปรับ 2026-09-14 รอบ 2 ตามภาพตัวอย่างที่ผู้ใช้ส่งมา เพราะตำแหน่งมุมขวาบนแบบเดิมล้นขอบปุ่มตอนจอมือถือแคบ) `bg-pending text-bg` ทึบตัน — คนละ pattern จาก pill สถานะทั่วไปที่ใช้คู่ soft-bg/colored-text เพราะตั้งใจให้เด่นแบบ notification dot) ซ่อนไปเลยถ้าค่าเป็น 0 (ต่างจากบรรทัด "เอกสารรอตรวจอยู่ที่" ที่โชว์ 0 เป็น default) — ตัวเลขคือ **จำนวนงานที่รอ Action ของคนที่ login อยู่ ณ ขณะนั้นเท่านั้น** (personalized ไม่ใช่ยอดรวมทุกคน ตาม `PRODUCT.md` ข้อ 5/7 "แจ้งเตือนถูกคน ถูกเวลา"): สถานะ `blank` ที่ตนเป็น owner (งานถูก assign มาแล้ว รอกด "ส่งตรวจ" — เพิ่ม 2026-09-14 ตามคำทักของผู้ใช้ว่า blank ก็ถือเป็นรอ action เหมือนกัน) + สถานะ `pending` ที่ตนเป็น inspector + สถานะ `done` ที่ตนเป็น owner (งานตีกลับมาแล้ว) รวมกัน — **ไม่รวมสถานะ `closed` เด็ดขาด** แม้จะกลับไปอยู่ที่ Owner ตามหลักการก็ตาม (ผู้ใช้ยืนยันชัดเจน) — คำนวณจาก `myNotificationCount(tasks, displayName)` ใน `board-derived.ts` — มือถือลดขนาด/padding ปุ่ม Sub-tab ลงเล็กน้อย (`px-2.5` แทน `px-5`, badge `h-4.5` แทน `h-5`) กันล้นขอบจอที่ 375px
- **Tab "Board"/"Excom" นับจากครั้งที่ประชุมที่ผูกกับ Tab นั้นจริง** (ดู `Meeting.tab` ข้อ 4.4) ไม่ใช่ข้อมูลชุดเดียวกันซ้ำกันทั้ง 2 Tab เหมือนก่อนหน้านี้ — Tab "อื่น ๆ" นับจาก `otherTasks` แยกเป็นของตัวเอง — ตัวเลขบน Tab หลักคือ**ยอดรวมทุก Sub-tab** ภายใต้ Tab นั้น (`mainTabNotifications`)

### 4.3 Tabs ย่อย (Level 2: Resume / ร่างรายงาน / Conduct) — แสดงเฉพาะ Board/Excom
- **แคปซูลกลมเต็มใบ (`border-radius: 999px`) เต็มความกว้าง แบ่ง 3 ส่วนเท่ากัน** ไม่ใช่ underline text เหมือนดีไซน์แรกเริ่ม
- ปุ่มที่ยังไม่เลือก: มีกรอบเส้นบาง (`1px solid var(--border)`) ให้เห็นขอบเขตชัดเจนตั้งแต่แรก ไม่ต้องรอ hover
- ปุ่มที่เลือก: พื้นหลัง `--accent-soft` + ตัวอักษรสี `--accent` + font-weight 600, ไม่มีเส้นขอบ (border-color: transparent)
- ให้ความรู้สึกเป็น "ชั้นรอง" จาก Tabs หลัก (ที่เป็นแคปซูลทึบ) แต่ยังดูมีน้ำหนักและสวยงาม ไม่ใช่แค่เส้นบางๆ
- **Red Badge (อัปเดต 2026-09-14 รอบ 3):** สไตล์เดียวกับ Tab หลัก (inline ต่อจาก label) — **ตัวเลขนับเฉพาะครั้งที่ประชุมที่ผูกกับ Sub-tab นั้นจริงแล้ว** (`subTabNotifications[key]`, ดู `Meeting.subTab` ข้อ 4.4 — เดิมทำเป็น hack ใช้ `reviewGroups` ของผู้ใช้เองกรองเพราะยังไม่มีข้อมูลจริง ตอนนี้เลิกใช้ hack นั้นแล้วเพราะมีข้อมูลจริงให้กรองแล้ว) แต่ละ Sub-tab จะโชว์เลขต่างกันจริงตามที่มีงานอยู่ในนั้น ไม่ใช่เลขเดียวกันซ้ำทุกกล่องเหมือนก่อนหน้า

### 4.4 ปุ่มสร้างครั้งที่ประชุม
ปุ่ม **"+ ครั้งที่ประชุม"** (สีทอง/`--primary`) วางชิดขวา เหนือรายการครั้งที่ประชุม
- **ฟอร์มสร้างครั้งที่ประชุมเพิ่มตัวเลือก Board/Excom (2026-09-14):** ก่อนช่องชื่อครั้งที่ประชุม เพิ่มปุ่มเลือก "Board"/"Excom" (สไตล์แคปซูลเดียวกับ Sub-tab ข้อ 4.3) ค่า default = Tab ที่กำลังดูอยู่ตอนกดปุ่มนี้ แก้ไขได้ก่อนบันทึก — เก็บเป็น field `Meeting.tab: "board" | "excom"` (บังคับมีค่า) ใน `board-types.ts` ใช้กรองว่าครั้งที่ประชุมไหนแสดงใน Tab ไหนจริง (`visibleMeetings` ใน `AppShell.tsx`) ปิด known gap เดิมที่ Board/Excom เคยโชว์ข้อมูลชุดเดียวกันซ้ำกันทั้งคู่
- **ขยายเพิ่มตัวเลือก Sub-tab ด้วย (2026-09-14 รอบ 2 — ตรงกับที่ `PRODUCT.md` ข้อ 3 ตั้งใจไว้แต่แรก "ภายใต้แต่ละ Tab ย่อย... สามารถกดปุ่ม + สร้างครั้งที่ประชุม"):** เพิ่มปุ่มเลือก "Resume"/"ร่างรายงาน"/"Conduct" ต่อจากตัวเลือก Board/Excom (สไตล์เดียวกัน) ค่า default = Sub-tab ที่กำลังดูอยู่ — เก็บเป็น field `Meeting.subTab: "resume" | "draft" | "conduct"` (type `MeetingSubTab` บังคับมีค่า) ใน `board-types.ts` ใช้กรอง `visibleMeetings` เพิ่มอีกชั้น (`m.tab === mainTab && m.subTab === subTab`) — ปิด known gap ที่เหลือ (Sub-tab ไม่เคยกรองครั้งที่ประชุมจริงมาตั้งแต่ Phase 2) ทำให้ Red Badge ของแต่ละ Sub-tab (ข้อ 4.3) โชว์เลขต่างกันจริงตามงานที่มีในนั้นแล้ว
- **เลือกต้นแบบวาระจาก Resume ตอนสร้างใน "ร่างรายงาน" (เพิ่ม 2026-09-15):** เมื่อ Sub-tab ที่เลือกอยู่ในฟอร์มเป็น `draft` (ร่างรายงาน) เท่านั้น จะมี field เพิ่มขึ้นมาก่อนช่องชื่อครั้งที่ประชุม — dropdown **"เลือกต้นแบบวาระจาก Resume (ถ้ามี)"** option แรกสุดคือ `-- ไม่ใช้ต้นแบบ (เริ่มจากว่าง) --` ตามด้วยรายชื่อครั้งที่ประชุมที่อยู่ใน Sub-tab `resume` **กรองเฉพาะ Tab เดียวกับที่กำลังเลือกอยู่ในฟอร์ม** (สลับ Tab แล้ว dropdown/ค่าที่เลือกไว้รีเซ็ตอัตโนมัติ) — เลือกต้นแบบแล้ว: (1) ช่อง "ชื่อครั้งที่ประชุม" ถูกเติมด้วยชื่อของต้นแบบทันที (ยังแก้ไขได้ก่อนบันทึก ไม่ได้ล็อก) (2) มีข้อความช่วยอธิบายบอกจำนวนวาระที่จะถูกดึงมาด้วย (3) ตอนกด "บันทึก" นอกจากสร้างครั้งที่ประชุมแล้ว จะวนสร้างวาระใหม่ทีละรายการ (`createTaskAction` ต่อรายการ, sequential ไม่ parallel) โดยเอามาแค่ **ชื่อวาระ** เท่านั้น — **ไม่เอา** ผู้รับผิดชอบ/สถานะ/ECM/วันครบกำหนด/ด่วน/หมายเหตุของต้นแบบมาด้วยเลย (เริ่มทุกฟิลด์เหล่านี้จากค่าว่าง/false ให้ผู้ใช้มากรอกเอง) — field ใหม่ `templateTaskTitles?: string[]` ส่งผ่าน `onCreate` ใน `CreateMeetingForm.tsx` ไปให้ `createMeeting()` ใน `AppShell.tsx` เป็นคนวนสร้าง — ฟีเจอร์นี้ทำให้ `Task.owner` ต้องเปลี่ยนเป็น optional (ดูข้อ 4.5.1) เพราะ RPC เดิมบังคับกรอกผู้รับผิดชอบมาตลอด

### 4.5 รายการครั้งที่ประชุม (Meeting Sessions)
- แสดงเป็น **กล่อง (การ์ด) แยกกันเรียงไล่ลำดับลงมาทีละกล่อง** ไม่ใช่ chip/dropdown ให้เลือก
- แต่ละการ์ด:
  - หัวการ์ด: ชื่อครั้งที่ประชุม (font ใหญ่, ใช้ `--display-font`) + ไอคอนเส้น **แก้ไข (สี่เหลี่ยมมุมมนพร้อมดินสอที่มุม — square-pen)** และ **ย้ายไปที่เก็บ (ตาขีดเส้นทับ — eye-off, ปรับจาก archive box เดิม 2026-09-13 ตามตัวอย่างที่ผู้ใช้ส่งมา)** — ไอคอนเป็นเส้น (outline/stroke) แบบ minimal เท่านั้น **ห้ามใช้ emoji** — ฝั่งขวา: ป้ายสถานะรวม (เช่น "ปิดงาน 4/26") + ปุ่ม "+ เพิ่มเรื่อง" (สีทอง)
  - **ปุ่มแก้ไข ("แก้ไขข้อมูล") ทำงานจริงแล้ว (2026-09-14 — เดิมเป็นปุ่มเปล่าไม่มี `onClick` มาตั้งแต่สร้าง ไม่มีใครสังเกตจนผู้ใช้ลองกด):** กดแล้วสลับพื้นที่หัวการ์ดทั้งแถบ (ไม่ใช่แค่ชื่อ) เป็นฟอร์ม inline 2 ช่อง — **ชื่อครั้งที่ประชุม** (text, required) และ **รายละเอียด** (text, optional, ค่าเดียวกับ field `timeline` ที่ใช้ตอนสร้างครั้งที่ประชุม) พร้อมปุ่ม "บันทึก"/"ยกเลิก" — ต้องมีทั้ง 2 ช่องเพราะฟอร์มตอนสร้าง (`CreateMeetingForm.tsx`) มีช่อง "รายละเอียด" ด้วย ตอนแก้ไขก็ต้องแก้ได้เหมือนกัน (ไม่ใช่แก้ได้แค่ชื่ออย่างเดียว) — เพิ่ม RPC ใหม่ `update_meeting_details(p_user_id, p_id, p_version, p_title, p_timeline)` (แทนที่ `update_meeting_title` เวอร์ชันแรกที่รับแค่ชื่อ ซึ่งถูก `drop function` ทิ้งแล้ว) เช็ค `can_manage_tasks` + `version` แบบเดียวกับ RPC เขียนเชิงโครงสร้างอื่นๆ ทุกตัว, เพิ่ม `updateMeetingDetailsAction` ใน `board-actions.ts` และฟังก์ชัน `editMeetingDetails` ใน `AppShell.tsx` (pattern เดียวกับ `archiveMeeting`) — ปุ่ม "ย้ายไปที่เก็บ" ถูกซ่อนระหว่างแก้ไข (โชว์แค่ฟอร์ม ไม่โชว์ปุ่มอื่นปนกัน) บรรทัด timeline สีแดงด้านล่างก็ถูกซ่อนตอนแก้ไขเช่นกัน (ค่าเดิมย้ายไปอยู่ในช่อง input แทน)
  - **มือถือ: บังคับ "ปิดงาน X/Y" + ปุ่ม "+ เพิ่มเรื่อง" อยู่แถวบนขวาเสมอ ไม่ตกลงไปแถวที่ 2 (แก้ 2026-09-14):** เดิม container หัวการ์ดเป็น `flex flex-wrap` ทำให้จอแคบที่ชื่อ+ไอคอน 2 ปุ่มกินพื้นที่มากพอ จะดันป้ายสถานะ+ปุ่มเพิ่มเรื่องตกไปอยู่แถวใหม่ด้านล่างแทนที่จะอยู่มุมขวาบนเหมือน desktop — เปลี่ยนเป็น `flex-nowrap` ถาวรทุกขนาดจอ, ฝั่งชื่อ+ไอคอนใส่ `min-w-0` + `truncate` ให้ชื่อยอมตัดด้วย `...` แทนการดันแถวลง, ฝั่งขวา (ป้ายสถานะ+ปุ่ม) ใส่ `flex-none` กันไม่ให้ถูกบีบเล็กจนอ่านไม่ออก พร้อมลดขนาด padding/font ทั้งป้ายสถานะและปุ่มเฉพาะมือถือ (`text-[11px] px-2 py-1` มือถือ / `text-xs px-3 py-1.5` desktop สำหรับป้าย, คล้ายกันสำหรับปุ่ม) ให้พอดีอยู่แถวเดียวกับชื่อได้จริงบนจอ 375px
  - เนื้อหา: บรรทัด Timeline (ตัวอักษรสี `--pending`) + บรรทัด "เอกสารรอตรวจอยู่ที่" พร้อม badge ชื่อ:จำนวน (สี `--accent`) — **ไม่ต้องมีกล่องสรุปตัวเลข (KPI tiles) แยกต่างหาก** เพราะข้อมูลซ้ำกับบรรทัด workload อยู่แล้ว — **แสดงบรรทัดนี้เสมอเป็น default (2026-09-14)** ไม่ว่าครั้งที่ประชุมจะยังไม่มีวาระเลย หรือปิดงานครบทุกวาระแล้วก็ตาม (เดิมสองเคสนี้ซ่อนบรรทัดไปเลย/ขึ้นข้อความ "ปิดงานครบทุกวาระแล้ว" แทน — ตัดออกแล้ว ให้ badge ของแต่ละผู้ตรวจโชว์ 0 แทนแบบธรรมดา สม่ำเสมอทุกสถานะของครั้งที่ประชุม) ฟังก์ชันคำนวณเปลี่ยนชื่อจาก `meetingWorkload(meeting, inspectors)` เป็น **`taskWorkload(tasks, inspectors)`** ใน `board-derived.ts` (รับ `Task[]` ตรงๆ แทน `Meeting` ทั้งก้อน เพราะไม่เคยใช้ field อื่นของ meeting เลย) คืนแค่ `{ name, count }[]` (ตัด field `allClosed` ที่ไม่ใช้แล้วออก) — ใช้ร่วมกันทั้งการ์ดครั้งที่ประชุม (`MeetingCard.tsx`, ส่ง `meeting.tasks`) และแท็บ "อื่น ๆ" (`AppShell.tsx`, ส่ง `otherTasks` ตรงๆ — เพิ่มบรรทัด badge นี้ให้แท็บ "อื่น ๆ" ด้วยเมื่อ 2026-09-14 เดิมไม่มีบรรทัดนี้เลยทั้งที่มี dropdown "เลือกผู้ตรวจ" อยู่แล้ว ใช้ `inspectors` เต็มไม่กรอง Sub-tab เหมือนเดิม เพราะแท็บนี้ไม่มี Sub-tab ให้อิง)
  - รายการวาระ/เรื่อง อยู่ล่างสุดของการ์ด (ดูโครงสร้างที่ข้อ 4.5.1)
  - **กลุ่มผู้ตรวจตาม Sub-tab (2026-09-14):** badge "เอกสารรอตรวจอยู่ที่" และ dropdown "เลือกผู้ตรวจ" (ตอนกด "ส่งตรวจ") ไม่ได้โชว์ผู้ตรวจ (Inspector) ทุกคนเสมอไปแล้ว — กรองตาม **Sub-tab ที่กำลังเลือกอยู่** (Resume / ร่างรายงาน / Conduct, ชุดเดียวกันใช้ร่วมกันทั้ง Board และ Excom) โดยแต่ละผู้ตรวจกำหนด `reviewGroups: string[]` ได้จากหน้า `/admin/users` (checkbox "กลุ่มตรวจ" ในฟอร์มแก้ไขผู้ใช้งาน ค่าเป็น `resume`/`draft`/`conduct`/`other`) — **เว้นว่างไว้ = แสดงในทุกกลุ่ม (default)** — เพิ่มกลุ่มที่ 4 **`other`** (label "อื่น ๆ") เมื่อ 2026-09-14 สำหรับแท็บ "อื่น ๆ" โดยเฉพาะ (เดิมแท็บนี้โชว์ผู้ตรวจทุกคนตายตัว ไม่มีทางกรอง ผู้ใช้ทักว่าต้องเลือกโชว์ผู้ตรวจได้เหมือน Sub-tab อื่นด้วย) — **สำคัญ:** `other` เป็นแค่ค่าหนึ่งใน `ReviewGroup`/checkbox ของฟอร์มแก้ไขผู้ใช้งานเท่านั้น **ไม่ใช่ Sub-tab pill** ที่โชว์ใต้ Board/Excom (คนละแนวคิดกัน) — `AppShell.tsx` แยก `SUB_TABS` (ใช้ render ปุ่ม Resume/ร่างรายงาน/Conduct, กรอง `other` ออกด้วย `REVIEW_GROUPS.filter(g => g.key !== "other")`) ออกจาก `REVIEW_GROUPS` เต็มชุด (ใช้แค่ตอนกรอง inspector จริง) ห้ามเอา `REVIEW_GROUPS` ทั้งชุดไป map เป็นปุ่ม Sub-tab ตรงๆ เพราะจะมีปุ่ม "อื่น ๆ" โผล่ผิดที่ (ปนอยู่กับ Resume/ร่างรายงาน/Conduct ใต้ Board/Excom) — แท็บ "อื่น ๆ" กรอง inspector ของตัวเองด้วย `isInReviewGroup(person, "other")` แยกจาก `visibleInspectors` ของ Board/Excom (`otherInspectors` ใน `AppShell.tsx`) ใช้คุมทั้ง badge และ dropdown "เลือกผู้ตรวจ" ของแท็บนี้ ไม่ต้องตั้งค่าอะไรก็ใช้งานได้เหมือนเดิม ระบุกลุ่มเมื่อไหร่ถึงเริ่มจำกัด (ดู `isInReviewGroup()` ใน `board-types.ts`) — เก็บใน Supabase คอลัมน์ `users.review_groups` (text[], default `{}`) หมายเหตุ: **อัปเดต 2026-09-14 — ทั้ง Tab "Board/Excom" และ Sub-tab "Resume/ร่างรายงาน/Conduct" ผูกกับครั้งที่ประชุมจริงแล้วทั้งคู่** (ดู `Meeting.tab`/`Meeting.subTab` ข้อ 4.4) known gap เดิมที่ว่า "ครั้งที่ประชุมทุกใบยังโชว์เหมือนกันทุก Sub-tab" ปิดไปแล้ว — กลุ่มผู้ตรวจ (`reviewGroups`) ยังคงมีผลแยกกันคนละเรื่อง: ควบคุมว่า **ผู้ตรวจคนไหนโชว์ในบรรทัดสรุป/dropdown ของ Sub-tab ไหน** (ไม่เกี่ยวกับว่าครั้งที่ประชุมอยู่ Sub-tab ไหน)

#### 4.5.1 การ์ดวาระ/เรื่อง (Task Row) — ปรับ 2026-09-13 ตามคำขอผู้ใช้ (อัปเดตต่อ 2026-09-14: ปุ่มแก้ไข/ลบ + มือถือ + รอบแก้ layout ใหญ่ท้ายเซสชัน)
- แต่ละวาระ/เรื่องเป็น**การ์ดของตัวเอง** (`rounded-xl border border-border`, พื้น `--bg` ต่างจากพื้น `--surface` ของการ์ดครั้งที่ประชุมที่ครอบอยู่) เรียงต่อกันด้วยระยะห่างเล็กน้อย (`gap-2`) — ไม่ใช่แถวตารางที่ติดกันแบบมีแค่เส้นคั่นบางๆ อีกต่อไป เพื่อให้แต่ละวาระแยกจากกันชัดเจนโดยเฉพาะตอนขยายดูรายละเอียด
- **ยังคงมีหัวตาราง** (วาระ/เรื่อง | ผู้รับผิดชอบ | สถานะ | ด่วน) อยู่ด้านบนสุดเป็นแถบของตัวเอง (สไตล์เดียวกับการ์ดวาระ) — หัวตารางและทุกแถวใช้ **CSS Grid template เดียวกัน** (ค่าคงที่ `TASK_ROW_GRID` ใน `TaskRow.tsx`) เพื่อการันตีว่าคอลัมน์ตรงกันเป๊ะทุกขนาดจอ ไม่เลื่อนหนีกันตอนย่อ/ขยายหน้าจอ — **ค่าล่าสุด (2026-09-14 ท้ายเซสชัน, แทนที่ค่าก่อนหน้าทั้งหมด):** มือถือ `minmax(0,1fr)_2.75rem_4.75rem_2.25rem_1.75rem` gap-1 / desktop (`sm:`) `minmax(0,22rem)_minmax(4.5rem,1fr)_minmax(7rem,1fr)_3rem_4rem` gap-3 (ไม่มี `justify-between` แล้ว เพราะคอลัมน์ผู้รับผิดชอบ/สถานะเป็น `1fr` เองอยู่แล้ว ดูเหตุผลด้านล่าง) — ถ้าจะแก้อีกต้องแก้ที่ `TASK_ROW_GRID` จุดเดียวเหมือนเดิม
- **ชื่อวาระ:** เปลี่ยนจาก `truncate` (ตัดด้วย `...` บรรทัดเดียว) เป็น **ขึ้นบรรทัดใหม่แทน** (`break-words`, ไม่ตัด) และเป็น **ตัวหนา** (`font-bold`) ทั้ง 2 breakpoint (2026-09-14 — ผู้ใช้ทักว่าชื่อยาวถูกตัดจนอ่านไม่ครบ) แถวจะสูงขึ้นเองถ้าชื่อยาวเกิน 1 บรรทัด ยอมรับได้เพื่อแลกกับเห็นชื่อเต็มเสมอ ขนาดตัวอักษร: มือถือ `text-[12.5px]` (เดิม `11.5px`) / desktop `text-[13px]`
- **badge ผู้รับผิดชอบ:** เปลี่ยนจาก**วงกลม** (`rounded-full`, ขนาดผันตามความยาวชื่อ) เป็น **สี่เหลี่ยมมุมมน** (`rounded-md`) **ขนาดคงที่เท่ากันทุกแถวเสมอ** ไม่ว่าชื่อจะสั้นหรือยาว (`h-6 w-11` มือถือ / `h-7 w-16` desktop, `whitespace-nowrap`) — เหตุผล: ชื่อยาว (เช่น "พี่โหนก") ใส่ในวงกลมเดิมแล้วดูเบียด/ไม่สวย เปลี่ยนเป็นกล่องสี่เหลี่ยมทรงเดียวกันทุกแถวแก้ปัญหานี้และดูเป็นระเบียบกว่า ตัวอักษรใหญ่ขึ้นด้วย (มือถือ `text-[9px]`→`text-[10.5px]` / desktop `text-[11px]`→`text-[11.5px]`) — **เคยลองใส่วงแหวน progress (SVG ring, สีไล่จากทอง `--primary` ไปเขียว `--done` ตามเปอร์เซ็นต์ความคืบหน้า) ล้อมรอบ badge นี้แล้ว แต่ผู้ใช้ขอเอาออก** ("progress bar ด้านนอกไม่เอาละ") → ลบโค้ดวงแหวนทั้งหมดทิ้ง (`PROGRESS_RING_RADIUS`/`PROGRESS_RING_CIRCUMFERENCE`/`progressRingColor` ใน `TaskRow.tsx`) **แถวย่อไม่มีตัวบอกความคืบหน้าใดๆ อีกแล้ว** (% ความคืบหน้ายังอยู่แค่ในส่วนขยายเหมือนเดิม ดูข้อ Progress Tracker ด้านล่าง)
- **ป้ายสถานะ:** ตัวอักษรใหญ่ขึ้นเล็กน้อย (มือถือ `text-[8.5px]`→`text-[9.5px]`) ลด padding แนวนอนลง (`px-1` แทน `px-1.5`) ให้พอดีกับคอลัมน์ที่แคบลงโดยไม่ล้น — desktop ไม่เปลี่ยน (`text-[11.5px]`)
- **ผู้รับผิดชอบไม่บังคับกรอกอีกต่อไป (2026-09-15):** `Task.owner` เปลี่ยนเป็น optional ทั้ง type และ DB column (`tasks.owner` ตัด `NOT NULL`) — ตอนไม่มีผู้รับผิดชอบ badge ในแถวย่อจะเป็นกล่องเปล่า **ไม่แสดง "-" หรือข้อความอื่นใด** (แค่ไม่ render ข้อความ, กล่อง/ขนาดยังอยู่เหมือนเดิมเพื่อคง alignment) — dropdown ใน `CreateTaskForm.tsx`/ฟอร์มแก้ไขข้อมูลใน `TaskRow.tsx` เพิ่ม option แรกสุด `-- ยังไม่กำหนด --` (value ว่าง) เป็นค่า default ตอนสร้างใหม่ (เดิม default เป็นคนแรกใน list อัตโนมัติ) — เกิดจากฟีเจอร์ "ก็อปวาระจากต้นแบบ" (ดูข้อ 4.4) ที่ต้องสร้างวาระโดยไม่มีผู้รับผิดชอบมาให้ตั้งแต่แรก
- **ขอบการ์ดสีแดงเมื่อเป็นคิวของผู้ login อยู่ (2026-09-15):** การ์ดวาระเปลี่ยนจาก `border-border` เป็น `border-pending` (แดง) เมื่อ `taskNeedsMyAction(task, currentUserName)` เป็นจริง — ใช้เกณฑ์เดียวกับตัวเลขวงกลมแดงที่ Tab/Sub-tab ทุกประการ (`status==="blank" && owner===ตัวเอง` หรือ `status==="pending" && inspector===ตัวเอง` หรือ `status==="done" && owner===ตัวเอง`) ดึงมาจากฟังก์ชันเดียวกัน (แยก predicate ออกมาเป็น `taskNeedsMyAction()` ใน `board-derived.ts`, `myNotificationCount()` เดิมเปลี่ยนไปเรียกใช้ฟังก์ชันนี้แทนเพื่อไม่ให้ logic สองที่ไม่ตรงกัน) — ต้องส่ง prop ใหม่ `currentUserName` ไล่ทั้งสาย `AppShell` → `MeetingCard`/`TaskTable` (ทั้ง 2 จุดที่เรียก) → `TaskRow`
- แถวย่อ (ไม่ขยาย): ชื่อวาระชิดซ้าย, badge ผู้รับผิดชอบ (จัดกึ่งกลางคอลัมน์), ป้ายสถานะ (หรือ "—"), ช่อง "ด่วน" (ดูด้านล่าง), **ปุ่มลบเป็นไอคอนถังขยะสีแดง** (`TrashIcon`, ไม่ใช่ข้อความ "ลบ" อีกต่อไป) ชิดขวาสุด — กดที่แถวเพื่อขยาย/ย่อ — **ไอคอนแก้ไขไม่อยู่ในแถวหลักแล้ว** ย้ายไปอยู่ในส่วนขยายแทน (ดูด้านล่าง) เพื่อไม่ให้แถวหลักรก
- **มือถือ: ลดพื้นที่ว่างรอบคอลัมน์ที่ไม่จำเป็น (2026-09-14):** คอลัมน์ผู้รับผิดชอบ/สถานะ/ปุ่มลบเดิมกว้างเกินเนื้อหาจริงมาก (padding ภายในคอลัมน์เหลือเป็นพื้นที่ว่างเปล่า) → ลดความกว้างคอลัมน์เหล่านี้ลงให้พอดีเนื้อหา (ดูค่าใหม่ด้านบน) ลด `gap` จาก `1.5` เป็น `1`, ลด padding แถว (`px-3 py-2.5` → `px-2 py-2` เฉพาะมือถือ `sm:` กลับไปเป็นค่าเดิม) — พื้นที่ที่ได้คืนมาทั้งหมดถูกยกให้คอลัมน์ชื่อวาระ (ที่เป็น `1fr` อยู่แล้วบนมือถือ) ทำให้ตัวอักษรชื่อใหญ่ขึ้นได้โดยไม่ล้นจอ (ดูหัวข้อชื่อวาระด้านบน)
- **ปุ่มลบ:** กดแล้วต้องยืนยันก่อนเสมอ (native `confirm`) แต่ข้อความยืนยัน **ต้องไม่ใช้คำว่า "ถาวร"/"ย้อนกลับไม่ได้"** เพราะการกดลบจากหน้าหลักแค่ย้ายวาระไปถังขยะ ยังกู้คืนได้ (ข้อความจริง: `ลบ "<ชื่อวาระ>" ใช่หรือไม่? (จะถูกย้ายไปที่ถังขยะ)`) — คำเตือนแบบ "ถาวรใช่หรือไม่ ย้อนกลับไม่ได้" สงวนไว้เฉพาะปุ่ม "ลบถาวร" ในหน้าถังขยะเท่านั้น (ข้อ 4.6)
- **ช่อง "ด่วน"** รวมทั้งไฟด่วนและวันครบกำหนดไว้ด้วยกัน (วันครบกำหนดไม่ได้อยู่ในส่วนขยายอีกต่อไป):
  - ด่วน + มีวันครบกำหนด: แสดงไฟ 🔥 ด้านบน วันที่เล็กๆ ด้านล่าง
  - ด่วนอย่างเดียว (ไม่มีวันครบกำหนด): แสดงไฟ 🔥 เท่านั้น
  - มีแค่วันครบกำหนด (ไม่ได้ toggle ด่วน): แสดงวันที่เท่านั้น **ไม่โชว์ไฟ**
  - ไม่มีทั้งคู่: แสดง "—"
  - รูปแบบวันที่: `วว/ดด/ปป` แบบพ.ศ. 2 หลัก (เช่น `20/03/69`) ตัวเล็ก สี **`--pending`** (แดง ไม่ใช่ `--muted` เหมือนเดิม — ปรับ 2026-09-14 ตามคำขอผู้ใช้ให้เด่นขึ้น ใช้ทั้ง desktop และมือถือ)
- **คอลัมน์ "วาระ/เรื่อง" มีเพดานความกว้าง + คอลัมน์ผู้รับผิดชอบ/สถานะยืดหยุ่นแทน (desktop, รื้อ 2026-09-14 ท้ายเซสชัน — แทนที่แนวทางเดิมทั้งหมด):**
  - **เดิม:** คอลัมน์แรก (ชื่อวาระ) เพดานตายตัว `26rem`, คอลัมน์อื่นๆ ก็เป็นความกว้างคงที่ล้วน แล้วใช้ `sm:justify-between` ให้ CSS Grid กระจายพื้นที่ว่างส่วนเกิน — ปัญหาที่พบภายหลัง: บนจอกว้างมาก พื้นที่ว่างส่วนเกินยัง"มองเห็นเป็นช่องว่างเปล่า"อยู่ดี (แค่ย้ายตำแหน่งจากท้ายแถวไปกระจายเป็นช่องว่างระหว่างคอลัมน์แทน) เพราะคอลัมน์ผู้รับผิดชอบ/สถานะ**ตัวกล่อง badge/pill ข้างในไม่ได้ขยายตาม** มีแต่ gap รอบๆ ที่ใหญ่ขึ้น ดูเหมือนคอลัมน์เหล่านี้ลอยอยู่กลางที่ว่างเปล่า
  - **แก้ใหม่:** ลดเพดานคอลัมน์ชื่อวาระจาก `26rem` เหลือ `22rem` (พอสำหรับชื่อ 1-2 บรรทัดหลังเปลี่ยนเป็น `break-words` แล้ว ไม่จำเป็นต้องกว้างเท่าเดิมที่ยังใช้ `truncate` บรรทัดเดียว) แล้วเปลี่ยนคอลัมน์ **ผู้รับผิดชอบ** และ **สถานะ** จากความกว้างคงที่เป็น `minmax(4.5rem,1fr)` / `minmax(7rem,1fr)` (ยืดได้จริง กิน "เนื้อที่" ของ cell ตัวเอง ไม่ใช่แค่ gap รอบๆ) — คอลัมน์ ด่วน/ปุ่มลบ ยังคงที่เหมือนเดิม (เนื้อหาเล็กมาก ไม่จำเป็นต้องยืด) — ผลคือพื้นที่ส่วนเกินไปอยู่ *ภายใน* กล่อง cell ของผู้รับผิดชอบ/สถานะเอง (เนื้อหายังจัดกึ่งกลางเหมือนเดิม) ทำให้แถวดูสมดุล/กระจายเต็มพื้นที่จริงๆ ไม่ใช่แค่ดันช่องว่างไปไว้ที่อื่น — ไม่ต้องมี `sm:justify-between` อีกต่อไปเพราะ `1fr` จัดการพื้นที่เหลือให้เองอยู่แล้ว
  - ทดสอบยืนยันด้วย `getBoundingClientRect()` ที่จอ 1920px: คอลัมน์ชื่อวาระนิ่งที่ 352px (เพดาน 22rem), คอลัมน์ผู้รับผิดชอบ+สถานะขยายเท่ากันที่ ~207px ต่อคอลัมน์ (แบ่งพื้นที่เหลือเท่าๆ กันตาม `1fr`) — มือถือไม่กระทบ (breakpoint `sm:` เท่านั้น) — ถ้าจะแก้เพดาน/สัดส่วนนี้อีกในอนาคต แก้ที่ `TASK_ROW_GRID` จุดเดียวเหมือนเดิม
- ส่วนขยาย (กดที่แถวแล้วเห็นเพิ่ม) อยู่ภายในการ์ดเดียวกัน (มีเส้นคั่น `border-t` ด้านบน ไม่ใช่กล่องแยก):
  - **แถวบนสุดของส่วนขยาย** คือปุ่ม **"แก้ไขข้อมูล"** (ไอคอนดินสอเล็ก `h-3 w-3` + ข้อความ `text-[10.5px]`) ชิดขวา สี `text-muted/70` (จางกว่าปกติ ไม่ให้เด่นเกินเนื้อหา) — กดแล้วเปิดฟอร์มแก้ไขแทนที่ทั้งการ์ดเหมือนเดิม (ย้ายมาจากแถวหลักตามคำขอผู้ใช้ "จะได้ไม่กินพื้นที่ข้างนอก")
  - **ECM** และ **หมายเหตุ**: แสดงคนละบรรทัด แต่ละบรรทัดมีไอคอนปุ่มปักหมุด 📌 นำหน้า (content marker เหมือน 🔥 ไม่ใช่ action icon จึงใช้ emoji ได้ตามข้อ 5) — **ถ้าไม่มีข้อมูลในช่องไหน ไม่ต้องแสดงบรรทัดนั้นเลย** (ไม่ใช่โชว์ "—" ค้างไว้)
    - **พื้นหลังเปลี่ยนเป็นเหลืองอ่อนเฉพาะโหมดสว่าง (2026-09-15):** เดิมใช้ `bg-surface` (กลืนกับพื้นการ์ดรอบๆ มองไม่ค่อยเห็นว่าเป็นกล่องแยก) ผู้ใช้ขอให้เด่นขึ้น (เสนอ "แดงอ่อนๆ หรือเหลืองอ่อนๆ หรือชมพูอ่อนๆ") **เฉพาะโหมดสว่าง** ("ใน Dark mode ดูจะปรับแล้วแย่") — เลือกเหลืองอ่อนโทนสติกเกอร์โน้ต (`--note-soft: #fbeec6`) เข้ากับไอคอน 📌 และไม่ชนความหมายกับสีสถานะอื่นที่มีอยู่แล้ว (แดง=pending/ด่วน, เขียว=done, ฟ้า=accent) — โหมดมืดตั้ง `--note-soft` เท่ากับ `--surface` เป๊ะ (ไม่มีการเปลี่ยนแปลงที่มองเห็นได้จากเดิมเลยตามที่ผู้ใช้ขอ) เปลี่ยนแค่ class จาก `bg-surface` เป็น `bg-note-soft` ใน `TaskRow.tsx`
  - **ปุ่ม action (ส่งตรวจ/ตรวจแล้ว/ปิดงาน) — รื้อใหม่เป็น 3-pill layout (2026-09-14):** เดิมโชว์แค่ปุ่มเดียวตามสถานะปัจจุบัน (conditional render) → เปลี่ยนเป็นแสดง **ทั้ง 3 ปุ่มพร้อมกันเสมอ** เป็นแถบ segmented เต็มความกว้าง (`flex w-full gap-2`, แต่ละปุ่ม `flex-1 rounded-full`) สีตามโทนสถานะที่ปุ่มนั้นนำไปสู่ (ส่งตรวจ = `bg-pending-soft text-pending`, ตรวจแล้ว = `bg-done-soft text-done`, ปิดงาน = `bg-status-closed-soft text-status-closed`) — ปุ่มที่ไม่ valid ตามสถานะปัจจุบันยังคงแสดงสีเดิมแต่ลด `opacity-35` + `cursor-not-allowed` + `disabled` (ไม่ซ่อนไปเลย เพื่อให้ layout นิ่งไม่ขยับ ผู้ใช้เห็น "เส้นทางทั้งหมด" ตลอดเวลา) — Logic ความ valid: blank/done/pending → ส่งตรวจ, pending → ตรวจแล้ว (ดูรายละเอียดเพิ่มที่หัวข้อ "กดข้ามขั้นตอนได้" ด้านล่าง) สถานะ `closed` ไม่โชว์แถบ 3 ปุ่มนี้เลย ใช้ปุ่ม **"ย้อนสถานะเพื่อดำเนินต่อ"** แทน (ดูหัวข้อ "ปุ่มย้อนสถานะ" ด้านล่าง)
    - **ปรับ 2026-09-14 (รอบต่อมา) ตามคำขอผู้ใช้:** ปุ่ม **"ปิดงาน" ไม่ล็อกตามสถานะอีกต่อไป** — เอา `canClose`/`disabled` ออกทั้งหมด กดปิดงานได้ทันทีไม่ว่าสถานะปัจจุบันจะเป็นอะไร (ต่างจาก "ส่งตรวจ"/"ตรวจแล้ว" ที่ยังคง valid-check ตามเดิม) — ปุ่มทั้ง 3 ช่องยังลดขนาดลงจาก `px-4 py-2.5 text-xs` เป็น `px-3 py-2 text-[11px]` ตามคำขอ "เล็กลงมาหน่อย"
  - **กด "ส่งตรวจ"/"ตรวจแล้ว" เปิด Modal ยืนยันตรงกลางจอแทนฟอร์ม inline เดิม (2026-09-14):** สร้าง component ใหม่ **`src/components/Modal.tsx`** (generic, ใช้ร่วมกันได้ทุกจุดในอนาคต — backdrop `fixed inset-0 bg-black/40` กดพื้นหลังเพื่อปิด, การ์ด `bg-surface rounded-2xl` กึ่งกลางจอ, prop `title`/`onClose`/`children`)
    - Modal "ส่งต่องาน (ส่งตรวจ)": dropdown "มอบหมายให้" (placeholder `-- เลือกผู้ตรวจ --` บังคับเลือกก่อนถึงกดยืนยันได้ — ต่างจากเดิมที่ default เป็นคนแรกในลิสต์อัตโนมัติ) + textarea "บันทึกช่วยจำ / Message" (placeholder "เพิ่มรายละเอียดงาน...") + ปุ่ม "ยกเลิก"/"ยืนยันส่งตรวจ"
    - Modal "ส่งต่องาน (ตรวจแล้ว)": ช่อง "ส่งคืนผู้รับผิดชอบ" แสดงชื่อ `task.owner` แบบ read-only (ไม่ใช่ dropdown ให้เลือก เพราะงานต้องกลับไปหาเจ้าของงานเดิมเสมอ) + textarea "บันทึกช่วยจำ / Message" เดียวกัน + ปุ่ม "ยกเลิก"/"ยืนยัน (ตรวจแล้ว)"
    - ปุ่ม "ปิดงาน" **ไม่มี** Modal (ยังเป็น action ทันทีเหมือนเดิม ไม่ได้ขอให้เพิ่ม)
  - **หลักการ "ยืดหยุ่น" แทนการจำกัดสิทธิ์ตาม Role (2026-09-14 — ตัดสินใจแล้ว ไม่ใช่ของค้างอีกต่อไป):** ผู้ใช้ยืนยันชัดเจนว่า **ไม่ต้องการ** ล็อกปุ่ม "ส่งตรวจ"/"ตรวจแล้ว"/"ปิดงาน" ตาม Role ของผู้ login (ที่ `PLAN.md` เดิมเคยระบุไว้ว่า "ตรวจแล้ว" ควรเป็นสิทธิ์ Inspector เท่านั้น, "ส่งตรวจ"/"ปิดงาน" ควรเป็นสิทธิ์ Owner เท่านั้น) — **ใครที่ login อยู่ก็กดปุ่มไหนก็ได้ทั้งหมด** เพื่อความยืดหยุ่นในการทำงานจริง (เผื่อกรณีมีคนช่วยกรอกแทนกัน) แต่ระบบยังคง**บันทึก log ให้ถูกต้องตามเจ้าของงานจริง** โดยอ่านจาก field `task.owner`/`task.inspector` ที่ผูกกับวาระนั้นอยู่แล้ว ไม่ใช่จากตัวตนของคนที่กด — เช่น "ส่งตรวจ" จะลง log ว่า `{task.owner} (ผู้รับผิดชอบ) ส่งตรวจ {inspector ที่เลือก}` เสมอ ไม่ว่าใครจะเป็นคนกดจริง
  - **กดข้ามขั้นตอนได้ (Skip-step, 2026-09-14):** ปุ่มทั้ง 3 ไม่ล็อกตามสถานะเป๊ะๆ อีกต่อไป — ตอนสถานะ `pending` (ส่งตรวจให้ใครอยู่) เปิดให้กด **"ส่งตรวจ" ซ้ำ (ส่งต่อให้คนใหม่/คนเดิม) หรือ "ปิดงาน" ตรงๆ ได้เลย** โดยไม่ต้องรอผู้ตรวจกด "ตรวจแล้ว" ก่อน (`canSend`/`canClose` เพิ่ม `task.status === "pending"` เข้าไปในเงื่อนไข) — เมื่อข้ามขั้นแบบนี้ ระบบ**เขียน log แทรกให้อัตโนมัติ 1 รายการก่อนเสมอ**: entry `reviewed` สังเคราะห์ขึ้น (attribute ให้ผู้ตรวจที่ถืองานอยู่ ณ ตอนนั้น คือ `task.inspector` เดิม เหมือนกับว่าเขากด "ตรวจแล้ว" เอง) แล้วค่อยตามด้วย entry ของ action จริงที่กด (`sent` หรือ `closed`) — ทำผ่านฟังก์ชันกลาง `withSkippedReviewEntry(t, history)` ใน `AppShell.tsx` ใช้ร่วมกันทั้ง `sendForReview`/`closeTask`/`sendOtherForReview`/`closeOtherTask` (เช็คแค่ `t.status === "pending"` ถึงแทรก ถ้าไม่ใช่ pending ก็ไม่แทรกอะไรเหมือนเดิม) — สถานะ `blank`→`ตรวจแล้ว` โดยตรง**ยังไม่เปิด** (ไม่มี `task.inspector` ให้ attribute log จึงไม่สมเหตุสมผล)
  - **`HistoryEntry` เพิ่ม field ใหม่ (2026-09-14):** `owner?: string` (เก็บชื่อ Owner ตอนเกิด action "sent"/"closed"/"reopened" สำหรับ log ตามข้อข้างบน) และ `message?: string` (บันทึกช่วยจำจาก Modal — field นี้เตรียมไว้สำหรับใช้เป็นข้อความส่ง LINE Notification ในอนาคตด้วย ตามที่ผู้ใช้ระบุ) — ฟังก์ชันสร้าง label ของ log เปลี่ยนจาก `HISTORY_LABEL: Record<HistoryAction, (inspector?) => string>` เป็น `historyLabel(entry: HistoryEntry): string` (รับ entry เต็มก้อนแทนแค่ inspector เพราะ "sent" ต้องใช้ทั้ง owner และ inspector ประกอบกัน)
  - **ปุ่มย้อนสถานะ (Reopen, 2026-09-14):** สถานะ `closed` เปลี่ยนจากข้อความเฉยๆ "งานนี้ปิดแล้ว" เป็นปุ่มเต็มความกว้าง **"↩ ย้อนสถานะเพื่อดำเนินต่อ"** (ไอคอน `RestoreIcon` ตัวเดียวกับปุ่มกู้คืนในถังขยะ) กดแล้วเรียก `onReopenTask(taskId)` ไม่มี confirm dialog เพราะไม่ใช่ action ทำลายข้อมูลตามเจตนาเดิม
    - **เปลี่ยนพฤติกรรมทั้งหมด (2026-09-15 — แทนที่ของเดิมด้านบน):** ผู้ใช้ขอว่า reopen **ไม่ต้องลง log ใหม่ว่า "ย้อนสถานะ" อีกต่อไป ให้ลบ record "ปิดงาน" ตัวล่าสุดออกจาก `history` ใน DB จริงๆ แทน** แล้วคำนวณสถานะใหม่จาก entry ที่กลายเป็นตัวล่าสุดหลังลบ — เดิมเคยเข้าใจว่า "ก่อน closed ต้องเป็น reviewed เสมอจึง hardcode done ได้" แต่พบ edge case จริงจากข้อมูลทดสอบเก่า (entry ก่อนหน้าเป็น legacy `reopened` จากก่อนมีการเปลี่ยนนี้) ทำให้ mapping แบบ hardcode พังเหลือ `blank` ผิดพลาด → แก้เป็น mapping ที่ทนทานกว่า: entry ล่าสุดหลังลบเป็น `reviewed`/`reopened`/`closed` (legacy ใดๆ ที่หมายถึง "เคยถึง done แล้ว") → status `done`, เป็น `sent` → `pending`, ไม่มี entry เหลือเลย → `blank` — โค้ดอยู่ใน `reopenTask()` (`AppShell.tsx`) ไม่ใช้ `runWorkflow()`/`buildEntry` pattern แบบ action อื่นๆ อีกต่อไปเพราะต้อง **ตัด** entry ไม่ใช่ **เพิ่ม** — ผลข้างเคียงที่ตั้งใจ: Timeline หลัง reopen จะไม่เห็นร่องรอยว่าเคยปิดงานมาก่อนเลย (ประวัติ "ปิดงาน" หายไปจริง ไม่ใช่แค่ถูกซ่อน) — เป็นการตัดสินใจของผู้ใช้เอง รับทราบแล้วว่าต่างจากหลักการทั่วไปเรื่องเก็บ History ให้ครบใน `AGENTS.md`
    - **ปรับสไตล์ 2026-09-14 (รอบต่อมา) ตามคำขอผู้ใช้ "เล็กลงนิดนึงและเปลี่ยนเป็นโทนสีเทา":** เปลี่ยนจาก `bg-primary text-on-primary` (ทอง ทึบ ขนาด `px-4 py-3 text-sm`) เป็น `bg-closed-soft text-closed` (โทนเทา/เบจ ตามชุดสี `--closed`/`--closed-soft` เดิมของแอป) ขนาดเล็กลงเหลือ `px-3.5 py-2 text-xs` — เดิมเคยลองแบบโปร่งแสง `bg-primary/15` แล้วอ่านไม่ออกในโหมดมืด (ดูบันทึกเดิม) แต่ `bg-closed-soft`/`text-closed` เป็นคู่สีทึบที่มีอยู่แล้วในระบบ (ใช้กับ badge สถานะ "ปิดงาน" ด้วย) จึงผ่านคอนทราสต์ทั้ง 2 โหมดโดยไม่ต้องปรับเพิ่ม
  - **Timeline ประวัติการดำเนินการ — ออกแบบใหม่ทั้งหมด (2026-09-14):**
    - หัวข้อเปลี่ยนจาก "ประวัติการดำเนินการ" เป็น **"Timeline"**
    - **เรียงจากล่างขึ้นบน** คือรายการล่าสุดอยู่**บนสุด** เก่าสุดอยู่ล่างสุด (`[...task.history].reverse()` ตอน render — ตัว state/array ที่เก็บจริงยัง push ต่อท้ายตามลำดับเวลาปกติเหมือนเดิม แค่กลับด้านตอนแสดงผลเท่านั้น)
    - แต่ละ entry เป็นการ์ดของตัวเอง (`bg-closed-soft/50 rounded-lg`) เชื่อมกันด้วยเส้นตรง + จุดกลม (`bg-accent`) ทางซ้าย ให้ความรู้สึกเป็น timeline จริงๆ
    - ข้อความหลักเปลี่ยนรูปแบบเป็น **"{ผู้กระทำ} > {การกระทำ}"** ตัวหนา (เช่น `บอย (ผู้รับผิดชอบ) > ส่งตรวจ พี่จา`, `พี่จา > ตรวจแล้ว`, `ฟิวส์ > ปิดงาน`, `ฟิวส์ > ย้อนสถานะ`) — เดิมเป็นประโยคภาษาธรรมชาติไม่มี ">"
    - ถ้ามี `message` แสดงเป็นกล่องแยกสีขาว/`--surface` มีกรอบ ใส่เครื่องหมายคำพูดครอบ (`"..."`) แทนที่จะเป็นแค่ข้อความ italic ลอยๆ แบบเดิม
    - **โชว์กล่อง Timeline เสมอเป็น default แม้ยังไม่มี history เลย (เพิ่ม 2026-09-14):** เดิมซ่อนหัวข้อ+กล่อง Timeline ไปทั้งหมดถ้า `task.history` ว่าง/ไม่มี — ผู้ใช้ขอให้คงหัวข้อ "Timeline" ไว้เสมอ พร้อมกล่อง placeholder ข้อความ "ยังไม่มีการดำเนินการ" (`border border-dashed border-border`, ตัวอักษร `text-muted text-xs` จางกว่าปกติ จัดกึ่งกลาง) แทนการซ่อนไปเลย — สม่ำเสมอกับหลักการเดียวกับบรรทัด "เอกสารรอตรวจอยู่ที่" ที่โชว์ 0 เป็น default ไม่ซ่อนบรรทัด
    - เวลาเปลี่ยนจาก `toLocaleString` แบบเดือนย่อภาษาไทย (`14 ก.ย. 2569 08:44`) เป็น**ตัวเลขล้วน + วินาที**: `formatLogDateTime()` ใหม่ → `14/9/2569 08:44:15` จัดชิดขวาใต้การ์ด
    - **เพิ่มหมายเหตุสะสมให้ entry ล่าสุดได้เรื่อยๆ ไม่จำกัดจำนวนครั้ง (2026-09-15, แก้ 2 รอบในวันเดียวกัน):** ถ้า entry เป็นตัวล่าสุดสุด (แสดงอยู่บนสุดของ Timeline เพราะ reverse แล้ว, index 0) จะมีปุ่มแคปซูล **"+ เพิ่มหมายเหตุ"** โผล่ชิดขวาของบรรทัด action เสมอ (ข้อความปุ่มคงที่ ไม่สลับเป็น "แก้ไข") กดแล้วเปิด textarea inline ว่างเปล่าเสมอ (progressive disclosure แบบเดียวกับ "+ เพิ่มขั้นตอน") มีปุ่ม "บันทึก"/"ยกเลิก" — **แต่ละครั้งที่บันทึกคือการเพิ่มหมายเหตุ "รายการใหม่" เข้าไปสะสม ไม่ใช่การเขียนทับของเดิม** ผู้ใช้ย้ำชัดว่าต้องเป็น "เพิ่ม" ไม่ใช่ "แก้ไข" เพราะในอนาคตแต่ละหมายเหตุจะสัมพันธ์กับการยิง Notification 1 ครั้ง (ช่องทางใดก็ได้ เช่น LINE Bot) จึงต้องเป็นประวัติสะสมที่นับจำนวนครั้งได้ ไม่ใช่ field เดียวที่ถูกแทนที่ (เวอร์ชันแรกสุดทำเป็น field เดียวเขียนทับ, เวอร์ชันที่ 2 แก้เป็น prefill ให้แก้ไขได้ ก็ยังผิดหลักการเดิมอยู่ — เวอร์ชันนี้คือของจริงที่ใช้)
      - **Data model:** เพิ่ม type ใหม่ `HistoryNote = { id, text, at }` และ field ใหม่ `HistoryEntry.notes?: HistoryNote[]` ใน `board-types.ts` (แยกจาก `message?: string` เดิมที่ยังคงไว้เหมือนเดิม — `message` คือโน้ตตอนสร้าง entry ผ่าน Modal "ส่งตรวจ"/"ตรวจแล้ว" ครั้งเดียวตอนนั้น ส่วน `notes[]` คือหมายเหตุที่ทยอยเพิ่มทีหลังได้เรื่อยๆ ระหว่างที่ entry นั้นยังเป็นตัวล่าสุด คนละจุดประสงค์กัน ไม่ได้รวมเป็นอันเดียวกัน) แสดงผลเรียงต่อกันใต้บรรทัด action: `message` (ถ้ามี) ก่อน ตามด้วย `notes[]` ทีละรายการ แต่ละรายการมีเวลาประทับของตัวเอง (`formatLogDateTime`)
      - **ฟังก์ชัน:** เปลี่ยนชื่อจาก `addHistoryMessage`/`onAddHistoryMessage` เป็น `addHistoryNote`/`onAddHistoryNote` ให้ตรงความหมาย — ข้างในสร้าง `{id: generateId(), text, at: new Date().toISOString()}` แล้ว **push ต่อท้าย `entry.notes` เดิม** (`[...(h.notes ?? []), note]`) ไม่ใช่ set ทับ ส่งทั้ง `history` array กลับผ่าน RPC เดิม `update_task_workflow` เหมือนเดิม (ไม่ต้องมี RPC ใหม่)
      - ปุ่มพื้นหลังเปลี่ยนจาก `bg-primary/20` (โปร่งแสง อ่านไม่ออกในโหมดมืด — เจอปัญหาเดียวกับ `--primary-soft` ก่อนหน้า) เป็น `bg-primary` ทึบ + `text-on-primary` ตรงตามคู่สีมาตรฐานที่ใช้ทั้งแอป
  - **Progress Tracker / Checklist (ใหม่ 2026-09-14):** เพิ่ม section "ความคืบหน้า" อยู่ระหว่างแถบปุ่ม action (หรือปุ่มย้อนสถานะ) กับ Timeline — **ในการ์ดโชว์แค่แถบ progress + ตัวเลข % เท่านั้น ห้ามโชว์รายการ checklist หรือปุ่มเพิ่มขั้นตอนในการ์ด** (ผู้ใช้ระบุชัดว่าไม่ต้องการให้รกในการ์ด) ส่วนการจัดการขั้นตอนทั้งหมดซ่อนอยู่ใน**ฟอร์ม "แก้ไขข้อมูล"**:
    - **Default checklist สร้างอัตโนมัติจากรายชื่อผู้ตรวจ** (`inspectors` prop ที่กรองตาม Sub-tab/กลุ่มตรวจจากหน้า `/admin/users` อยู่แล้ว) — ผู้ตรวจ 1 คนได้ 2 ขั้น (`ส่งตรวจ{ชื่อ}` + `{ชื่อ}ตรวจแล้ว`) ต่อท้ายด้วยขั้น `ปิดงาน` เสมอเป็นขั้นสุดท้าย → ผู้ตรวจ 3 คน = 7 ขั้น ตามตัวอย่างที่ผู้ใช้ระบุ (ถ้ามีชื่อผู้ตรวจใน history ที่ไม่อยู่ในลิสต์ปัจจุบัน เช่นถูกย้ายกลุ่มไปแล้ว ระบบ append คู่ขั้นของคนนั้นต่อท้ายให้ด้วย เพื่อไม่ให้ progress ค้าง)
    - **Auto-progress:** ขั้น default ติ๊กเองจาก `task.history` ไม่ได้เก็บ state ซ้ำ — `ส่งตรวจ{X}` ติ๊กเมื่อมี entry `sent` ที่ `inspector === X`, `{X}ตรวจแล้ว` ติ๊กเมื่อมี entry `reviewed` ของคนนั้น (ครอบคลุม entry สังเคราะห์จาก "กดข้ามขั้นตอน" ให้อัตโนมัติด้วย) และ `ปิดงาน` ติ๊กเมื่อ `task.status === "closed"` — ผลพลอยได้คือกด "ย้อนสถานะ" แล้ว % ลดกลับเองโดยไม่ต้องเขียน logic แยก
    - **ปิดงานแล้ว = 100% เสมอ** (ผู้ใช้ยืนยัน 2026-09-14): ถ้า `status === "closed"` ให้ทุกขั้นนับเป็น done ทั้งหมด ไม่สนว่าส่งตรวจครบทุกคนหรือติ๊กขั้นที่เพิ่มเองครบหรือยัง (เหตุผล: งานปิดแล้วคือเสร็จ ไม่ควรเหลือ % ค้าง เช่นปิดงานตั้งแต่ส่งตรวจคนเดียวก็ต้องเป็น 100% ไม่ใช่ 50%)
    - **ขั้นที่เพิ่มเอง (custom):** เก็บใน `Task.checklist?: ChecklistItem[]` (`{ id, label, done }` ใน `board-types.ts`) เรียงแทรก**ก่อนขั้น "ปิดงาน"** — ทั้งการเพิ่ม/ติ๊ก/ลบ ทำได้เฉพาะในฟอร์มแก้ไขข้อมูลเท่านั้น (ผู้ใช้เลือกข้อนี้เอง ไม่ให้ติ๊กจากการ์ด เพราะอยากให้การ์ดเหลือแค่แถบ progress)
    - **ฟอร์มแก้ไขข้อมูล** มี section "ขั้นตอนที่ track (ความคืบหน้า)" ท้ายฟอร์ม: แถว default แสดงแบบ read-only (`opacity-60` + ป้าย "อัตโนมัติ" ท้ายแถว กดติ๊กเองไม่ได้) ส่วนแถว custom ติ๊ก/ลบได้ + ปุ่ม "+ เพิ่มขั้นตอน" แบบ Progressive Disclosure (input + ปุ่ม "เพิ่ม"/"ยกเลิก", กด Enter ในช่องก็เพิ่มได้) — **checklist ที่แก้ไปจะ commit ตอนกด "บันทึก" เท่านั้น** (เก็บใน local state ของฟอร์มก่อน กด "ยกเลิก" = ทิ้งทั้งหมด) ให้ semantics ตรงกับฟิลด์อื่นในฟอร์มเดียวกัน
    - `board-derived.ts`: `taskSteps(task, inspectors): ProgressStep[]` (`{ key, label, done, auto }`) เป็นตัวประกอบขั้นทั้งหมดตามกฎข้างบน และ `taskProgress(task, inspectors)` = % ปัดเศษจาก `taskSteps` — **ทั้ง 2 ฟังก์ชันต้องรับ `inspectors` ด้วยเสมอ** เพราะ default checklist ผูกกับลิสต์ผู้ตรวจ
    - **ลบขั้น default (ผู้ตรวจ) ออกจาก Track ได้ทีละขั้น (เพิ่ม 2026-09-15):** ผู้ใช้ขอให้ลบขั้นอัตโนมัติออกได้ (เผื่อข้ามขั้นตอน/ผู้ตรวจลางาน) **แต่ต้องไม่กระทบ dropdown เลือกผู้ตรวจหรือลำดับ/รายชื่อผู้ตรวจใน `/admin/users` เลย** — เพิ่ม field ใหม่ `Task.excludedAutoSteps: string[]` (เก็บ step `key` เช่น `sent:พี่โอ๋`, `reviewed:พี่โอ๋` ที่ถูกลบออกจากการ track เฉพาะวาระนั้น) แถวขั้น default (ทั้งคู่ ส่งตรวจ/ตรวจแล้ว ต่อผู้ตรวจ 1 คน) มีปุ่ม **"ลบ"** ต่อท้ายในฟอร์มแก้ไขข้อมูล (สไตล์เดียวกับปุ่มลบของขั้น custom ที่มีอยู่แล้ว) กดแล้วหายจากลิสต์ทันที (แก้ใน local state ของฟอร์มก่อน commit ตอนกด "บันทึก" เหมือน checklist custom — กด "ยกเลิก" ทิ้งการเปลี่ยนแปลงทั้งหมดเหมือนเดิม) — **ขั้น "ปิดงาน" ลบไม่ได้เสมอ** (ปุ่มลบไม่โผล่ให้ขั้นนี้) — ลบแล้ว % ความคืบหน้าคำนวณจากขั้นที่เหลือเท่านั้น (ยอดรวมขั้นน้อยลง เปอร์เซ็นต์ต่อขั้นที่เสร็จแล้วจึงสูงขึ้น) — `taskSteps()` กรอง key ที่อยู่ใน `excludedAutoSteps` ออกก่อนคืนค่า ฟอร์มแก้ไขข้อมูลเรียก `taskSteps({...task, excludedAutoSteps: []}, inspectors)` แยกต่างหากเพื่อได้ "ลิสต์เต็มไม่กรอง" มาแสดงเทียบกับ draft state ที่กำลังแก้อยู่ — DB เพิ่มคอลัมน์ `tasks.excluded_auto_steps text[]` (migration `add_task_excluded_auto_steps`) RPC `update_task_details` เพิ่ม param ใหม่ `p_excluded_auto_steps` (default null → coalesce เป็น `{}`) และ RPC `list_active_tasks`/`list_deleted_tasks` ต้องแก้ (drop+create ใหม่เพราะเปลี่ยน return type) ให้ส่งคอลัมน์นี้กลับมาด้วย
    - Prop `onUpdateChecklist(taskId, checklist)` ส่ง array ทั้งก้อนขึ้นไปทีเดียว ผ่านสายเดิม `AppShell` → `MeetingCard` → `TaskTable` → `TaskRow` (และคู่ขนานฝั่งแท็บ "อื่น ๆ") — `AppShell.tsx` เก็บ logic แค่เซ็ต `checklist` field ตรงๆ ไม่มี logic พิเศษ

### 4.6 หน้าถังขยะ (Trash) — แยกหน้าจากหน้าหลัก
- เข้าถึงผ่านไอคอนถังขยะที่ Header เท่านั้น มีปุ่ม "← กลับหน้าหลัก" ด้านบน
- แบ่ง 2 ส่วน: **"ครั้งที่ประชุมที่เก็บไว้"** และ **"วาระ/เรื่องที่ลบ"**
- ครั้งที่ประชุมในถังขยะ: แสดงแบบย่อ (แค่ชื่อ + ป้ายสถานะ) มีปุ่ม **กู้คืน** และ **ลบถาวร** (มี confirm พร้อม warning "ถาวร...ย้อนกลับไม่ได้" ก่อนลบจริง)
- วาระ/เรื่องในถังขยะ: แสดงเป็นแถวสั้นๆ (ชื่อวาระ + "จาก [ชื่อครั้งที่ประชุม]") มีปุ่ม **กู้คืน** และ **ลบถาวร** เช่นกัน (confirm+warning แบบเดียวกัน)
- **กู้คืนแล้วต้องกลับไปอยู่ตำแหน่งเดิม:** ทั้งครั้งที่ประชุมและวาระ/เรื่อง เวลากู้คืนจากถังขยะต้องกลับไปอยู่ตำแหน่งเดิมในรายการ (ไม่ใช่ต่อท้ายรายการเสมอ) — **อัปเดต (DB Persistence Migration):** เดิมทำด้วยการเก็บ `index` ไว้ในตัว wrapper type (`TrashedTask.index`/`ArchivedMeeting.index`) แล้ว splice คืนตอน restore เพราะ mock state ไม่มีที่เก็บลำดับถาวร — ตอนนี้แถวใน DB มีคอลัมน์ `sort_order` ติดตัวอยู่แล้วเสมอ (ไม่ถูกลบ/reset ตอนย้ายเข้าถังขยะ แค่ตั้ง `is_deleted`/`is_archived` เป็น true) กู้คืน (`restore_task`/`restore_meeting`) แค่ตั้ง flag คืนเป็น false เฉยๆ ตำแหน่งกลับที่เดิมเองตาม `sort_order` ที่ค้างอยู่ — ไม่ต้องมี wrapper type คอย track index อีกต่อไป `TrashView.tsx` รับ `Meeting[]`/`Task[]` ตรงๆ
- หลักการ: **ไม่มีการลบทันทีจากหน้าหลัก** ไม่ว่าจะเป็นครั้งที่ประชุมหรือวาระ/เรื่อง ต้องผ่านถังขยะก่อนเสมอ — ปุ่มลบที่หน้าหลักจึงถาม confirm แบบธรรมดา (ไม่ใช้คำว่า "ถาวร") ส่วนคำเตือนแบบถาวรสงวนไว้เฉพาะปุ่ม "ลบถาวร" ในถังขยะเท่านั้น (ดูข้อ 4.5.1)

### 4.7 DB Persistence & Concurrency (2026-09-14, DB Persistence Migration)
- **ข้อมูลจริงใน Supabase แล้ว:** ครั้งที่ประชุม/วาระ/เรื่อง (`meetings`/`tasks` table) ไม่ใช่ mock React state อีกต่อไป — reload หน้าแล้วข้อมูลยังอยู่ครบ (รวม history/checklist ที่เก็บเป็น JSONB ในแถว task)
- **Optimistic concurrency lock:** ทุกแถวมีคอลัมน์ `version` (เลขนับ) — ทุกครั้งที่กด action ที่แก้ไขข้อมูล (ส่งตรวจ/ตรวจแล้ว/ปิดงาน/แก้ไข/ลบ ฯลฯ) ฝั่ง Client ต้องส่ง `version` ที่ตัวเองเห็นล่าสุดไปด้วย ถ้าไม่ตรงกับ DB (มีคนอื่นแก้ไปก่อนแล้ว) ระบบจะ**ปฏิเสธ action นั้นทันที ไม่ทำอะไรเลย** พร้อมโชว์แบนเนอร์สีแดงเต็มความกว้างด้านบนสุดของหน้า (ใต้ Header): ข้อความ "ข้อมูลนี้มีการอัปเดตแล้ว กรุณา Refresh หน้าก่อนทำต่อ" + ปุ่ม "Refresh" (โหลดหน้าใหม่ทั้งหมด) + ปุ่ม "ปิด" (ปิดแบนเนอร์เฉยๆ ไม่ refresh) — **ไม่ทำ Realtime sync** ระหว่างผู้ใช้หลายคน (ตัดสินใจแล้วว่า refresh มือพอสำหรับ V1) ต้องกด action ชนกันเองถึงจะเห็นแบนเนอร์นี้
- **สิทธิ์เขียนข้อมูล 2 ระดับ:** (1) Action บนงานที่มีอยู่แล้ว (ส่งตรวจ/ตรวจแล้ว/ปิดงาน/ย้อนสถานะ) เปิดให้ทุกคนที่ login (2) งานเชิงโครงสร้าง (สร้าง/แก้ไข/ลบ วาระและครั้งที่ประชุม) ต้องมีสิทธิ์ `can_manage_tasks` (ดูข้อ 2 หัวข้อ checkbox "สร้าง/แก้ไข/ลบวาระได้") — เช็คทั้งฝั่ง UI (ซ่อนปุ่ม) และฝั่ง RPC (ปฏิเสธถ้าไม่มีสิทธิ์จริง)

### 4.8 แดชบอร์ดภาระงาน (Workload Dashboard, ใหม่ทั้งหมด 2026-09-15)
เข้าถึงผ่านไอคอนกราฟที่ Header (`ChartIcon`) — เป็นหน้าจอเต็ม (สลับจากหน้าหลักแบบเดียวกับถังขยะ) หัวข้อแค่ **"Dashboard"** คำเดียว ไม่มีคำอธิบายใต้หัวข้อ (ผู้ใช้ขอตัดออกทั้งหมดหลังลองใส่คำอธิบายยาวไปรอบหนึ่งแล้ว) มีส่วนเดียวคือ Heat Map (เดิมมีตารางสรุปรายคนอยู่ข้างบนด้วย — ยกเลิกแล้ว 2026-09-16 ดูด้านล่าง):

**ตารางสรุปรายคน (Assigned / On hand / In Process / Done / งานที่ต้องตรวจ) — ยกเลิกแล้ว 2026-09-16**
ผู้ใช้สั่งตัดออกเพราะข้อมูลซ้ำกับ Heat Map ("เหลือแค่ Heat Map ก็น่าจะพอบอกข้อมูลได้ คิดว่ามันซ้ำๆ กันอยู่")
โค้ดที่ถูกลบไปพร้อมกัน: `SummaryRow` + `countBadge` ใน `WorkloadDashboard.tsx`, `workloadSummary()`/`PersonWorkload` ใน `board-derived.ts`
นิยามของตัวเลขแต่ละตัวยังมีอยู่ในฟังก์ชันภายใน (`onHandCount`/`inProcessCount`/`toReviewCount`) เพราะ Heat Map ใช้ต่อ — ถ้าจะเอาตารางกลับมาต้องสร้าง component ใหม่

**Heat Map:** ตาราง Matrix คน (แถว) × Tab/Sub-tab (คอลัมน์ คงที่ 7 ช่อง: Board×Resume/ร่างรายงาน/Conduct, Excom×Resume/ร่างรายงาน/Conduct, อื่น ๆ) — หัวข้อแค่ **"Heat Map"** คำอธิบายบรรทัดเดียวตายตัวตามที่ผู้ใช้กำหนด: *"หมายเหตุ : จำนวนงานที่ได้รับ Assign + งานที่ต้องตรวจ (เฉพาะผู้ตรวจ) ณ ปัจจุบัน ไม่รวมที่ปิดงานแล้ว"* — แต่ละช่องนับ On hand+In Process+ที่ต้องตรวจ รวมกัน (เท่ากับ Assigned+ที่ต้องตรวจ ไม่รวม Done/closed) ไล่สี 4 ระดับ (ไม่ใช้สีโปร่งแสงบน token ตามกฎเดิม) — **เกณฑ์ตามที่ผู้ใช้กำหนด 2026-09-16** (เดิมเป็น 0 / 1 / 2+ ซึ่งงานเยอะแค่ไหนก็เข้มเท่ากันหมด):
  0 = กรอบจาง `border-border bg-bg` · **1-2 งาน** = `bg-pending-soft text-pending` ตัวหนา · **3-4 งาน** = `bg-pending-mid text-pending` ตัวหนา · **5 งานขึ้นไป** = `bg-pending text-bg` (ทึบ) ตัวหนามาก
  (เกณฑ์นี้ผู้ใช้ปรับเป็นครั้งที่สองในวันเดียวกัน จาก 1-3 / 4-6 / 7+ เป็น 1-2 / 3-4 / 5+ — **เปลี่ยนที่เดียวคือ `heatCellClass()` + `HEAT_LEGEND` ใน `WorkloadDashboard.tsx`**)
  - **Layout ต้อง responsive แบบยืด/หดเต็มความกว้างจริง ห้ามมี horizontal scrollbar และห้ามเหลือช่องว่างเกินความจำเป็น** — เคยลองทั้ง `overflow-x-auto`+`min-w-[46rem]` (มี scrollbar) และคอลัมน์ชื่อแบบสัดส่วน `fr` (เหลือช่องว่างเยอะตอนชื่อสั้นแต่จอกว้าง) ผู้ใช้ไม่ชอบทั้งคู่ — ค่าสุดท้ายที่ใช้: คอลัมน์ชื่อคงที่มีเพดาน `minmax(2.25rem, 4rem)` (ค่าคงที่ `NAME_COL` ใช้ร่วมกันทั้งตารางสรุปและ Heat Map กันหลุด sync) คอลัมน์ข้อมูลที่เหลือ `minmax(0, 1fr)` เกลี่ยพื้นที่ที่เหลือเท่าๆ กันเสมอ ไม่มี `overflow-x-auto`/`min-width` ใดๆ อีก

**Drag and Drop จัดลำดับ (เฉพาะ Admin, ใหม่ 2026-09-15 · เหลือตารางเดียวตั้งแต่ 2026-09-16):** Heat Map มีคอลัมน์ grip icon (`GripIcon`) ซ้ายสุดของแต่ละแถว — **มองเห็นและลากได้เฉพาะ role Admin** (คนอื่นเห็นคอลัมน์ว่างเปล่าแทน ลำดับเหมือนกันทุกคนแต่ลากไม่ได้) ลำดับมาจาก state ตัวเดียว (`orderedPeople`) — **ลำดับนี้เป็นระบบแยกต่างหากจากลำดับผู้ใช้ที่ `/admin/users`** (คนละคอลัมน์ในฐานข้อมูล คนละ RPC ตามที่ผู้ใช้เลือกไว้ชัดเจนว่าอยากได้ระบบใหม่แยกเฉพาะหน้านี้ ไม่ใช้ร่วมกับหน้าจัดการผู้ใช้งาน) ใช้ library เดียวกับ `/admin/users` (`@dnd-kit`) ตาม pattern เดียวกันทุกประการ (ดูข้อ 2 หัวข้อ "การจัดลำดับเปลี่ยนจากปุ่มขึ้น/ลงเป็น Drag and Drop") — ไม่มีการเรียงอัตโนมัติตาม workload อีกต่อไป (เคยเรียงมากไปน้อยอัตโนมัติในรอบแรก ผู้ใช้ขอเปลี่ยนเป็นให้ Admin กำหนดเองล้วนๆ แทน)

### 4.9 จอกว้างมาก (`lg:` ขึ้นไป) — ขยายพื้นที่ใช้งาน + ตัวอักษรใหญ่ขึ้น (2026-09-15)
ผู้ใช้ทักว่าหน้าจอ Desktop กว้างมีพื้นที่ว่างเหลือเยอะสองข้าง ตัวอักษรเล็กไป อยากให้ "ใช้พื้นที่ข้างๆ ให้มากขึ้น" และอ่านง่ายขึ้นโดยรวม (ไม่ระบุตัวเลขเป๊ะ ให้ลองปรับดู) — ใช้ breakpoint `lg:` (1024px ขึ้นไป) เพิ่มเข้าไปเป็นชั้นที่ 3 ต่อจาก mobile/`sm:` เดิม **ไม่แก้ค่า mobile/`sm:` ที่เคยจูนละเอียดมาก่อนหน้านี้เลยแม้แต่จุดเดียว**:
- **ความกว้าง container หลัก:** `AppShell.tsx` เปลี่ยนจาก `max-w-5xl` (64rem) ตายตัว เป็น `max-w-5xl lg:max-w-6xl` (72rem ที่จอกว้าง) — ยืนยันด้วย `getBoundingClientRect()` จริงว่า container กว้าง 1152px ที่ viewport 1280px (ก่อนหน้านี้จะกว้างแค่ 1024px แล้วเหลือขอบว่างมากกว่านี้)
- **Tabs หลัก/ย่อย:** padding/font โตขึ้น (`lg:py-3.5 lg:text-base` ทั้งคู่), Red Badge โตตาม (`lg:h-6 lg:min-w-6 lg:text-[12.5px]`)
- **การ์ดครั้งที่ประชุม/แท็บ "อื่น ๆ":** padding การ์ด `lg:p-6`, หัวข้อ `lg:text-xl` (จาก `text-lg`), ป้ายสถานะ/ปุ่ม "+ เพิ่มเรื่อง"/badge workload โตขึ้นหนึ่งขั้นทั้งชุด, ระยะห่างระหว่างการ์ด `lg:gap-5` (จาก `gap-4`)
- **แถวตารางวาระ (`TASK_ROW_GRID` ใน `TaskRow.tsx`):** เพิ่มชุดค่า `lg:` ต่อท้ายของเดิม — เพดานคอลัมน์ชื่อวาระขยับจาก `22rem` เป็น `28rem`, คอลัมน์ผู้รับผิดชอบ/สถานะขยับ minmax ขึ้นตาม, คอลัมน์ด่วน/ปุ่มลบขยับเล็กน้อย, `gap-3`→`lg:gap-4`, padding แถว `lg:px-4 lg:py-3.5` — ชื่อวาระ `lg:text-[15px]` (จาก `13px`), badge ผู้รับผิดชอบ/ป้ายสถานะ/ไอคอนลบโตขึ้นตามสัดส่วน — ระยะห่างระหว่างการ์ดวาระ (`TaskTable.tsx`) `lg:gap-3` (จาก `gap-2`)
- **ยืนยันด้วย `getComputedStyle()` จริงที่ viewport 1280px:** ชื่อวาระ `15px` ตรงตามที่ตั้ง, หัวข้อครั้งที่ประชุม `20px` ตรงตาม `text-xl` — `tsc --noEmit` ผ่าน ไม่มี error
- **หมายเหตุเครื่องมือ:** ทดสอบผ่าน Browser tool ที่ viewport กว้าง (1280px) แล้วภาพ screenshot ที่ได้ดูเหมือนเนื้อหาแคบผิดปกติ (ไม่ตรงกับที่ควรเป็น) แต่เช็คด้วย `getBoundingClientRect()`/`getComputedStyle()` ตรงๆ แล้วค่าจริงถูกต้องครบทุกจุด — **สรุปว่าเป็นข้อจำกัดการ render screenshot ของ Browser pane ตอน emulate viewport กว้างกว่าพาเนลจริง ไม่ใช่บั๊กของโค้ด** ถ้าเจอเหตุการณ์คล้ายกันอีกในอนาคต ให้เชื่อค่าที่วัดจาก JS ตรงๆ มากกว่าภาพ screenshot

### 4.10 "งานของฉัน" (My Tasks — ใหม่ทั้งหมด 2026-09-15)
มาจากการชวนคิดแบบ brainstorm ("แอพนี้ควรมีอะไรอีกไหม") ผู้ใช้เลือกทำไอเดีย "รวมงานที่ต้องทำข้ามทุก Tab ไว้จุดเดียว" — เข้าถึงผ่านไอคอนเช็คลิสต์ (`ChecklistIcon`) ที่ Header วางไว้ก่อนไอคอนแดชบอร์ด มี **Red Badge นับจำนวนตรงไอคอนเลย** (ต่างจาก Dashboard/ถังขยะที่ไม่มีเลข) เพราะเป็นมุมมองส่วนตัวเหมือน Red Badge บน Tabs ทุกประการ ใช้ logic เดียวกันเป๊ะ (`taskNeedsMyAction`)

- **ขอบเขต:** รวมวาระ/เรื่องจากทุกครั้งที่ประชุมที่ไม่ถูกเก็บเข้าที่เก็บ (`!m.isArchived`) ทุก Tab/Sub-tab บวกแท็บ "อื่น ๆ" — กรองเฉพาะที่ `taskNeedsMyAction(task, ผู้ใช้ปัจจุบัน)` เป็นจริง (นิยามเดียวกับ Red Badge เดิมทุกจุดในแอป ไม่ใช่นิยามใหม่)
- **เรียงตามวันครบกำหนดใกล้สุดก่อน** ตามที่ผู้ใช้ขอ — รายการที่ไม่มีวันครบกำหนดอยู่ท้ายสุดเสมอ (เทียบ string ISO date `YYYY-MM-DD` ตรงๆ เรียงได้ถูกต้องโดยไม่ต้อง parse เป็น Date object)
- **แต่ละรายการมีบรรทัด context บอกที่มา** (เช่น "Board / Resume · ครั้งที่ 10/2569" หรือ "อื่น ๆ") เพราะหลุดออกจากบริบทการ์ดครั้งที่ประชุมเดิมแล้ว จำเป็นต้องรู้ว่าวาระนี้อยู่ที่ไหน — **ปรับตาม feedback รอบแรก:** สลับลำดับเป็น Tab/Sub-tab ขึ้นก่อนชื่อครั้งที่ประชุม (เดิมเอาชื่อครั้งที่ประชุมขึ้นก่อน) และเพิ่มขนาด/น้ำหนักตัวอักษรให้เด่นขึ้นเป็น `font-display text-sm font-semibold text-text` (เดิม `text-[11px] text-muted` เล็ก/จางเกินไปจนดูกลืนไปกับพื้นหลัง)
- **ใช้ `TaskRow.tsx` ตัวเดิมเป๊ะ ไม่สร้าง component แสดงผลใหม่** — action ทั้งหมด (ส่งตรวจ/ตรวจแล้ว/ปิดงาน/แก้ไข/ลบ/เพิ่มหมายเหตุ) ทำงานได้เต็มรูปแบบจากหน้านี้เลยโดยตรง ไม่ต้องกลับไปหน้าเดิม
- **กรองผู้ตรวจตาม Sub-tab ของต้นทางจริง (ปรับตาม feedback รอบแรก):** ตอนแรกใช้ `inspectors` เต็มไม่กรอง (ตั้งใจยอมรับเป็นข้อจำกัด v1 ก่อน) — ผู้ใช้ขอให้กรองจริงหลังทดสอบ → คำนวณ `inspectors` แยกรายแถวใน `AppShell.tsx` ตาม Sub-tab ของครั้งที่ประชุมต้นทางของวาระนั้นๆ (`isInReviewGroup(person, m.subTab)`) แท็บ "อื่น ๆ" ใช้ `otherInspectors` เดิม — `myTaskEntries`/prop `entries` ของ `MyTasksView.tsx` เลยพก `inspectors` มาเป็นรายการต่อ entry แทนที่จะรับ prop `inspectors` เดียวทั้งหน้าเหมือนตอนแรก
- Empty state: "ไม่มีงานที่ต้องดำเนินการตอนนี้"
- **หน้าตา (ปรับตาม feedback รอบแรก):** ตัดคำอธิบายใต้หัวข้อออกทั้งหมด เหลือแค่ "งานของฉัน" คำเดียว (แนวทางเดียวกับหน้า Dashboard) — ครอบรายการทั้งหมดด้วยการ์ด `bg-surface` (เดิมลอยอยู่บนพื้นหน้าเปล่าๆ กลืนกับพื้นหลังทั้งหน้า)
- **เจอบั๊ก dev server ระหว่างทดสอบ (infra ไม่ใช่โค้ด):** สร้างไฟล์ `MyTasksView.tsx` ใหม่แล้ว Turbopack ไม่ยอมรับไฟล์ใหม่เข้า module graph (`Module not found` ค้างซ้ำๆ ใน log แม้ navigate/reload หลายรอบ) จนกว่าจะ `preview_stop`+`preview_start` ใหม่ทั้งกระบวนการ — ตรงกับบั๊ก infra ข้อ 7 ที่เคยจดไว้ก่อนหน้านี้ (ไฟล์ใหม่ที่สร้างกลางเซสชันบางครั้ง Turbopack ไม่ pick up ให้อัตโนมัติ ต้อง restart เต็มรูปแบบ ไม่ใช่แค่ reload)

### 4.11 บอร์ด / โน้ต (วางแผนไว้ 2026-09-15 เป็น Phase 6, ยังไม่ได้สร้างจริง — ดู `PLAN.md` Phase 6 และ `PRODUCT.md` ข้อ 10)
มาจาก brainstorm ระหว่าง Phase 5 — ผู้ใช้ขอ mockup ก่อนตัดสินใจ ("ขอแบบเก๋ๆ สวยๆ น่าใช้ ไม่เอาเว็บบอร์ดโบราณ") ทำ mockup 2 หน้าจอผ่าน visualize widget แล้วผู้ใช้ยืนยันแนวนี้ ("ถูกใจ เอาแบบนี้แหละ") — ใช้ Design Token/สไตล์ badge เดียวกับแอปจริงทุกจุด **ไม่ใช่ตารางกระทู้/หัวข้อ/ผู้ตั้ง/ผู้อ่านล่าสุดแบบเว็บบอร์ดเก่า**

**หน้าบอร์ดกลาง (หน้าใหม่เต็มจอ เข้าถึงจากไอคอนที่ Header แบบเดียวกับ Dashboard/งานของฉัน/ถังขยะ):**
- Tabs กรองด้านบนแบบ capsule เดียวกับ Tabs ย่อยเดิม (Resume/ร่างรายงาน/Conduct): **ทั้งหมด / แท็กฉัน / ของฉัน**
- ปุ่ม `+ สร้างกระทู้` มุมขวาบน ใช้สไตล์ปุ่มทองเดียวกับ `+ เพิ่มเรื่อง`/`+ ครั้งที่ประชุม` (`bg-primary text-on-primary rounded-full`)
- รายการเป็น **การ์ดฟีด** (ไม่ใช่ตาราง) แต่ละใบ (`rounded-xl border border-border bg-surface`):
  - แถวบน: badge คนที่ถูก Tag (ถ้ามี) — สไตล์ป้ายเล็ก `bg-accent-soft text-accent rounded-md` เขียน `@ชื่อ`
  - เนื้อความกระทู้
  - แถวล่าง (footer): badge ผู้สร้าง (สไตล์เดียวกับ badge ผู้รับผิดชอบในตาราง วาระ — `bg-closed-soft`) + เวลาแบบ relative ("2 ชม.ที่แล้ว") ทางซ้าย, ไอคอนคอมเมนต์+จำนวน และ pill วันที่เตือน (ถ้ามี) ทางขวา — pill สีตามความเร่งด่วน: ใกล้ถึง = `bg-pending-soft text-pending` มี 🔥 นำหน้า (ใช้ emoji เดิมตามกฎข้อ 5 ไม่ใช่ icon ใหม่), ยังไกล = `bg-closed-soft text-muted` เฉยๆ
- ไม่มี avatar วงกลม/รูปย่อชื่อ (ตรงกับที่ตัดออกไปแล้วทั้งแอปตามข้อ 7 ด้านล่าง) ใช้ badge ข้อความล้วนทุกจุด

**ส่วนเพิ่มในหน้า "งานของฉัน" (ข้อ 4.10) — หัวข้อใหม่ "บอร์ด/โน้ตของฉัน" ต่อท้ายรายการวาระเดิม:**
- แถวกระชับกว่าการ์ดบอร์ดกลาง (`rounded-lg border border-border`) มีเส้นสีบาง (`border-l-2`) ด้านซ้ายบอกความเร่งด่วนของวันที่เตือน (สีเดียวกับ pill ด้านบน, ไม่มีวันที่เตือน = ไม่มีเส้นสี)
- ข้อความกระทู้ตัดบรรทัดเดียว (`truncate`) + pill วันที่เตือนชิดขวา
- บรรทัดเล็กใต้ข้อความบอกที่มา: "แท็กโดย {ชื่อ}" หรือ "สร้างโดยฉัน" — กระทู้ส่วนตัวมีไอคอนกุญแจเล็กๆ นำหน้าข้อความ + ต่อท้ายบรรทัดที่มาด้วย "· ส่วนตัว"

**สีที่ใช้ทั้งหมดอ้างอิง Token เดิมในข้อ 2 ไม่มีสีใหม่เพิ่ม** — badge Tag ใช้ `accent`/`accent-soft`, badge ผู้สร้างใช้ `closed-soft`, pill วันที่เตือนใช้ `pending`/`pending-soft` (ใกล้) หรือ `closed-soft`/`muted` (ไกล)

**แก้ไขกระทู้ (เพิ่ม 2026-09-15):** ปุ่มไอคอนดินสอ (`EditIcon`) ขึ้นเฉพาะกระทู้ที่ตัวเองสร้างเท่านั้น (ต่างจากปุ่มลบที่ทุกคนที่มีสิทธิ์จัดการวาระเห็น) เปิด Modal เดียวกับตอนสร้าง (`CreateBoardPostForm.tsx` รับ prop `editingPost` เสริมเพื่อ prefill ค่าเดิม) ใช้ได้ทั้งในหน้าบอร์ดและหน้า "งานของฉัน"

**กดที่ตัวกล่อง Memo เพื่อดู Reply ได้เลย (เพิ่ม 2026-09-15 ต่อเนื่อง):** เดิมต้องกดที่ไอคอนคอมเมนต์+ตัวเลขเท่านั้นถึงจะขยาย thread ผู้ใช้ขอให้กดที่**ตัวกล่อง/แถวทั้งใบ**แล้วขยายได้เลย (pattern เดียวกับที่ `TaskRow.tsx` ใช้อยู่แล้วสำหรับแถววาระ — คลิกที่แถวเพื่อขยายดูรายละเอียด) ไอคอนคอมเมนต์+ตัวเลขเปลี่ยนจากปุ่มเป็นแค่ตัวเลขแสดงจำนวน reply เฉยๆ (ไม่ใช่ปุ่มกดแยกอีกต่อไป) — ปุ่ม Edit/Delete และพื้นที่ thread ที่ขยายออกมา (คอมเมนต์/ฟอร์มตอบกลับ) ยังกดแยกได้ตามปกติโดยไม่ไปกระตุ้นให้ยุบ/ขยายกล่องซ้ำ (`stopPropagation`) ใช้ทั้ง `BoardPostCard.tsx` (หน้า The Wall) และ `BoardPostMiniRow` ใน `MyTasksView.tsx` (ส่วน "My Memo")

**สไตล์ Post-it (ไม่ใช่มือถือ ตั้งแต่ `sm:` ขึ้นไป, เพิ่ม 2026-09-15, ขยายขอบเขตจาก `lg:` เดิมในวันเดียวกัน):** ผู้ใช้ถามเล่นๆ ว่าอยากได้แนวกระดานโน้ตกาวสไตล์ post-it บนจอ Desktop ได้ไหม แล้วขอให้ทำจริงเลย — ตอนแรกทำแค่ `lg:` (≥1024px) แต่ผู้ใช้ทักว่าย่อหน้าต่าง Desktop แคบกว่านั้น (เช่น ~780px) ยังเห็นเป็น list ธรรมดาอยู่ ไม่ต่อเนื่อง อยาก ได้ post-it "ทุกขนาดหน้าจอ ใน Desktop" → ขยับ threshold ลงมาเป็น `sm:` (≥640px) แทน เฉพาะหน้าบอร์ดกลาง (ไม่ใช่ส่วน "บอร์ด/โน้ตของฉัน" ในหน้างานของฉันที่ยังเป็น list กระชับเหมือนเดิม เพราะเป็นคนละจุดประสงค์กัน):
- Layout เปลี่ยนจาก list แนวตั้งเป็น **CSS multi-column masonry** — `sm:columns-2` (จอกลาง 640-1023px) ขยับเป็น `lg:columns-3` (จอกว้าง ≥1024px) `gap-5` การ์ด `sm:break-inside-avoid` ให้ความสูงต่างกันได้ตามธรรมชาติเหมือนกระดานจริง — **มือถือจริง (<640px) เท่านั้นที่ยังเป็น list เดิม** ไม่งั้นคอลัมน์แคบเกินไปจนดูอึดอัด
- แต่ละใบวนใช้สีพื้นหลัง pastel จาก Token ที่มีอยู่แล้ว 5 สี (`primary-soft`/`pending-soft`/`done-soft`/`accent-soft`/`closed-soft` — ตัดแต่ `note-soft` ออกเพราะ dark mode เท่ากับ `surface` เฉยๆ ทำให้ใบนั้นดูไม่มีสีต่างจากใบอื่น) วนตาม index แบบ deterministic ไม่ใช่สุ่ม
- เอียงเล็กน้อยคงที่ต่อใบ (`--note-rotate` CSS variable ผูกกับ `sm:rotate-[var(--note-rotate)]`, ชุดมุมที่ใช้ -1.5deg ถึง 1.5deg สลับกันไป) ตั้งตรงเมื่อ hover (`sm:hover:rotate-0`) พร้อมเงาเข้มขึ้น (`sm:hover:shadow-lg`) ให้ความรู้สึกหยิบขึ้นมาดู — ไม่มี border (`sm:border-0`) แทนด้วยเงา (`sm:shadow-md`) ให้ดูเหมือนแปะอยู่บนกระดาน
- Badge Tag/ผู้สร้างเปลี่ยนพื้นหลังเป็น `sm:bg-surface/70` (โปร่งแสง) แทนสีเดิม เพื่อให้อ่านออกชัดไม่ว่าตัวใบจะเป็นสีอะไรใน 5 สีที่วนมา (กัน badge สีเดียวกับพื้นใบแล้วกลืนหายไป)
- ทดสอบยืนยันด้วย Browser จริงที่ 3 ขนาด: 600px (มือถือ ยัง list เดิม), 780px (post-it 2 คอลัมน์), ≥1024px (post-it 3 คอลัมน์) ทำงานถูกต้องครบ

### 4.12 The Wall — Red Badge "ของใหม่", ตัวเลขท้ายแท็บ และการเลิกใช้ Private Memo (2026-09-16)

**1) Red Badge ที่ไอคอน The Wall = ของใหม่ที่ยังไม่ได้เปิดดู (ไม่ใช่ยอดสะสม)**
เดิม badge นับ memo ที่เกี่ยวกับฉันทั้งหมด (สร้างเอง + ถูก tag) → ตัวเลขค้างอยู่ตลอดไปจนกว่าจะมีคนลบ memo ทิ้ง (ปัญหาข้อ 6 ใน `FIX-INSTRUCTIONS.md`)
นิยามใหม่ตามที่ผู้ใช้กำหนด: **"ข้อความใหม่ที่ Tag ฉันแล้วฉันยังไม่ได้กดดู"**
- เก็บที่คอลัมน์ `board_posts.seen_by uuid[]` (ไม่ได้สร้างตารางใหม่ — ลบ memo แล้วข้อมูลการอ่านหายไปพร้อมกัน ไม่มีแถวกำพร้าค้าง) ดู migration `0007`
- ฐานข้อมูลคืนค่า `unread` (boolean ต่อผู้ใช้ที่เรียก) มาให้แล้ว ฝั่งเว็บแค่นับ (`boardPostUnreadCount`)
- **กดเข้าหน้า The Wall = อ่านทั้งหมด** badge กลับเป็นศูนย์ทันทีโดยไม่รอเซิร์ฟเวอร์ (`openWall()` ใน `AppShell.tsx` → `markWallSeenAction()`) — ไม่ใช่กดทีละใบ เพราะเนื้อความแสดงเต็มอยู่บนการ์ดอยู่แล้ว
- badge ที่ไอคอน "งานของฉัน" ใช้ตัวเลขเดียวกันนี้บวกกับจำนวนวาระที่รอ action (เดิมบวกยอดสะสมของ memo จึงไม่มีวันเป็นศูนย์เหมือนกัน)

**2) ป้าย "New" บนการ์ด — ตอบคำถามว่า "กดเข้าไปแล้วจะรู้ได้ยังไงว่าอันไหนใหม่"**
- ตอนกดเข้าหน้า จะจำ id ของ memo ที่ยังไม่ได้อ่าน ณ วินาทีนั้นไว้ (`newPostIds`) แล้วค้างไว้ตลอดรอบที่เปิดหน้านั้นอยู่ — ถ้าอิง `unread` ตรง ๆ ป้ายจะหายพร้อม badge ทันทีจนผู้ใช้ไม่ทันเห็น
- การ์ดที่เป็นของใหม่: ป้าย **New** ทึบ `bg-accent text-bg` (ตัวพิมพ์ใหญ่ ขนาด 11px) วางเป็นป้ายแรกก่อน "For you" + กรอบ/ring สีน้ำเงิน `border-accent sm:ring-2 sm:ring-accent` แทนกรอบแดงปกติ
- ออกจากหน้าแล้วกลับเข้ามาใหม่ = ป้ายหายหมด (ไม่มีอะไรใหม่แล้ว)
- **คนเขียนไม่ถูกนับเป็นของใหม่ให้ตัวเอง** — ตอน insert ใส่ `seen_by = array[ผู้เขียน]` ไว้เลย

**3) ตัวเลขท้ายแท็บ Tagged / Mine — คนละความหมายกับ Red Badge**
- เป็น **จำนวนทั้งหมด** ของแท็บนั้นแบบถาวร ไม่เกี่ยวกับการอ่าน — ลดลงเมื่อ memo ถูกลบเท่านั้น (ตามที่ผู้ใช้สั่ง: "อันนี้เป็นแบบถาวร แค่เพื่อแสดงจำนวน จะหายไปก็ต่อเมื่อลบ Memo")
- สไตล์: วงกลมเล็กในตัวปุ่ม `h-4.5 min-w-4.5 rounded-full` — แท็บที่เลือกอยู่ `bg-accent-soft text-accent`, แท็บที่ไม่ได้เลือก `bg-surface/70 text-muted` (**ตั้งใจไม่ใช้สีแดง** `pending` เพื่อไม่ให้สับสนกับ Red Badge แจ้งเตือน)
- ซ่อนเมื่อเป็น 0 · แท็บ **All** ไม่มีตัวเลข (จำนวนการ์ดที่เห็นตรงหน้าคือคำตอบอยู่แล้ว)

**4) เลิกใช้ Private Memo**
ผู้ใช้สั่งเอาออกทั้งหมด 2026-09-16 ("มันมองไม่เห็น คุมจำนวนไม่ได้ เดี๋ยวรก db") — หลักการของ The Wall คือแปะสิ่งที่ต้องทำ/แจ้งข่าว แล้วลบทิ้งเมื่อทำเสร็จหรือเลยเหตุการณ์ไปแล้ว
- ตัด checkbox "Private" ออกจาก `CreateBoardPostForm` · ตัดไอคอนกุญแจ (`LockIcon`) ออกจากการ์ดบอร์ดและแถว memo ในหน้า "งานของฉัน" · แท็บ All ไม่ต้องกรองอะไรอีกแล้ว
- ลบคอลัมน์ `board_posts.is_private` ทิ้ง (migration `0007`) — ตรวจก่อนลบแล้วว่าไม่มีกระทู้ที่ตั้งส่วนตัวค้างอยู่เลยสักรายการ จึงไม่มีบันทึกของใครกลายเป็นสาธารณะจากการลบนี้
- `LockIcon` ยังอยู่ใน `src/components/icons.tsx` แต่ไม่มีที่ไหนเรียกใช้แล้ว

### 4.13 Accessibility — ใช้คีย์บอร์ดล้วนได้ และอ่านออกจริง (Phase E, 2026-09-16)

**1) กรอบโฟกัส (U10)** — เพิ่มกฎกลางใน `globals.css`: `:focus-visible { outline: 2px solid var(--accent); outline-offset: 2px }`
ใช้ `:focus-visible` ไม่ใช่ `:focus` → กดด้วยเมาส์ไม่ขึ้นกรอบ ขึ้นเฉพาะตอนเดินด้วยคีย์บอร์ด
กฎนี้อยู่นอก `@layer` จึงชนะ `outline-none` ของ Tailwind ที่ช่องกรอกทุกช่องใส่ไว้ (ยืนยันด้วยค่า computed จริงในเบราว์เซอร์)

**2) แถววาระ / การ์ด memo กดด้วยคีย์บอร์ดได้ (U8)** — เดิมเป็น `<div onClick>` ล้วน กด Tab ไม่ถึงและ Enter ไม่ทำงาน
ทำ **เฉพาะ "ชื่อวาระ" (TaskRow) และ "ตัวข้อความ memo" (BoardPostCard / BoardPostMiniRow)** ให้เป็น `<button>` จริง พร้อม `aria-expanded` + `aria-controls` ชี้ไปที่กล่องรายละเอียดที่กางออกมา
**ทำไมไม่ครอบทั้งแถวเป็นปุ่มเดียว** ตามที่รายงานตรวจโค้ดเสนอ: ในแถวมีปุ่มลบ/แก้ไขอยู่ข้างในด้วย ปุ่มซ้อนปุ่มเป็น HTML ที่ผิดและ screen reader อ่านพัง — คนใช้เมาส์ยังคลิกที่ไหนก็ได้ในแถวเหมือนเดิม

**3) Modal เต็มรูปแบบ (U9)** — `Modal.tsx`: `role="dialog"` + `aria-modal` + `aria-labelledby` (ผูกกับหัวข้อผ่าน `useId`), Esc ปิด, focus trap วน Tab อยู่ในกรอบ, ล็อกไม่ให้พื้นหลังเลื่อน, ปิดแล้วคืนโฟกัสให้ปุ่มที่เปิด
เพิ่ม `max-h-[90vh] overflow-y-auto` ให้ตัว dialog ด้วย (เนื้อหายาวบนจอเตี้ยเดิมล้นออกนอกจอจนกดปุ่มยืนยันไม่ได้)
⚠️ **ตัวเลือก focusable ต้องมี `:not([disabled])`** ไม่งั้น "ตัวสุดท้ายในกรอบ" กลายเป็นปุ่มที่โฟกัสไม่ได้ แล้วการวน Tab ตาย — เจอจริงตอนทดสอบ dialog "ส่งตรวจ" ที่ปุ่มยืนยันยังกดไม่ได้

⚠️⚠️ **effect ที่สั่งโฟกัสตอนเปิด dialog ต้องมี deps เป็น `[]` เท่านั้น ห้ามใส่ `onClose`** (แก้ 2026-09-16 หลังผู้ใช้เจอของจริงบนเว็บ)
  `onClose` ถูกสร้างใหม่ทุก render ของ component แม่ → พิมพ์ 1 ตัวอักษร = แม่ render ใหม่ = effect ทำงานซ้ำ = **โฟกัสเด้งกลับไปช่องแรกของ dialog ทุกตัวอักษร** (ผู้ใช้เจอตอนพิมพ์บันทึกช่วยจำแล้วโฟกัสกระเด้งไป dropdown)
  วิธีที่ใช้: แยกเป็น 2 effect — ตัวโฟกัส/ล็อกสกรอลล์/คืนโฟกัส deps `[]` · ตัวดักปุ่มคีย์บอร์ด deps `[onClose]` (ถอด/ใส่ตัวดัก event ไม่ยุ่งกับโฟกัส)

**4) ขนาดตัวอักษรขั้นต่ำ 11px (U11)** — ภาษาไทยมีวรรณยุกต์บน-ล่าง ที่ 8.5–10px พร้อม `leading-tight` อ่านแทบไม่ออก
ไล่แก้ทั้งโปรเจกต์ 23 จุด: `8.5px`/`9px`/`9.5px`/`10px` → `11px`, `10.5px` → `11.5px` (ตอนนี้ไม่มีอะไรต่ำกว่า 11px เหลืออยู่)
`EMPTY_MARK_CLASS` เปลี่ยนจาก `text-muted/40` (contrast ~1.8:1 ต่ำกว่าเกณฑ์ WCAG AA มาก) เป็น `text-muted/70`

**5) หัวคอลัมน์ Heat Map บนมือถือใช้ชื่อย่อ 2 บรรทัด (ผลพวงจากข้อ 4)** — พอตัวอักษรขึ้นเป็น 11px ชื่อเต็มชนกันเองที่ความกว้าง 375px
จอ `sm:` ขึ้นไปยังเป็นชื่อเต็มเหมือนเดิมทุกประการ ส่วนมือถือแสดงเป็น **B Re · B ร่าง · B Con · Ex Re · Ex ร่าง · Ex Con · อื่น ๆ** (บรรทัดบน = Board/Excom, บรรทัดล่าง = กลุ่มตรวจ) และมี `title` เป็นชื่อเต็มให้ชี้ดูได้
  (ชุดย่อนี้ผู้ใช้เลือกเองเมื่อ 2026-09-16: `E` → `Ex` และ `Res` → `Re`)

**6) Legend ของ Heat Map (U12)** — เดิมความเข้มของสีเป็นสัญญาณเดียว คนตาบอดสีแดง-เขียวแยกระดับไม่ออก
เพิ่มแถบคำอธิบายใต้หมายเหตุ: ไม่มีงานค้าง / 1 งาน / 2 งานขึ้นไป โดย**ใช้ `heatCellClass()` ตัวเดียวกับตารางจริง** จะได้ไม่มีวันหลุด sync กัน

**7) ปุ่มดำเนินการบอกสถานะระหว่างรอ (U5)** — `TaskRow` มี state `busy`: ระหว่างรอเซิร์ฟเวอร์ (ราว 3 วินาที) ปุ่มทุกตัวถูก disable และข้อความเปลี่ยนเป็น **"กำลังบันทึก..."**
เดิมหน้าจอนิ่งสนิทจนผู้ใช้กดซ้ำ แล้วได้ `version_conflict` ปลอม ๆ ทั้งที่ไม่มีใครมาแก้ชนจริง

**8) วันที่/เวลายึดเวลาไทยเสมอ (B12/C12)** — ดูข้อ 4.6 ของ `CODE-REVIEW.md`
`board-derived.ts` คำนวณด้วย UTC + ชดเชย +7 ชม. เอง (`parseDateOnly`, `thaiDayNumber`, `formatLogDateTime`) → เซิร์ฟเวอร์ (UTC) กับเบราว์เซอร์ (UTC+7) ได้ผลตรงกันเสมอ
เวลาแบบ "2h ago" ย้ายไปอยู่ใน component `<RelativeTime>` ที่แสดงวันเวลาเต็มก่อนแล้วสลับเป็นแบบสัมพัทธ์หลัง hydrate (ใช้ `useSyncExternalStore` ไม่ใช่ `useState`+`useEffect` เพราะกฎ lint ห้าม setState ใน effect)
**ถอด `suppressHydrationWarning` ออกแล้ว 8 จาก 9 จุด** เหลือจุดเดียวที่ `<html>` ใน `layout.tsx` ซึ่งมีเหตุผลรองรับ (script ตั้งธีมก่อน React ทำงาน — เขียนอธิบายไว้ในไฟล์แล้ว)

**9) หน้าหลักบนมือถือแน่นขึ้น (ผู้ใช้สั่ง 2026-09-16)**
- บรรทัด timeline ของครั้งที่ประชุม และแถว "เอกสารรอตรวจอยู่ที่" ลดจาก `text-xs` (12px) เหลือ **11px เฉพาะจอมือถือ** (จอ `sm:` ขึ้นไปเท่าเดิม) → ทั้งสองบรรทัดจบใน 1 บรรทัดแล้ว
- หัวคอลัมน์ **"ผู้รับผิดชอบ" ต้องอยู่ 2 บรรทัด** — แก้โดย**ขยายคอลัมน์จาก `2.75rem` เป็น `3.25rem` และถอด `pl-2.5` ออก** ไม่ใช่ลดขนาดตัวอักษร
  เพราะ 11px คือพื้นล่างที่ตั้งไว้ตามเกณฑ์อ่านออก (ข้อ 4 ด้านบน) ลดกว่านี้จะย้อนกลับไปสู่ปัญหาเดิมที่วรรณยุกต์ไทยอ่านไม่ออก
  **ขนาดที่ลงตัว:** `3.5rem` ก็ได้ 2 บรรทัดเหมือนกันแต่ไปเบียดคอลัมน์ชื่อวาระจนหัวข้อยาวขึ้นเป็น 3 บรรทัด — `3.25rem` คือจุดที่ทั้งสองอย่างพอดี (วัดจากความสูงจริงในเบราว์เซอร์ ไม่ได้กะเอา)

### 4.14 แยก 5 หน้าจอเป็น route จริง (Phase F1, 2026-09-16)

**ปัญหาเดิม:** ทั้งแอปอยู่ที่ URL `/` เดียว หน้าจอสลับด้วย state ในหน่วยความจำ (`screen`) ผลคือ
- กดปุ่ม **Back ของเบราว์เซอร์ = ออกจากแอปทันที** (ไม่ใช่ย้อนกลับหน้าที่แล้ว)
- **refresh แล้วเด้งกลับหน้าแรกเสมอ** ไม่ว่าจะค้างอยู่หน้าไหน
- ส่งลิงก์หน้าใดหน้าหนึ่งให้เพื่อนไม่ได้ · เปิดแท็บใหม่แยกดูไม่ได้

**ตอนนี้:**

| URL | หน้าจอ | ชื่อแท็บเบราว์เซอร์ |
|---|---|---|
| `/` | หน้าหลัก (ครั้งที่ประชุม/วาระ) | Workload Tracker |
| `/board` | The Wall | The Wall · Workload Tracker |
| `/my-tasks` | งานของฉัน | งานของฉัน · Workload Tracker |
| `/dashboard` | Heat Map | Dashboard · Workload Tracker |
| `/trash` | ถังขยะ (admin เท่านั้น) | ถังขยะ · Workload Tracker |

**โครงสร้างที่เลือกใช้ และเหตุผล:**
- ทุก route เรียก `loadAppData()` ตัวเดียวกัน (`src/app/app-data.ts`) แล้วส่งให้ `<AppShell screen="..." />`
  **ไม่ได้แตก AppShell ออกเป็น 5 ไฟล์** เพราะ Header (ซึ่งมีเลขแจ้งเตือนของทั้งงานและ memo) ต้องใช้ข้อมูลชุดเต็มอยู่แล้วทุกหน้า การแตกจะได้แค่โค้ดซ้ำ 5 ที่
- ปุ่มบน Header เปลี่ยนจาก `<button onClick>` เป็น **`<Link href>`** → Next.js prefetch หน้าปลายทางให้ กดแล้วขึ้นทันที และคลิกกลางเปิดแท็บใหม่ได้
- ปุ่ม "กลับหน้าหลัก" ในแต่ละหน้ายังเป็น callback `onBack` เหมือนเดิม (เรียก `router.push("/")`) ไม่ต้องแก้ component ลูกทั้ง 4 ตัว
- `/trash` **ตรวจสิทธิ์ admin ซ้ำในหน้าเอง** ไม่พึ่ง middleware อย่างเดียว (แก้ B10) และทุกหน้า `redirect("/login")` เมื่อไม่มี session แทนที่จะคืนหน้าเปล่า (แก้ B9)

**ผลข้างเคียงที่ต้องรู้:** การเปลี่ยนหน้า = ดึงข้อมูลจากเซิร์ฟเวอร์ใหม่ทุกครั้ง (~200-700ms) และ `AppShell` ถูกสร้างใหม่ทุกครั้ง
- state ชั่วคราว (toast, ข้อความเตือนข้อมูลชนกัน, แท็บที่เลือกอยู่) จะรีเซ็ตเมื่อเปลี่ยนหน้า — **ตั้งใจให้เป็นแบบนี้** เพราะข้อมูลที่ได้มาก็สดกว่าเดิม
- จังหวะ "เพิ่งเปิดหน้า The Wall" ที่ใช้ทำป้าย New จึงผูกกับการ mount ของ `AppShell` ได้พอดี (ดูข้อ 4.12)

## 5. ไอคอน (Icons)
- ใช้ไอคอนเส้น (stroke-based, minimal) เท่านั้นสำหรับ action buttons — **ห้ามใช้ emoji แทนไอคอนการทำงาน** (แก้ไข, ย้ายที่เก็บ, ลบถาวร, กู้คืน, กลับหน้าหลัก)
- รูปทรงไอคอนล่าสุด (`src/components/icons.tsx`, ปรับ 2026-09-13): `EditIcon` = สี่เหลี่ยมมุมมนพร้อมดินสอทับมุม (square-pen), `ArchiveIcon` = ตารูปวงรีมีเส้นทแยงขีดทับ (eye-off) — ทั้งคู่เปลี่ยนจากทรงเดิม (ดินสอเดี่ยว / กล่องลิ้นชัก) ตามตัวอย่างภาพที่ผู้ใช้ส่งมา ใช้ชื่อฟังก์ชันเดิมไม่เปลี่ยน (แค่เปลี่ยน path ข้างใน) จุดที่ import อยู่แล้วไม่ต้องแก้โค้ดเพิ่ม
- `ChartIcon` (แดชบอร์ด) และ `ChecklistIcon` (งานของฉัน, เพิ่ม 2026-09-15 ดูข้อ 4.10) เป็นไอคอนเส้นแบบเดียวกับชุดเดิม
- ปุ่มลบวาระ/เรื่องที่หน้าหลัก (`TaskRow.tsx`) และปุ่มลบผู้ใช้ (`DeleteUserButton.tsx`) ใช้ `TrashIcon` เป็นไอคอนอย่างเดียว ไม่ใช่ข้อความ "ลบ" อีกต่อไป
- ข้อยกเว้น: สัญลักษณ์ไฟ 🔥 สำหรับ "ความด่วน" ยังคงใช้ emoji ต่อไป เพราะเป็น content marker ที่ระบุไว้ใน PRODUCT.md ไม่ใช่ action icon
- แนะนำให้ทำเป็น SVG sprite (`<symbol>` + `<use>`) เพื่อลดการซ้ำโค้ด: `i-edit`, `i-archive`, `i-trash`, `i-restore`, `i-back`, `i-chevron`

## 6. องค์ประกอบสถานะ (Status & Badges)
- สถานะวาระ/เรื่องแสดงเป็น pill (`border-radius: 999px`) พร้อมสีความหมาย (semantic color) แยกจากสี accent หลักเสมอ:
  - ว่าง/ยังไม่ส่ง: ไม่มีป้าย (แสดงเครื่องหมายค่าว่างมาตรฐาน — ดูด้านล่าง)
  - ส่งตรวจ: pill สี `--pending` (เช่น "ส่ง พี่จา")
  - ตรวจแล้ว: pill สี `--done` (เช่น "พี่โอ๋ แล้ว")
  - ปิดงาน: pill สี `--status-closed` (โทนเทาล้วน ตั้งใจให้ดูซีด/นิ่งกว่าสถานะอื่น)
- ผู้รับผิดชอบแสดงเป็น badge พื้น `--closed-soft` — **ถ้ายังไม่กำหนดผู้รับผิดชอบ ต้องแสดง `EMPTY_MARK` (`–`) ไม่ใช่ badge ว่างเปล่า** (แก้ 2026-09-15 เดิมโชว์ pill สีเทาว่าง ๆ ไม่สื่ออะไร)
- **เครื่องหมายแทนค่าว่าง/ไม่มีข้อมูล — มาตรฐานเดียวทั้งแอป (`EMPTY_MARK`, เพิ่ม 2026-09-15):** เดิมแต่ละจุดใช้ em-dash `—` (`text-muted` เข้ม ตัวหนาบ้าง) ไม่สม่ำเสมอกัน ผู้ใช้ขอให้ **สั้นกว่าเดิม จางกว่าเดิม บางกว่าเดิม ไม่ต้องเน้น และใช้แบบเดียวกันทุกหน้า** → กำหนดค่าคงที่ `EMPTY_MARK = "–"` (en-dash สั้นกว่า em-dash) และ `EMPTY_MARK_CLASS = "font-normal text-muted/40"` export จาก `board-derived.ts` ให้ทุกไฟล์ import ใช้ร่วมกัน (ห้ามพิมพ์ `"—"`/`text-muted` ตรงๆ อีก) — ปัจจุบันใช้ที่ `TaskRow.tsx` (ป้ายสถานะว่าง, ช่องด่วน/วันครบกำหนดว่าง) และ `WorkloadDashboard.tsx` (ตัวเลข 0 ทุกคอลัมน์, Heat Map cell ว่าง) — จุดใหม่ในอนาคตที่ต้องโชว์ "ไม่มีข้อมูล" ให้ใช้ค่าคงที่นี้เสมอ ไม่สร้างขึ้นใหม่เอง

## 6.1 Dialog "บันทึกย้อนหลังว่าผู้ตรวจตรวจแล้ว" (เพิ่ม 2026-09-15 — สเปกเต็มดู PRODUCT.md ข้อ 5.1)

ขึ้นเมื่อกด **"ปิดงาน"** หรือ **"ส่งตรวจ"** ขณะงานยังค้างที่ผู้ตรวจ (สถานะ `pending`) — ถ้าไม่ได้ค้างที่ใคร **ต้องไม่ขึ้นมากวน**

ใช้ `<Modal>` ตัวเดิม (`src/components/Modal.tsx`) หัวข้อ "ปิดงาน" / "ส่งต่องาน (ส่งตรวจ)" โครงภายใน:

```
┌─ กล่อง border-border bg-bg rounded-lg p-3 ────────────┐
│ งานนี้ยังค้างอยู่ที่ **พี่ปู** (ยังไม่ได้กด "ตรวจแล้ว" ในระบบ) │
│  ( • ) พี่ปู ตรวจแล้ว — บันทึกย้อนหลังให้ด้วย   ← default │
│  (   ) พี่ปู ยังไม่ได้ตรวจ — ไม่ต้องบันทึก              │
│  [ หมายเหตุ (ถ้ามี) เช่น ผบ. แจ้งทางไลน์ 15 ก.ย. ]      │
└────────────────────────────────────────────────────┘
                              [ ยกเลิก ]  [ ยืนยันปิดงาน ]
```

- radio ใช้ `accent-accent` ขนาด `h-3.5 w-3.5` · ตัวอักษร `text-xs text-text` · ชื่อผู้ตรวจตัวหนา
- ช่องหมายเหตุ**แสดงเฉพาะตอนเลือก "ตรวจแล้ว"** (ซ่อนเมื่อเลือกอีกตัว)
- ในโหมด "ส่งตรวจ" กล่องนี้อยู่**เหนือ**ช่องเลือกผู้ตรวจคนใหม่และช่องข้อความ
- ชื่อผู้ตรวจในกล่องนี้ต้องเปิดจากรหัสประจำตัวเสมอ (`personName(people, task.inspectorId)`) ไม่ใช่ข้อความที่ฝังไว้

**การแสดงผลใน Timeline:** บรรทัดที่ถูกบันทึกย้อนหลังต้องต่อท้ายด้วยข้อความจาง ๆ ว่าใครบันทึกแทน
`พี่ปู > ตรวจแล้ว` + `(บันทึกย้อนหลังโดย โบ)` (`font-normal text-muted` ต่อในบรรทัดเดียวกัน)
— ห้ามทำให้ดูเหมือนเจ้าตัวกดเอง เพราะเอกสารประชุมต้องตรวจสอบย้อนหลังได้

## 6.2 แถบแจ้งเตือนเมื่อข้อมูลชนกัน (ปรับ 2026-09-15)

แถบสีส้ม (`bg-pending-soft`) ใต้ Header เมื่อมีคนอื่นแก้ข้อมูลเดียวกันไปก่อน
- ปุ่มเดิมชื่อ "Refresh" และ **reload ทั้งหน้า** ทำให้สิ่งที่ผู้ใช้พิมพ์ค้างไว้หายหมด
- **ปัจจุบันชื่อ "ดูข้อมูลล่าสุด"** ดึงข้อมูลใหม่มาแสดงในที่เดิม (ไม่ reload) ระหว่างโหลดเปลี่ยนข้อความเป็น "กำลังดึงข้อมูล..." และ disable ปุ่ม
- หลักการที่ต้องยึด: **อย่าสั่งให้ผู้ใช้ไป refresh เอง ระบบต้องดึงให้**

## 7. สิ่งที่ตัดออกจากดีไซน์ก่อนหน้า (Removed)
- พื้นหลังเหลืองอ่อน/เบจ (Pale Yellow/Beige) เดิม — เปลี่ยนเป็นพาสเทลอิง กฟผ. ตามข้อ 2
- แท็บย่อยแบบ underline บางๆ — เปลี่ยนเป็นแคปซูลเต็มพื้นที่ตามข้อ 4.3
- กล่องสรุปตัวเลข KPI (วาระทั้งหมด/รอตรวจ/ตรวจแล้ว/ด่วน) แยกจากบรรทัด workload — เอาออก ซ้ำซ้อน
- Avatar ตัวย่อชื่อผู้ใช้ที่ Header — เอาออก ใช้ข้อความชื่อเฉยๆ (**กลับมาแล้ว 2026-09-15 ต่อเนื่อง — ผู้ใช้ขอเพิ่มคืน ดูรายละเอียดที่ข้อ 4.1**)
- ข้อความ role (Owner, Inspector) ต่อท้ายชื่อผู้ใช้ที่ Header — เอาออก
- ปุ่ม Export Excel — เอาออกชั่วคราว (ยังไม่ทำใน V1 นี้ รอคุยเพิ่มเติม)
- ปุ่มลบครั้งที่ประชุมแบบกดแล้วหายทันที และ Archive panel แบบ inline ในหน้าหลัก — แทนที่ด้วยหน้าถังขยะแยกตามข้อ 4.6
- ป้ายตัวเลขแจ้งเตือนบนไอคอนถังขยะ — เอาออก
