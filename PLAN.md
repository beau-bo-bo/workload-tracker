# PLAN.md — Development Phases

## แนวทางการพัฒนา (Build Strategy)

- **Sequential Execution:** Coding Assistant **ต้องทำตามลำดับ Phase ห้ามข้ามขั้น**
- **Stop and Wait:** เมื่อจบแต่ละ Phase ให้หยุดทำงานและขอคำอนุมัติ (Approval) จากผู้ใช้ทันที ห้ามแอบเขียนโค้ดสำหรับ Phase ถัดไปเตรียมไว้
- **Mock Data First:** ในช่วงแรก (Phase 2-3) ให้ใช้ Mock Data ก่อน เพื่อให้เห็นภาพ UI และ Flow การทำงาน จากนั้นค่อยเชื่อมต่อ/บันทึกจริงในขั้นตอนถัดไป

---

## Phase 1: Authentication & Basic Structure

**เป้าหมาย:** สร้างโครงสร้างพื้นฐานของระบบ และระบบการจัดการผู้ใช้งาน (User Management) อย่างง่าย

1.  **Project Setup:**
    - ตั้งค่าโครงสร้างโปรเจกต์ React/Next.js/HTML (ตามเทคโนโลยีที่เลือกใช้)
    - ตั้งค่า Tailwind CSS สำหรับการจัดการ Styling ให้ตรงตาม `DESIGN.md`
2.  **User Data/Mock Database:**
    - สร้างโครงสร้างข้อมูล (หรือ Mock Database) สำหรับ User Accounts (Admin, Owners, Inspectors)
    - กำหนด Roles/สิทธิ์เบื้องต้น
3.  **Authentication UI:**
    - สร้างหน้า Login แบบเรียบง่าย
    - สร้างฟังก์ชันและปุ่ม Reset Password พื้นฐาน (เปลี่ยนรหัสผ่านในระบบ)
4.  **Admin Panel (Minimal):**
    - สร้างหน้าจอ (หรือระบบหลังบ้านแบบง่าย) ให้ Admin สามารถเพิ่มรายชื่อ User, Username, Password และ Roles ได้
5.  **User Check & Output:**
    - แสดงหน้า Login
    - ทดสอบการ Login ด้วย Role ต่างๆ (Admin, Owner, Inspector)
    - _-> หยุดและรอ Approval_

---

## Phase 2: Navigation (Tabs) & Session View Layout

**เป้าหมาย:** สร้างโครงสร้าง Layout หลัก (Tabs) และหน้าจอโครงร่างของการแสดง "ครั้งที่ประชุม" (อ้างอิงจากรูปภาพ)

1.  **Main Layout & Tabs:**
    - สร้างระบบ Navigation: Level 1 Tabs (`Board`, `Excom`, และ `อื่น ๆ`)
    - สร้างระบบ Navigation: Level 2 Tabs (`Resume`, `ร่างรายงาน`, `Conduct`)
    - จัด Styling สีและรูปร่าง Tabs ตาม `DESIGN.md` (ยังไม่ต้องใส่ตัวเลข Notification สีแดง)
2.  **Meeting Session Structure (ครั้งที่ประชุม):**
    - ปุ่ม `+ สร้างครั้งที่ประชุม` เหนือรายการ (Mock — ยังไม่ต้องเปิด Modal จริง)
    - แสดงครั้งที่ประชุมเป็นการ์ดเรียงไล่ลงมา (ไม่ใช่ dropdown/chip เลือก) แต่ละการ์ดมี Header: ชื่อครั้งที่ประชุม, ไอคอนเส้น (แก้ไข/ย้ายไปที่เก็บ — ตาม `DESIGN.md` ข้อ 5 ห้ามใช้ emoji), ป้ายสถานะรวม, ปุ่ม `+ สร้างวาระ`
    - สร้าง Summary Section (ข้อความกำหนดการ และ Workload แบบ Mock ข้อความไปก่อน) — ไม่ต้องมีกล่อง KPI แยก
3.  **Task Table/List Structure:**
    - สร้างโครงสร้างหัวตาราง (วาระ/เรื่อง | ผู้รับผิดชอบ | สถานะ | ด่วน)
4.  **Archive & Trash (Mock):**
    - กด "ย้ายไปที่เก็บ" บนการ์ดครั้งที่ประชุม → ย้าย (client-side) ไปหน้าถังขยะ ไม่ลบทันที
    - กด "ลบ" ที่แถววาระ/เรื่อง → ย้ายไปหน้าถังขยะเช่นกัน (พร้อมอ้างอิงครั้งที่ประชุมต้นทาง)
    - หน้าถังขยะแยกต่างหาก เข้าถึงจากไอคอนที่ Header มีปุ่มกู้คืน/ลบถาวรต่อรายการ ตาม `DESIGN.md` ข้อ 4.6
    - _-> หยุดและรอ Approval_

---

## Phase 3: Task Management & Core UI Components

**เป้าหมาย:** สร้างระบบการเพิ่มวาระ/เรื่อง และการ์ดแสดงผลชิ้นงานให้สมบูรณ์

1.  **Create Task Functionality:**
    - สร้าง Modal หรือ Form เมื่อกดปุ่ม `+ สร้างวาระ`
    - รับข้อมูล: ชื่อวาระ, เลือกผู้รับผิดชอบ (Owner), ตั้งค่าด่วน (🔥)
2.  **Create Meeting Session Functionality** _(เพิ่มเข้ามาระหว่างตรวจ Phase 3 — ผู้ใช้ขอให้ปุ่ม `+ สร้างครั้งที่ประชุม` ที่เดิมเป็น Mock เฉยๆ ตาม Phase 2 ให้ทำงานได้จริงในรอบนี้เลย):**
    - เปิดใช้งานปุ่ม `+ สร้างครั้งที่ประชุม` ให้ขยายเป็นฟอร์มตามหลัก Progressive Disclosure เดียวกับฟอร์มสร้างวาระ
    - รับข้อมูล: ชื่อครั้งที่ประชุม อย่างเดียว
    - ยังเป็น Mock Data (เก็บใน React state ฝั่ง client เหมือนวาระ) ไม่เชื่อม Supabase ตาม Build Strategy เดิม
3.  **Task Card Component:**
    - สร้าง Component แถว/การ์ดแสดงชิ้นงาน ตาม `DESIGN.md`
    - แสดงชื่อเรื่อง จัดชิดซ้าย
    - แสดงป้าย (Badge) ชื่อผู้รับผิดชอบ
    - แสดงไอคอนไฟ (🔥) หากเป็นเรื่องด่วน
    - แสดงปุ่มลบ (Delete) ขวาสุด
    - _(เรื่อง "สถานะ" ปล่อยเว้นว่างไว้ก่อนตามกฎของ State เริ่มต้น)_
4.  **User Check & Output:**
    - ทดสอบสร้างวาระใหม่ สังเกตการแสดงผลในตารางว่าข้อมูลครบและตรงตาม UI หรือไม่
    - ทดสอบสร้างครั้งที่ประชุมใหม่ สังเกตว่าการ์ดใหม่ขึ้นมาถูกต้อง
    - _-> หยุดและรอ Approval_

---

## Phase 4: Workflow, State Machine & Notifications

**เป้าหมาย:** นำระบบ Workflow การส่งตรวจ (Inspector Routing) และการแจ้งเตือน (Notifications) มาใช้งานจริง

1.  **Routing & Dropdown (การส่งตรวจ):**
    - เพิ่มปุ่มหรือกลไกให้ Owner กด "ส่งตรวจ" ที่วาระนั้นๆ
    - สร้าง Dropdown รายชื่อผู้ตรวจ (Inspector) ให้เลือก
    - **กลุ่มผู้ตรวจตาม Sub-tab (เพิ่ม 2026-09-14):** ผู้ตรวจแต่ละคนกำหนดได้ว่าเกี่ยวข้องกับ Sub-tab ไหนบ้าง (Resume / ร่างรายงาน / Conduct — ใช้ชุดเดียวกันทั้ง Board และ Excom) ผ่านหน้า `/admin/users` — default ไม่ระบุ = โชว์ทุก Sub-tab ปรับจำกัดได้ทีหลัง ใช้คุมทั้ง Dropdown "เลือกผู้ตรวจ" และแถบสรุป "เอกสารรอตรวจอยู่ที่" ให้ตรงกับ Sub-tab ที่กำลังดูอยู่ (ดูรายละเอียดที่ `DESIGN.md`)
2.  **State Machine (การเปลี่ยนสถานะ):**
    - เมื่อ Owner กดส่ง -> อัปเดตสถานะงาน และเปลี่ยนป้ายสถานะเป็นการ์ดข้อความ เช่น `ส่ง พี่ปู` (สีแดง)
    - เมื่อ Inspector ที่ได้รับมอบหมาย Login เข้ามาดูงาน -> ให้มีปุ่ม `ตรวจแล้ว`
    - เมื่อ Inspector กดตรวจแล้ว -> อัปเดตสถานะ และเปลี่ยนป้ายสถานะเป็น `พี่ปู แล้ว` (สีเขียว) กลับไปหา Owner
    - เพิ่มปุ่มให้ Owner กด `ปิดงาน` -> เปลี่ยนสถานะเป็น `ปิดงาน` (สีเทา)
    - แต่ละวาระ/เรื่องต้องมีปุ่มแก้ไข (เปิดฟอร์มแก้ชื่อ/ECM/ผู้รับผิดชอบ/วันครบกำหนด/ด่วน/หมายเหตุ) และกดที่ตัวแถวเพื่อขยายดูรายละเอียดเต็ม + ปุ่ม action ตามสถานะปัจจุบัน
    - เก็บ **Log ประวัติการดำเนินการ** ต่อวาระ (ส่งตรวจ/ตรวจแล้ว/ปิดงาน พร้อมชื่อผู้ตรวจและเวลา) แสดงในส่วนที่ขยายออกมา
    - **ตัดสินใจแล้ว (2026-09-14):** ไม่ล็อกปุ่มตาม Role ของผู้ login (ยกเลิกแนวคิด "ตรวจแล้ว = สิทธิ์ Inspector เท่านั้น, ส่งตรวจ/ปิดงาน = สิทธิ์ Owner เท่านั้น" ที่เคยวางแผนไว้) — เปลี่ยนเป็นหลักการ **"ยืดหยุ่น"**: ใครที่ login อยู่กดปุ่มไหนก็ได้ทั้งหมด แต่ log บันทึกโดยอ้างอิงจาก field `task.owner`/`task.inspector` ของวาระนั้นเสมอ ไม่ใช่ตัวตนคนกด (เช่น "ส่งตรวจ" ลง log เป็น `{owner} (ผู้รับผิดชอบ) ส่งตรวจ {inspector}` เสมอ) — ปุ่ม "ส่งตรวจ"/"ตรวจแล้ว" เปิด Modal ยืนยันกึ่งกลางจอ มีช่อง "บันทึกช่วยจำ / Message" ให้กรอก note ประกอบทุกครั้ง (เก็บใน `HistoryEntry.message`, แสดงใน Log และเตรียมไว้ใช้เป็นข้อความส่ง LINE Notification ในอนาคต) ดูรายละเอียด UI ที่ `DESIGN.md` ข้อ 4.5.1
3.  **Notification System (In-app Badges):** ✅ เสร็จแล้ว (2026-09-14)
    - เขียน Logic คำนวณจำนวนชิ้นงาน (Workload) ที่รอการ Action
    - นำตัวเลขไปแสดงผลในจุดต่างๆ:
      - แสดงเป็น **Red Badge** (วงกลมสีแดง) บน Tabs หลักและ Tabs ย่อย
      - แสดงสรุปตัวเลขบน Summary Section ของแต่ละครั้งที่ประชุม (เช่น _เอกสารรอตรวจอยู่ที่ พี่ปู 3_)
    - **ตัดสินใจแล้ว (2026-09-14):** Red Badge คือ **แจ้งเตือนเฉพาะคนที่ login อยู่** (personalized ตาม `PRODUCT.md` ข้อ 5/7 "แจ้งเตือนถูกคน ถูกเวลา") ไม่ใช่ยอดรวมทุกคนแบบเดียวกับ Summary Section — นับจากสถานะ `blank`/`pending` ที่ตนเป็น owner/inspector ตามลำดับ บวกสถานะ `done` ที่ตนเป็น owner ไม่รวม `closed` เด็ดขาด
    - **ขยาย scope เพิ่มเติมจากที่ระบุไว้เดิม (ผู้ใช้อนุมัติแล้วทั้ง 2 รอบ):** เพิ่มการเลือก **ทั้ง Board/Excom และ Resume/ร่างรายงาน/Conduct** ตอนสร้างครั้งที่ประชุม (`Meeting.tab` + `Meeting.subTab`) เพื่อให้ Red Badge/รายการครั้งที่ประชุมแยกทั้ง Tab หลักและ Sub-tab ได้จริง — ปิด known gap เดิมทั้งหมดที่ Board/Excom และ Sub-tab เคยโชว์ข้อมูลชุดเดียวกันซ้ำกันทุกกล่อง ตรงกับที่ `PRODUCT.md` ข้อ 3 ตั้งใจไว้แต่แรก (ดูรายละเอียดที่ `DESIGN.md` ข้อ 4.2-4.5)
4.  **User Check & Output:**
    - ทดลองจำลอง Role ข้ามไปมา (Login เป็น Owner -> ส่งงาน -> Login เป็น Inspector -> รับ Notification สีแดง -> กดตรวจแล้ว -> Login เป็น Owner -> ปิดงาน)
    - _-> หยุดและรอ Approval_

---

## Phase 5: Final Polish & Dashboard (Optional for V1)

**เป้าหมาย:** เก็บตกข้อผิดพลาด ปรับแต่ง UI ให้สมบูรณ์ และเพิ่มหน้าสรุปผล

1.  **Workload Dashboard:** ✅ เสร็จแล้ว (2026-09-15)
    - สร้างหน้า Dashboard หรือตารางสรุปแบบรวบยอด ว่าพนักงาน/ผู้ตรวจแต่ละคนมีงานค้างรวมจากทุกแท็บกี่ชิ้น
    - ผู้ใช้ยืนยันให้ทำ (ไม่ข้าม แม้ระบุว่า optional ไว้) — สร้าง `WorkloadDashboard.tsx` เข้าถึงผ่านไอคอนกราฟที่ Header แบ่งเป็นตารางสรุปรายคน (Assigned/On hand/In Process/Done/งานที่ต้องตรวจ) + Heat Map (คน × Tab/Sub-tab) ดูรายละเอียดครบที่ `DESIGN.md` ข้อ 4.8
    - **ขยาย scope เพิ่มเติมระหว่างทำ (ผู้ใช้ขอเพิ่ม ไม่ได้ระบุไว้ใน Phase 5 เดิม):** Drag and Drop จัดลำดับแถวในหน้านี้ได้ เฉพาะ role Admin — เป็นระบบลำดับแยกเฉพาะหน้า Dashboard เอง ไม่ใช้ร่วมกับลำดับผู้ใช้ที่ `/admin/users` (คอลัมน์ `users.dashboard_sort_order` + RPC `list_workload_people`/`admin_set_dashboard_order` ใหม่)
    - **ฟีเจอร์เสริมอื่นที่เกิดจาก brainstorm ระหว่างทำ Phase 5 (นอก scope เดิมเช่นกัน):** หน้า **"งานของฉัน"** (`MyTasksView.tsx`) รวมงานที่ต้องดำเนินการข้ามทุก Tab เรียงตามวันครบกำหนด และ **ลบขั้นตอน default ออกจาก Progress Tracker ได้เป็นรายวาระ** (เผื่อข้ามขั้น/ผู้ตรวจลางาน) — ดูรายละเอียดที่ `DESIGN.md` ข้อ 4.10 และข้อ 4.5.1 ตามลำดับ
2.  **Edge Cases & Validation** และ **3. Final Review** — **ย้ายไปรวมเป็น Phase 7 แทน (ตัดสินใจ 2026-09-15)** เหตุผล: ระหว่าง brainstorm เพิ่มฟีเจอร์ "บอร์ด/โน้ต" (ดู Phase 6) ผู้ใช้ตั้งข้อสังเกตว่า Final Review ควรเป็นด่านสุดท้ายจริงๆ ไม่ใช่ด่านก่อนสุดท้าย — ถ้าทำ Final Review ตรงนี้ก่อนแล้วค่อยเพิ่ม Phase 6 ทีหลัง จะต้องทำ Final Review ซ้ำสองรอบ จึงย้ายทั้งสองข้อไปทำรวบยอดครั้งเดียวหลัง Phase 6 เสร็จแทน (ครอบคลุมทั้ง Dashboard + บอร์ดในรอบเดียว)

---

## Phase 6: บอร์ด / โน้ต (Board & Notes) — ✅ **อนุมัติแล้ว 2026-09-16** (ผู้ใช้ยืนยัน "Phase 6 เห็นชอบแล้ว")
> หมายเหตุ: หลังอนุมัติยังมีการปรับตามคำสั่งผู้ใช้อีกรอบใหญ่ในวันเดียวกัน (Red Badge แบบ unread + ป้าย New · ตัวเลขถาวรท้ายแท็บ · **เลิกใช้ Private Memo**) ดู `PRODUCT.md` ข้อ 10 และ `DESIGN.md` ข้อ 4.12

**เป้าหมาย:** เพิ่มพื้นที่สื่อสารนอกเหนือจาก workflow ส่งตรวจของวาระเดิม สำหรับ Note/Reminder/Todo ทั่วไปที่ไม่ต้องมีสถานะ/ผ่านการตรวจ (เช่น "พรุ่งนี้อย่าลืม...", "วันที่... ต้องทำ...") — มาจาก brainstorm ระหว่าง Phase 5 (2026-09-15) ผ่านการถามตอบและดู mockup ยืนยันหน้าตาจนได้ข้อสรุปแล้ว ดูดีไซน์เต็มที่ `DESIGN.md` ข้อ 4.11 และขอบเขตที่ `PRODUCT.md` ข้อ 10

1.  **หน้าบอร์ดกลาง** (หน้าใหม่ เข้าถึงจากไอคอนที่ Header เหมือน Dashboard/งานของฉัน/ถังขยะ):
    - แสดงทุกกระทู้ที่ไม่ได้ติ๊ก "ส่วนตัว" ตอนสร้าง เรียงล่าสุดก่อน เป็นการ์ดฟีด (ไม่ใช่ตารางแบบเว็บบอร์ดเก่า)
    - Tabs กรอง: **ทั้งหมด / แท็กฉัน / ของฉัน**
    - ปุ่ม `+ สร้างกระทู้`
    - แต่ละกระทู้ตอบกลับ/คอมเมนต์ได้ (thread เต็มรูปแบบ)
2.  **สร้างกระทู้ — รับข้อมูล:**
    - ข้อความ (Note/Reminder/Todo อิสระ)
    - Tag คนได้หลายคน (optional)
    - วันที่เตือน จริง มี date picker (optional) — เก็บเป็นวันที่จริงไว้ตั้งแต่ตอนนี้ เผื่อใช้ต่อยอดทำ Dashboard "งานด่วน/งานที่ต้องทำ" ในอนาคต (ผู้ใช้ระบุไว้ชัดเจนว่าจะทำต่อ แต่ไม่ใช่ scope ของ Phase 6 นี้)
    - Checkbox **"ส่วนตัว"** — ถ้าติ๊ก ไม่ขึ้นหน้าบอร์ดกลาง แต่ยังขึ้นหน้า "งานของฉัน" ของคนสร้าง+คนที่ถูก Tag ตามปกติ
3.  **หน้า "งานของฉัน" (`MyTasksView.tsx`):** เพิ่มหัวข้อใหม่ **"บอร์ด/โน้ตของฉัน"** ต่อท้ายรายการวาระเดิม แสดงกระทู้ที่เราสร้างเองหรือถูก Tag (รวมกระทู้ส่วนตัวด้วย) เรียงตามวันที่เตือนใกล้สุดก่อน (แถวกระชับ ไม่ใช่การ์ดเต็มแบบหน้าบอร์ด)
4.  **การแจ้งเตือน:** Red Badge ในแอปเท่านั้น ใช้กลไกเดียวกับระบบเดิม (ไม่ต่อ Email/LINE ใน Phase นี้ — เก็บไว้ทำต่อภายหลัง ตรงกับ `PRODUCT.md` ข้อ 7 ที่ระบุไว้แต่เดิมว่า V1 ไม่มี Line OA Notification)
5.  **การปิด/ลบ:** ต้องเข้าถังขยะก่อนเสมอ เหมือนวาระ/เรื่องปัจจุบัน (กู้คืนได้จากถังขยะ) — เมื่อปิด/ลบสำเร็จแล้ว หายจากทุกที่ทั้งบอร์ดกลางและหน้างานของฉันของทุกคนที่เกี่ยวข้องทันที
6.  **User Check & Output:**
    - ทดสอบสร้างกระทู้ทั้งแบบติ๊ก/ไม่ติ๊ก "ส่วนตัว", Tag หลายคน, ตอบกลับกระทู้
    - ทดสอบลบกระทู้ไปถังขยะแล้วกู้คืนกลับมาถูกต้อง
    - ทดสอบว่ากระทู้ที่ถูก Tag ไปขึ้นหน้า "งานของฉัน" ของคนที่ถูกแท็กและคนสร้างถูกต้อง (รวมกระทู้ส่วนตัว)
    - _-> หยุดและรอ Approval_

---

## Phase 7: Edge Cases, Validation & Final Review

**เป้าหมาย:** ตรวจสอบรวบยอดทั้งระบบครั้งเดียว (ครอบคลุม Dashboard จาก Phase 5 และบอร์ดจาก Phase 6) ก่อนปิดโครงการ — ย้ายมาจาก Phase 5 ข้อ 2-3 เดิม (ดูเหตุผลที่ Phase 5)

1.  **Edge Cases & Validation:**
    - ทดสอบระบบการลบงาน (ปุ่มลบ) ทั้งวาระและกระทู้บอร์ด
    - ทดสอบเมื่อกดสลับ Tabs ไปมา ข้อมูลต้องไม่หาย
    - ปรับปรุง Responsive Design ให้ใช้งานบนมือถือได้ลื่นไหล
2.  **Final Review:**
    - ตรวจสอบระบบทั้งหมดเทียบกับ `PRODUCT.md` และ `DESIGN.md` (รวมฟีเจอร์บอร์ดที่เพิ่มใน Phase 6)
    - _-> โครงการเสร็จสมบูรณ์เมื่อได้รับ Approval สุดท้าย_

```eof

```


---

## Phase R: Code Review & Remediation (2026-09-15/16) — ✅ ทำไปแล้ว 4 ชุด

**ไม่ใช่ Phase สร้างฟีเจอร์** — แทรกเข้ามาหลังผู้ใช้ให้ผู้ตรวจอิสระรีวิวโค้ดทั้งระบบ แล้วสั่งแก้ทีละชุด
**ไม่กระทบลำดับเดิม:** Phase 6 ยังรอ Approval · Phase 7 ยังไม่เริ่ม

**เอกสารหลักของ Phase นี้:** `CODE-REVIEW.md` (ผลตรวจ) · `FIX-INSTRUCTIONS.md` (แผน Phase A–H + บันทึกการตัดสินใจของผู้ใช้)

| ชุด | เรื่อง | สถานะ |
|---|---|---|
| R1 | ปิดช่องโหว่ RPC เปิดให้คนนอกเรียก (ยึด admin ได้ใน 3 requests) + memo ส่วนตัวในถังขยะรั่ว + cookie ทำให้ login วนลูปตอน build จริง | ✅ |
| R2 | ดึง SQL ทั้ง 42 ฟังก์ชันลง `supabase/migrations/` (เดิมมีแค่บน production) | ✅ |
| R3 | บั๊กย้อนสถานะ + เปลี่ยนเป็นถามก่อนบันทึกย้อนหลัง + ติดตั้ง vitest (12 tests) | ✅ |
| R4 | ผูกคนด้วยรหัสประจำตัวแทนชื่อ (`owner_id`/`inspector_id`) + ลบคอลัมน์เดิม | ✅ |
| R5 | ประสิทธิภาพ: ลบ `revalidatePath` 20 จุด + รับ props ใหม่จริง + useMemo + แก้ปัญหาแก้ชนกันติดค้าง | ✅ |
| R6 | **Badge สะสมไม่มีวันเป็นศูนย์** | ⏸️ **ผู้ใช้ขอคุยหลักการก่อน — ห้ามเริ่มเอง** |
| R7 | Accessibility (คีย์บอร์ด/Modal/focus ring/ขนาดตัวอักษร) | ⬜ ยังไม่เริ่ม |
| R8 | แยก 5 หน้าจอเป็น route จริง | ⬜ ยังไม่เริ่ม |

**งาน LINE Login** (ผูก LINE ID ผ่าน LINE OA แล้วให้ตั้งรหัสผ่านสำหรับ Desktop ทีหลัง) เป็นแผนของผู้ใช้เอง
→ งาน session revoke ให้รอทำพร้อมงานนี้ **อย่าลงแรงรื้อระบบ session ตอนนี้**
